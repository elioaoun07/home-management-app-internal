# PM Command Center (`pnpm pm`, Delivery, `/pm`, `/pm/live`)

**Type:** Cross-cutting (tooling — not an app module)
**Routes:** local `pnpm pm` (React primary, `?ui=classic` Preact reference, `?ui=old` legacy rollback) · hosted read-only `/pm` · phone relay `/pm/live`
**Owners:** [PM Tooling](../../../10%20-%20Project%20Management/PM%20Tooling/PM%20Tooling%20—%20Master%20Book.md) (surfaces, parsing, identity) · [Delivery](../../../10%20-%20Project%20Management/Delivery/Delivery%20—%20Master%20Book.md) (execution) · accepted spec [Plans/Command Center](../../../10%20-%20Project%20Management/Plans/Command%20Center.md)

## What it does

Reads the eleven campaign checklists and Master Books under `ERA Notes/10 - Project Management/` as the only backlog, lets the owner organize and complete outcomes, and runs Delivery sessions (V1 operating, V2 prepared for the first owner-started desktop trial). Markdown is the task store; `.delivery/` and `.pm/` hold runtime state.

**Local Delivery setup (2026-09-13):** V2 is ready for the owner-started DLV-107 desktop trial. `scripts/delivery-v2/setup-subscriptions.mjs` provisions native subscription credentials and a dedicated provider proxy; `worker/subscription.mjs` rejects paid/unknown auth and unavailable included usage before SDK jobs; `worker/egress.mjs` enforces exact-host tunnels and kernel UID routing; `worker/seccomp.mjs` adds scoped nested-sandbox support to Moby's default policy. `worker-boundary.mjs` owns isolated volumes, stdin dispatch and auth readiness; `probes/worker-qualification.mjs --boundary <file> --backend <id>` qualifies exact image/profile/proxy bindings. `trial-checks.mjs` renders DLV-107's real missing-item link against synthetic receipts. Active policy and credential artifacts stay local under `.delivery/` and Docker named volumes. See Delivery DLV-96/97/110/111 and the local test wizard for current evidence and expiry/remote limits.

## Files at a glance

- **Shared parsing (every surface):** `scripts/pm/shared/md-scan.mjs` (checkbox ordinals), `tasks.mjs` (row grammar), `work-id.mjs` (canonical IDs, references, `### <ID>` lookup), `links.mjs` (GitHub heading slugs/anchors), `declarations.mjs` (`Depends on`, `Touches`, HELD — read by the portfolio and by V2 coordination), `text.mjs`, `frontmatter.mjs`
- **Read models:** `scripts/pm/shared/product.mjs` (campaigns, decisions, history), `scripts/pm/shared/portfolio.mjs` (contracts, dependencies, topics) — the classic `scripts/pm/src/lib/product.js` / `portfolio.js` only re-export them
- **History and metrics (Phase 6):** `scripts/pm/shared/history.mjs` (the one history parser: Shipped Log / Cancelled Log records, identity exact/referenced/unidentified, date day/range/unrecorded, notes — used by the app, classic build and the bridge's legacy `history` row), `scripts/pm/shared/metrics.mjs` (open work, work outcomes, weekly series, bugs, sprint placeholder, delivery attempts, resources, coverage)
- **Primary React app:** `scripts/pm/app/` — `App.tsx` (Memory mark; Home / Work / Delivery / Analytics navigation), `Home.tsx` (queue cards, module grid, A place to begin), `Explore.tsx` + `explore-state.ts` (search/status/layout URL state), `Board.tsx`, `Work.tsx` (item detail + Organize), `Delivery.tsx`, `Run.tsx` (V1 session), `DeliveryV2.tsx` (V2 launch, run, pairing), `DeliveryQuestion.tsx` + `deliveryQuestionModel.ts` + `delivery-question.css` (supplied option text → editable answer), `v2model.ts` (V2 labels, cost rows), `Auxiliary.tsx` (Reader, Activity, Capture), `Dashboard.tsx` + `AnalyticsOverview.tsx` + `dashboard.css` (`#/analytics`; `#/dashboard` alias), `components.tsx` (Markdown, HistoryList, visible WorkHeading IDs), `model.ts` (`buildWorld`, `briefPath`, `historyPath`), `state.tsx`, `api.ts` (`v2Read`/`v2Post` carry the CSRF token), `types.ts`; built by `scripts/pm/app-build.mjs`, shell `app-shell.mjs`
- **Local server + writes:** `scripts/pm-server.mjs` (`/api/data`, mutations, SSE, Delivery routes), `scripts/pm/mutations.mjs`, `scripts/pm/write-guards.mjs` (row witnesses, Undo guards), `scripts/pm/archive.mjs` (Ship/Discard/sweep), `scripts/pm/health.mjs`
- **Classic/static/legacy:** `scripts/pm/src/` (Preact, `store.js` writes), `scripts/pm/build.mjs`, `scripts/pm/ui.mjs`, `scripts/build-pm-dashboard.mjs` (`_dashboard.html`, hosted `public/pm.html`), `scripts/pm/client.js` (legacy rollback)
- **Governance CLIs:** `scripts/pm/lint.mjs` (`pnpm pm:lint`), `scripts/pm/check-docs.mjs` (`pnpm pm:check-docs`), `scripts/pm/scan.mjs`
- **Delivery V1:** `scripts/delivery/` (`server-routes.mjs`, `run-session.mjs`, `budgets.mjs`, drivers)
- **Delivery V2:** `scripts/delivery-v2/` — `journey.mjs` (the one run lifecycle: deliver, plan/decide/answer/message, dispatch, checks, Result, controls, projections), `interaction.mjs` (plans, questions, receipts, instructions), `work-ref.mjs` (witnessed selection, acceptance revision), `contracts.mjs`, `entry.mjs` (HTTP routes), `local-auth.mjs` (paired session, CSRF, bridge credential), `policy.mjs` (`buildDeliver`, catalog/runtime/checks/dispatch sections), `jobs.mjs` (admission, exclusive claim, last-moment checks, resources, in-transaction `gate`), `coordination.mjs` (parallel items: verdicts and reasons, footprints, prerequisites, writer/job slots, fleet allowance), `store.mjs` (schema 5; `runs.coordination_json`), `qualification.mjs` (bound receipts), `worker-boundary.mjs` (container worker/checker) + `worker/runner.mjs` (runs inside the worker), `checks.mjs`, `results.mjs`, `service.mjs`, `adapters/` (per-run model/effort, effective settings, activity), `probes/` (canary battery, `container-feasibility.mjs`, `worker-qualification.mjs`)
- **Transport seam (Phase 4):** `scripts/pm/app/transport.ts` (interface, capabilities, `PendingCommand`), `localTransport.ts` (Node server), `CommandCenter.tsx` (the shared views both shells mount), `api.ts` (thin delegates)
- **Protected Apply:** `scripts/delivery-v2/apply.mjs` (plan / journal / write / inspect / rollback / integrated snapshot — no process, Git or DB), journey `apply` command (`apply`, `resume`, `rollback`, `recheck`), store `applications` table (schema 4, partial unique index = exclusive claim), `POST /api/delivery/v2/apply`
- **Phone relay:** `scripts/pm/relay-shared.mjs` (row ids, receipt states, snapshot assembly, liveness, attention keys — used by both ends), `scripts/pm/relay.mjs` (installation, corpus rows, secret guard, command journal + reconciliation, V2 command execution, attention ledger, availability, lock), `scripts/pm/bridge.mjs` (wiring); phone: `src/app/pm/live/page.tsx` → `CommandCenterEntry.tsx` → `CommandCenterLive.tsx`, `src/features/pm-live/relay/` (`transport.ts`, `commands.ts`, `cache.ts`); legacy rollback `?ui=legacy`: `src/components/pm-live/`, `src/features/pm-live/`; setup: `ERA Notes/06 - Setup & Onboarding/PM Relay Setup.md`
- **Tests:** `tests/pm-ui/` (queue labels: `coordination-model.test.ts`; metric contract, reconciliation, relay parity and a read-only live-corpus check: `metrics.test.ts`), `tests/delivery-v2/` (Apply: `apply.test.ts`; parallel items: `parallel.test.ts`, rules: `coordination.test.ts`; shared fixture `fixtures/v2-harness.ts`), `tests/delivery/`, `tests/pm-bridge.test.ts`, `tests/pm-relay.test.ts` (bridge over a Supabase double), `tests/pm-live-relay.test.ts` (phone transport, cache, intents), `tests/pm-live-query.test.ts`, `tests/pm-live-derive.test.ts`

## Common edit scenarios

- **"Already implemented / owner UAT / Done / should this be deliverable?"** → `shared/work-lifecycle.mjs` is the explicit `Execution` / `Implementation` guard, shared by `delivery-v2/work-ref.mjs` and V1 `server-routes.mjs`. `shared/product.mjs` excludes owner-only checks; `portfolio.mjs` preserves their dependency gate. `app/model.ts#doneReceipts`, `Explore.tsx` (`?view=done`), `Work.tsx` and `components.tsx` preserve completed scope/receipts and V1/V2 history. `pm/check-docs.mjs` rejects owner/open-completed checklist drift. Tests: `tests/pm-ui/work-lifecycle.test.ts`, V1 route and V2 entry/source tests. Manual results live in `docs/Delivery-UAT.md`.

- **"Delivery tabs / plan.md / file diff / readable verification"** → `scripts/pm/app/DeliveryReview.tsx`, `deliveryReviewModel.ts`, `delivery-review.css`; shell/actions in `DeliveryV2.tsx`. Product order is Plan → Verification → Activity → Changes. Plan artifacts sit at right on desktop; Risks/Unknowns, current/previous verification, application history and run facts stay visible. `scripts/delivery-v2/review.mjs` reads bounded candidate/base bytes only after hash verification; `interaction.mjs` retains complete plan records. Tests: `tests/pm-ui/delivery-review.test.ts`, `tests/delivery-v2/review.test.ts`.

- **"An item's acceptance/dependencies don't show"** → `work-id.mjs` (`idSection`, `workIds`) and `portfolio.js`; add a case to `tests/pm-ui/work-identity.test.ts`.
- **"A link/anchor opens the wrong place"** → `links.mjs` (`slugify`, `headingAnchors`), `model.ts` (`briefPath`, `historyPath`), Reader in `Auxiliary.tsx`.
- **"Complete/move/ship/discard hit the wrong row"** → client witnesses in `Work.tsx` / `src/app/store.js`, server check `write-guards.mjs` via `pm-server.mjs` ops.
- **"Delivery launch/selection"** → V1 `scripts/delivery/server-routes.mjs`; V2 `entry.mjs` → `journey.mjs` (`deliver`) → `policy.mjs` → `work-ref.mjs`.
- **"Focused profile / prepared plan / missing task context"** → `scripts/delivery-v2/task-input.mjs` (versioned advisory split, readiness and brief), `policy.mjs` (resolved profile/input freeze), `entry.mjs` (no-job preparation), `journey.mjs` (queue/continuation/recovery and scope stop), `worker/task-brief.mjs` + `worker-boundary.mjs` (recovery file). Plan display: `scripts/pm/app/DeliveryReview.tsx`, `deliveryReviewModel.ts`. Fixtures: `tests/delivery-v2/focused-delivery.test.ts`, `task-input.test.ts`, `task-brief-boundary.test.ts`, `worker-task-brief.test.ts`; source format in `Plans/Delivery V2.md`.
- **"V2 run stuck / wrong state"** → `journey.mjs` (`afterJob`, `ownerActionFor`, `control`), `jobs.mjs` (claims, `reconcileOutstanding`); fixtures `tests/delivery-v2/journey.test.ts`.
- **"Executor not offered / refused"** → `service.mjs` `describeExecutors`, `qualification.mjs` (receipt binding), `adapters/registry.mjs` (`validateRunSettings`), policy `executors.catalog`.
- **"V2 command returns 401/403"** → `local-auth.mjs`; pair with the code `pnpm pm` prints (or `pnpm pm:pair`).
- **"Home/Work/Delivery screen"** → `scripts/pm/app/`; keep the classic build passing (`tests/pm-ui/build-smoke`, `static-twin`). Never build a URL or assume a write in a view — ask `transport()` and its `capabilities`.
- **"Home queues / Analytics / keep search / work IDs"** → `Home.tsx`, `App.tsx`, `AnalyticsOverview.tsx`, `Dashboard.tsx`, `Explore.tsx`, `explore-state.ts`, `Board.tsx`, `components.tsx#WorkHeading`. Queue buckets (lowercase `now/next/waiting/later`) are filters; Board's capitalized `Now/Next/Later` lane is the selected phone column. Keep these distinct while preserving search and campaign context. `#/dashboard` remains an Analytics alias; daily totals count dated shipped receipts, not unique completed work. PM Tooling R69 owns the presentation scope.
- **"Delivery question choices / recommended answers"** → `DeliveryQuestion.tsx`, `deliveryQuestionModel.ts`, `delivery-question.css`; regression: `tests/pm-ui/delivery-question.test.ts`. The current backend retains only question text and blocking status. Render choices already explicit in that text, never infer an answer or recommendation; keep free text and the existing answer command. Guaranteed generated choices require a separate worker/protocol change, outside R69.
- **"Phone shows stale / wrong / another laptop's data"** → `src/features/pm-live/relay/transport.ts` (`fullLoad`, `replaceRows`, `chooseInstallation`), `cache.ts` (owner/installation/schema keys); bridge publishers in `bridge.mjs`.
- **"Phone command never answers / ran twice"** → phone `relay/commands.ts` (intent id = row id, `resume`); bridge `drainOnce`/`reconcileClaimed` + `relay.mjs#reconcileClaimedCommand` and `.delivery/pm-relay/commands.ndjson`.
- **"Apply refused / conflict / interrupted"** → `apply.mjs#planApplication` (refusal codes), journey `applyCandidate`/`resumeApplication`/`rollbackApplication`, `recoverApplications`; fixtures `tests/delivery-v2/apply.test.ts`.
- **"An item waits / Scope required / Capacity full"** → rules in `coordination.mjs` (`relate`, `coordinate`, `capacityReasons`); journey `evaluateCoordination`, `reservingRuns`, `drainQueue`; the item's `**Touches:**` / `**Depends on:**` lines and HELD title; queue view `QueueV2` in `DeliveryV2.tsx`, owner-facing labels in `v2model.ts#reasonLabel`; fixtures `tests/delivery-v2/parallel.test.ts`.
- **"Apply says Fails on current source"** → journey `reassess` over `apply.mjs#previewIntegration`; the base drifted since the candidate was checked and the checker failed the preview. Nothing was written.
- **"A dashboard number looks wrong / a record is missing"** → parsing in `shared/history.mjs` (`parseHistory`, `receiptIdentity`), counting in `shared/metrics.mjs`; the Dashboard only renders. Add the case to `tests/pm-ui/metrics.test.ts` and keep the reconciliation assertions green.

## Gotchas

- Compare IDs normalized (`SCH-4.3b` ≡ `SCH-4.3B`); display the source spelling (`Work.label`). References never truncate to a different ID.
- Anchors are GitHub slugs (one hyphen per space, none collapsed) — the same function `pnpm pm:check-docs` validates links with.
- Checkbox writes require `expectLine` (428 without); a changed row, mismatched `expectId` or duplicate ID in the file is 409. Never resolve a write by ordinal alone.
- V2 `deliver` requires a selection witness and binds the Master Book `### <ID>` revision into the contract. The run names its executor, model and effort; there is no installation default (`POST /api/delivery/v2/executor` is 410).
- V2 dispatch needs a container `runtime` in `.delivery/v2/execution-policy.json` and a qualification receipt bound to that image/SDK/boundary; without both, launch refuses. Every provider call is preceded by an exclusive claim and a recheck of source, acceptance, grant, policy revision, qualification and resources.
- V2 commands authenticate by paired session + `x-era-csrf`; the body's `actor` is ignored. Unknown usage stays unknown (never $0); a failed PM writeback retries only the writeback.
- V2 launch recommendations are deterministic local policy and persist with any owner override in `runs.settings_json`; the production launch UI does not print their rationale. Focused is bounded to one plan and implementation job with no automatic repair; use Investigate for further discovery.
- Prepared Focused plans may omit the planning job only with explicit complete owner-reviewed low-risk material and existing runnable checks. They still require owner approval, one implementation, protected verification and Apply. New `binding-v2:` contracts separate explicitly delimited advisory guidance; legacy fingerprints remain whole-section. Worker images must advertise `task_brief_version: 1`; old images refuse qualification binding rather than lose the recovery input.
- `worker/runner.mjs` runs only inside the worker image; `ERA_V2_SYNTHETIC=1` restricts it to scripted writes. The real-container smoke is opt-in: `ERA_V2_DOCKER=1 pnpm exec vitest run tests/delivery-v2/worker-boundary.docker.test.ts`.
- `_Archive/` is never scanned. Only `_Archive/Cancelled Log.md` is read, as history (`world.sources`), never as backlog.
- No surface writes production data (Hard Rule 26); relay schema changes are owner-run migrations (`migrations/2026-09-12_pm-v2-relay.sql`).
- Relay rows are `cc:<installation>:<kind>[:<key>]`; a phone command's id is its relay row id and its V2 `command_id`, so a retry is the same command end to end. A timeout or a stale heartbeat is never shown as failed or stopped.
- Apply never runs candidate code on the host: integrated checks go through `runtime.checkExecutor` only; with no checker the application is `checks-pending`, not `applied`. The integrator refuses protected paths (`PROTECTED_PATHS` + policy `apply.protectedPaths`), links/junctions, case collisions and any path changed since the base.
- Parallel items (Phase 5): at most two writers (`concurrency.maxWriters`; the ceiling is `WRITER_CEILING` in code). Every job that may still be running — reserved, active, paused, unknown, or asked to stop without an observed stop — holds a job slot. Writer slots are derived from job and run state, not a stored lock. A missing `**Touches:**` line is unknown scope and blocks writing beside another reservation. A verified, unapplied candidate reserves its paths until applied, rolled back, superseded or released (control `release`). The queue rides the relay `v2runs` row; pre-launch `assess` is local only. V1's `isBuildLockActive` guard is unchanged.
- `/pm/live` mounts the shared views; `?ui=legacy` is the rollback. The PM app CSS loads only with the lazily imported `CommandCenterLive` chunk (root `font-size: 14px`) so the legacy Tailwind view is unaffected.
- Dashboard units never mix: a history record is a receipt, a work outcome is one exact ID (latest dated receipt; an ID open again is Reopened), a delivery attempt is a session/run. V1 SHIPPED is "Accepted" with disposition unrecorded; V2 verified candidate and applied change are reached levels, not slices. Only records with a stated day (or a range inside one week) are placed in a week. Estimated USD, tokens and settled amounts are summed separately with known/total.

## Connected docs

- `ERA Notes/10 - Project Management/_Conventions.md` (grammar), `Plans/Delivery V2.md`, `Plans/Command Center.md`
- Atlas: `ERA Notes/04 - UI & Design/Page & Feature Atlas/pm.md` (hosted `/pm`)
