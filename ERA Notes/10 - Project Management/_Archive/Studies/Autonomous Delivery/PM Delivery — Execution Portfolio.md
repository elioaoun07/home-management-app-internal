---
created: 2026-09-06
updated: 2026-09-09
type: delivery-plan
status: baseline-frozen
owner: Elio
plan_revision: "2.1"
---

> Archived study. Current ownership and execution criteria live in the [PM index](<../../../_index.md>); unchecked boxes here are historical proposals.

# PM Delivery — Execution Portfolio

> **V2.1 plan, not implementation.** The owner accepted the ownership reassessment and authorized this documentation refactor. No slice below is shipped, no provider is qualified by this document, and no live job, migration, operational gate change or release is authorized here. S0–S3 replace the original Stage 0–5 blueprint; the pointer map is in [Migration Strategy](<PM Delivery — Migration Strategy.md>).

## 1. Execution contract for every future slice

Read [V2 Architecture](<PM Delivery — V2 Architecture.md>), [Evidence & Autonomy](<PM Delivery — Evidence & Autonomy.md>), [Context & Agent Model](<PM Delivery — Context & Agent Model.md>) and the relevant [Diagnosis](<PM Delivery — Current System Diagnosis.md>) witnesses before implementation. Architecture owns the schemas and V2-I01–V2-I10; Evidence owns F-ID, F-AUTH, F-ISOLATION, F-JOB, F-COST, F-EVIDENCE, F-RESULT, F-RESUME, F-COMMAND and F-PUBLISH. This portfolio names fixtures against those contracts rather than redefining enums or success semantics.

Proposed code lives under **scripts/delivery-v2/**; proposed tests under **tests/delivery-v2/**; proposed local runtime storage under **.delivery/v2/** uses SQLite plus immutable artifacts. These paths/modules are planning boundaries, not existing implementations. Do not add an ORM, event framework, generic broker or duplicate public type package without a demonstrated requirement. Keep new authoritative schemas in one module and reuse them at entry/projection boundaries.

Every slice states exact files, input/output, dependency, invariant, fixtures, observable exit and stop/rollback behavior. Implement only that slice; a newly discovered gap becomes an explicit dependency rather than an improvised subsystem. Split a slice if its proof cannot fit one reviewable change. New fixture names below are **proposed fixtures**, not tests reported as passing today.

All simulated provider/process fixtures use disposable files, synthetic secrets and fake endpoints. Do not run real providers, production relay or paid preflights as test setup. Real product samples require separate implementation/pilot authorization. Current no-Git-write/no-worktree/no-bypass/no-production-write rules remain binding. No worker writes the host checkout, store, trusted evidence, acceptance policy or publication credentials. Missing confinement means read-only assistance or a stop, never unsandboxed write access.

For every sample, preserve requested disposition even when only a candidate is available. Record owner setup, explanation, decisions, recovery, review, application and release effort as well as outcome, latency, cost basis and limitations. Unobserved owner minutes remain unknown. The comparison is against the same required outcome and evidence.

## 2. S0 — Contract and native baseline

**Stage exit:** one eligible product item has a frozen outcome/evidence/disposition contract, and a comparable ordinary-native workflow has an honest baseline record. No backlog migration or new dashboard.

### S0.1 — Select and freeze one item

- **Files:** proposed scripts/delivery-v2/contracts.mjs, work-ref.mjs and tests/delivery-v2/work-ref.test.ts. Read existing scripts/pm/shared/md-scan.mjs, scripts/pm/shared/tasks.mjs and scripts/delivery/packet.mjs; reuse parsing, not ordinal authority.
- **Input → output:** current selected Markdown block and fingerprint → validated WorkRef and immutable Contract revision. Human ID is an alias; source locator and fingerprint protect selection. Preserve required disposition and unresolved criteria.
- **Dependency/invariant:** canonical Architecture contracts; F-ID/F-AUTH/F-RESULT. Store only mappings for selected items. No V1 state or source mutation.
- **Fixtures:** selection-reordered.md prepends R-9 before selected R-1 and must still resolve R-1; selection-edited.md changes its text and must require a new revision; duplicate human IDs must produce conflict; a deployment request retains that disposition when candidate-only execution is proposed.
- **Done:** deterministic selection/contract output and resolving source references; re-reading identical content yields the same mapping/revision. No existing checklist is rewritten.
- **Stop/rollback:** ambiguous source stays unselected. Remove only new generated projection/mapping in a disposable test; preserve source and previously issued contracts.
- **Implemented 2026-09-07 — code and synthetic fixtures only.** `scripts/delivery-v2/contracts.mjs` holds the canonical enums, WorkRef, immutable Contract, Criterion, resource basis and disposition predicate; `scripts/delivery-v2/work-ref.mjs` resolves a selected row through `(file, alias, text fingerprint)` and never through an ordinal. 24 fixtures in `tests/delivery-v2/work-ref.test.ts` over `tests/delivery-v2/fixtures/selection-{baseline,reordered,edited,duplicate-alias}.md`: prepending R-9 does not retarget R-1 (with D01 kept as an explicit counterexample), an intent edit refuses as `stale-source` and obliges a linked revision 2, a duplicate alias returns both candidates instead of a guess, and a requested `verified_deployment` survives a candidate-only outcome and cannot be revised away without an owner decision reference. `scanLines`/`parseTaskMeta` are reused unchanged; ordinal authority is not. Nothing was launched, qualified or dispatched.

### S0.2 — Specify the proof and measure the native baseline

- **Files:** proposed scripts/delivery-v2/profiles/fast-local.json, a pilot record under .delivery/v2/artifacts/ and tests/delivery-v2/fixtures/fast-control/. Use canonical artifact references; do not create a new PM campaign.
- **Input → output:** selected product outcome, standing policy and actual environment → explicit check plan, resource-policy basis, requested disposition and native baseline record. A display-value change requires a click/action-value observation, not just a string match.
- **Dependency/invariant:** S0.1; F-EVIDENCE/F-COST/F-RESULT. The owner or a trusted previously established checker approves the observation plan independently of the writer.
- **Fixtures:** a button displaying 20 whose action still emits 25 fails the behavioral criterion; an unrelated boolean change fails scope if the approved change is exact. Use fake action handlers, no financial mutations.
- **Done:** criteria state what passes/fails, who can observe it and which source/environment identity matters. Separately authorized ordinary-native work records all owner effort and outstanding release obligations; do not invent a baseline from prior smoke sessions.
- **Stop/rollback:** if no safe relevant product task or observation exists, narrow the pilot explicitly without weakening the item's outcome. No baseline job runs under this planning authorization.
- **Partly implemented 2026-09-07 — proof machinery and synthetic fixtures; the baseline run itself is outstanding.** `scripts/delivery-v2/criteria.mjs` evaluates a criterion against one attributed observation into the six evidence states, with strict observer eligibility and named forbidden substitutes; `scripts/delivery-v2/baseline.mjs` holds the sample record; `scripts/delivery-v2/profiles/fast-local.json` states the resource/evidence/disposition policy and records that **nothing is qualified**. 48 fixtures in `tests/delivery-v2/criteria.test.ts` and `baseline.test.ts` over `tests/delivery-v2/fixtures/fast-control/`: a control that displays `20` while emitting `25` fails its behavioural criterion (observed by invoking the control, not by a literal); a surplus boolean edit in the approved file fails the exact-change criterion — with V1's `verifyInstantEdit` passing the same diff kept as the D04 counterexample; a source match cannot satisfy a behaviour criterion (`forbidden-substitute`, and `ineligible-observer` even with no list written); and exit-zero over zero selected tests stays `missing`. **Not done:** the ordinary-native baseline has not been run. It requires selecting and performing a real product item, which this implementation was not authorized to do; `.delivery/v2/artifacts/baselines/` is empty and carries that obligation. S0 therefore remains open.

## 3. S1 — One supervised delivery slice

**Stage exit:** one separately authorized product task passes its required local checks on a frozen candidate, survives a forced interruption/restart, and appears in the existing PM experience with its actual remaining application/release action. Implement the four slices in order; fake fixtures precede the real pilot.

### S1.1 — Qualify one native backend and scratch boundary

- **Files:** proposed scripts/delivery-v2/adapters/codex.mjs, candidate.mjs, tests/delivery-v2/backend-profile.test.ts and isolation.test.ts. Read scripts/delivery/drivers/codex.mjs, tests/delivery/drivers-codex.test.ts and the native interfaces in Context. Reuse stream/injection seams only after checking their contracts.
- **Input → output:** pinned adapter/runtime configuration and fixture workspace → qualification profile plus native execution reference/candidate export through the canonical adapter methods. Codex exec/SDK is the first candidate; no second backend or live app-server UI is required.
- **Dependency/invariant:** S0.1–S0.2; F-ISOLATION/F-JOB/F-COST. Describe actual writable/readable roots, permitted network/connectors, credentials and stopping scope. A copied checkout is not itself a security boundary; exclude .git, production secrets and authoritative control files. Qualify launch in that metadata-free snapshot using the selected interface's supported non-Git option (Codex CLI --skip-git-repo-check or its supported SDK equivalent); never create .git to satisfy a repository check.
- **Fixtures:** attempted outside-scratch write/read, fake-secret access, .git mutation, reparse/symlink escape, fake disallowed network call, inherited child process after stop and worker access to store/verifier. Exercise boundary enforcement with disposable canaries, not merely the generated settings object.
- **Done:** record supported/unsupported controls on the intended host with pinned versions; candidate export cannot publish or certify. Prove or refuse the strict whole-job monetary profile, including inner requests/retries/subagents/paid tools and in-flight margin. Explicit threshold admission is a different owner-authorized policy.
- **Stop/rollback:** no qualified scratch confinement means no autonomous edit pilot. No qualified hard bound means no strict monetary promise. Disable the profile and preserve diagnostics; never turn off the sandbox to pass.
- **Implemented 2026-09-07 — adapter, scratch boundary, candidate freeze and a qualification battery that ran. The stop/rollback clause above is now in force.** `scripts/delivery-v2/adapters/adapter.mjs` owns the canonical six operations, `executionRef = {backend_id, dispatch_key: job_id, native_ref?}`, the JobRequest, the authority-quarantine on adapter answers and the profile/admission predicates; `adapters/codex.mjs` implements them against Codex exec/SDK with an injectable SDK seam; `scratch.mjs` and `candidate.mjs` are the trusted loader and byte-derived candidate identity; `probes/canary.mjs` + `probes/codex-qualification.mjs` are the F-ISOLATION battery and harness. 66 new fixtures in `tests/delivery-v2/backend-profile.test.ts` and `isolation.test.ts` (repo suite 2178 → 2244, `tsc --noEmit` exit 0, `eslint` exit 0).
  - **Containment, observed on this host with disposable canaries, free** (`codex sandbox` runs a command under the Windows restricted-token sandbox with no model involvement; record `.delivery/v2/artifacts/qualification/q-6762c4c68503.json`): writes outside the workspace **denied (EPERM)**, an append into a `.git/HEAD` **denied (EPERM)**, outbound connect **denied by policy (EACCES)**, workspace writes allowed. **But reads are not confined** — the sandboxed process read a synthetic host-private secret, followed a real Windows junction planted inside the workspace and read through it, and read the supervisor store. Descendant survival after stop is **unobserved**. So `admitProfile()` refuses this profile on `filesystem.hostSecretRead`, `store.workerAccess` and one unverified control, and **no autonomous edit pilot may run on it as it stands** — Context §4's "profile ineligible for confidential/unattended work". The battery's negative control passed first: run unconfined, every containment canary escapes, so it can detect an escape.
  - **The strict monetary profile is refused on evidence, not deferred.** The installed `@openai/codex-sdk` 0.144.1 `Usage` carries four token counters and no monetary field, and `TurnOptions` carries only `outputSchema` and `signal`; fixtures re-read the installed `.d.ts` so the claim fails if the vendor adds one. There is no whole-job bound to enforce, so `strictBound` is false with the reason on the record, and token counters are never priced into a dollar figure.
  - **Two lifecycle refusals follow from the same interface.** `inspect` cannot reconcile by dispatch key (the SDK resumes by provider thread id only), so a lost launch acknowledgement stays `unknown` with its reservation held and is never retried; `stop` is requested-only (the sole primitive is `TurnOptions.signal`), so requested / locally-stopped / provider-confirmed stay three distinct facts and the third is always false.
  - Launch uses the SDK's `skipGitRepoCheck`; no `.git` is created in a scratch tree to satisfy a repository check. `danger-full-access` has no code path.
  - **Not established:** the SDK cannot resolve its own binary in this checkout (`@openai/codex` is absent), so a real dispatch needs `codexPathOverride` or that dependency. No model was run, nothing was paid, no product work was dispatched, and no store or job record exists yet.
- **Amended 2026-09-09 — a second executor, independently qualified, and the read boundary deferred as its own item.** The owner asked for Claude and Codex to both remain selectable, which S1.1's "do not build both adapters to start" had deliberately postponed. Context §2's governing clause is the one that permits it and constrains it: *"an unavailable provider does not authorize weaker containment or an unknown billing basis."* Nothing was weakened.
  - `adapters/claude.mjs` implements the same six operations over `@anthropic-ai/claude-agent-sdk` 0.3.207; `adapters/registry.mjs` is the closed, two-entry catalogue that turns a choice into an adapter. The supervisor gained no provider branch. Claude's extra capabilities land in existing contract fields: a **caller-minted `sessionId`** makes `lifecycle.inspectByDispatchKey: "derived"` (a lost launch acknowledgement is reconcilable against the local session transcript — the one lifecycle refusal Codex could not lift), and `total_cost_usd` makes `usage.costUsd` a real number, so a finished Claude job's reservation actually closes where a Codex job's never can.
  - **Its profile is unqualified too, for its own reason.** `probes/claude-qualification.mjs` is interface-only: this backend's confinement is a property of a running query, and a query is a paid dispatch, so the free battery that qualified Codex has no equivalent. The record carries `controls: null` — nothing observed, which is neither "everything failed" nor "nothing tried" — and Context §2's dated documentation finding that this SDK's Bash sandbox omits native Windows is recorded as a *documentation finding*, never promoted to a control observation. `admitProfile()` refuses on `profile-unqualified` plus three `confinement-unproven` controls.
  - **Cross-executor confusions are refused, not assumed away.** `admitProfile` now carries `backend_id`; `admitJob` refuses an admission describing a different backend than the dispatch; `dispatchJob` refuses a job reserved for another backend; `reconcileOutstanding` reconciles each job through its own adapter and leaves a job whose backend cannot be loaded outstanding rather than letting the other provider answer for it.
  - **The strict monetary profile is refused for Claude on evidence too**, and for a different reason worth stating precisely: `SDKResultMessage` reports a cost, so a spend can be READ; `Options` carries `maxTurns` and no monetary ceiling, so a spend cannot be BOUNDED. Reading a cost is not bounding it. Fixtures re-read the installed `.d.ts` so the claim fails the day a vendor adds one.
  - **The read boundary is deferred, with the two obvious fixes checked and rejected.** A per-path `deny` in the Codex permission profile would hide the canary while the read surface stays the whole host — gaming the fixture. Relocating the supervisor store does nothing: the canary's store was already outside the workspace and was read anyway. What remains is an OS boundary applied from outside either SDK (elevated Windows sandbox backend, a low-privilege account with denying ACLs, WSL2, or a container), which would cover both executors at once. Tracked as Delivery **DLV-94**. The battery is unchanged and stays truthful.

### S1.2 — Persist admission, jobs and unknown outcomes

- **Files:** proposed scripts/delivery-v2/store.mjs, jobs.mjs, tests/delivery-v2/jobs.test.ts and store.test.ts. Read scripts/delivery/fsx.mjs and scripts/delivery/drivers/fake.mjs; the latter supplies scripted failure/event seams, not OS containment proof.
- **Input → output:** current WorkRef/Contract, unrevoked Grant, profile, resource reservation and unique Job ID → durable executionRef with backend_id and dispatch_key equal to job_id, plus optional native_ref → native status/usage readings → reconciled receipt or explicit unknown. Each new native start/resume/repair/review dispatch gets a Job ID; read-only inspect/reattach is not new paid work.
- **Dependency/invariant:** S1.1; F-AUTH/F-JOB/F-COST. Choose and pin one locally compatible SQLite binding with transaction/backup support. Persist intent/reservation/executionRef before launch and dispatch_started_at immediately before calling the backend; absent native_ref does not prove no dispatch. Check current revisions, expiry, effects and allowance before every paid continuation. Native tools never transact against this store.
- **Fixtures:** lost-launch-ack accepts launch then drops its response; restart inspects/reconciles without issuing a second start. failed-then-success retains first-job usage; duplicate-usage does not double-count cumulative readings; unknown-cost keeps reservation; revoked-resume refuses new paid work; lost-stop-ack revokes publication but does not report stopped. Reuse failStartSession, throwsEvery and throwsUsage ideas from the V1 fake.
- **Done:** transactional crash/restore retains records and artifact references; duplicate admission is idempotent; failed/unknown work remains visible and resource-accounted. No native transcript event needs a corresponding authoritative database transaction.
- **Stop/rollback:** inspect by dispatch key only where the qualified backend supports it; otherwise a lost start acknowledgment remains unknown and reserved. Never blindly retry. Disable dispatch and preserve store/artifacts; deleting unknown jobs is not recovery.
- **Implemented 2026-09-07 — durable store, admission and reconciliation. 51 fixtures, all faults injected through fakes.** `scripts/delivery-v2/store.mjs` is the local transactional store on **`node:sqlite`** (Node 22.20's built-in `DatabaseSync` + `backup`; no ORM, no new dependency, nothing to rebuild after a Node upgrade), with `journal_mode=WAL` and `synchronous=FULL`. `jobs.mjs` owns admission, dispatch, reconciliation and stop.
  - **Three invariants live in the schema rather than in calling code.** `CHECK (dispatch_key = job_id)` — V2-I04 cannot drift in storage, and a fixture proves the constraint by inserting past the helper. `PRIMARY KEY (job_id, reading_key)` with `INSERT OR IGNORE` — a cumulative reading seen twice is a no-op, so F-COST's no-double-count is a property of the table rather than of whoever remembered to deduplicate. `commands.command_id` carries its actor and payload digest — a repeat returns its own receipt, a different payload or actor conflicts.
  - **Admission is one transaction** (§7 step 2): command dedup → run/source/profile/grant/allowance checks → job identity → reservation → persisted `executionRef` → receipt. A rollback fixture proves a crash mid-admission leaves nothing, and a nested-transaction fixture proves an inner unit of work cannot commit early. Command dedup runs *first*, so a retried command reports what actually happened rather than being re-refused by a grant revoked in the meantime.
  - **`dispatch_started_at` is committed by the adapter's `onDispatchStart`, immediately before the external call, and the dispatch itself happens outside the transaction.** It is idempotent and keeps the earliest timestamp. The marker is the only thing `retryEligibility` reads: absent → provably undispatched, may be sent; present → potentially dispatched, held. There is no other code path that starts a second dispatch against one intent.
  - **F-JOB/F-AUTH/F-COST fixtures proven:** lost-launch-ack leaves `unknown` + marker set + reservation open and refuses a second dispatch; a restart reconciles by read-only `inspect` and issues no `start` (asserted on the call list) and creates no new job row; a failure *before* the marker stays retry-eligible and later succeeds; failed-then-success retains both attempts' usage; the same adapter result recorded twice inserts nothing; unknown cost keeps the reservation open even on a finished, successful job; a malformed final message erases neither usage nor outcome; revoked, expired, contract-moved, effect-not-permitted, executor-not-permitted, allowance-exhausted, stale-source, unadmitted-profile and closed-run all refuse; a second FAST repair refuses at the limit of one; resume gets its own job id and inherits the native ref, and refuses when no session was ever observed; a lost stop acknowledgement revokes publication immediately, shows **Stop requested**, keeps the reservation and blocks late output — and revokes even when the adapter's stop call throws.
  - **Storage faults:** transactional restart keeps records and markers; `backupTo` uses SQLite's online backup rather than a file copy; `verifyRestore` reports missing and corrupted blobs separately and refuses to call a restore dispatchable while jobs are unreconciled; a published artifact is temp-written, flushed, renamed and hash-verified on read-back before its row is committed, so a crash leaves an orphan blob rather than a dangling reference.
  - **Not established:** nothing was dispatched to a real provider; every fault here is injected. Candidate verification, checks and results are S1.3, and there is still no entry point.

### S1.3 — Freeze, check and report the actual candidate

- **Files:** proposed scripts/delivery-v2/checks.mjs, results.mjs, tests/delivery-v2/evidence.test.ts and results.test.ts. Read tests/delivery/acceptance.test.ts, instant.test.ts, finish-package.test.ts and validation-baseline.test.ts; preserve counterexamples while rejecting generic filename-as-proof semantics.
- **Input → output:** stopped/reconciled writer export → frozen code or research candidate identity → protected independently selected check execution → evidence and Result with actual disposition, pending obligations and raw references. Bind and emit the minimal canonical Checkpoint from Context §5 at candidate handoff/task-level wait/planned pause, using its shared shape in contracts.mjs; fresh-session continuation is qualified in S2.2. The candidate cannot alter trusted check selection or mint its receipt.
- **Dependency/invariant:** S1.2; F-EVIDENCE/F-RESULT/F-PUBLISH. Use a clean controlled check environment without publication credentials. Pin trusted checker inputs outside the writer; changes to tests/config/lockfiles or acceptance rules trigger independent scrutiny and evidence invalidation.
- **Fixtures:** exit-zero with zero tests fails required behavior; arbitrary existing proof file fails; a required malformed review stays inconclusive, while optional malformed engineer prose does not block independently sufficient evidence; surplus edit and stale bound action fail the S0 fixture; candidate mutation stales proof; missing artifact blocks completion; build:null must not resurrect completed work; a verified candidate does not complete a required deployment.
- **Done:** one result evaluator drives view/export/continuation; evidence records actual commands, counts and identities. Required local observations pass for the candidate; missing phone/live-DB/release evidence remains an obligation. Initial FAST policy allows at most one supervisor-dispatched post-check repair, only within the current Grant/resource policy. Native inner repairs remain inside the whole-job bound; they are not separately scheduled ERA Jobs. Further escalation needs available standing authority or a concrete decision.
- **Stop/rollback:** any false completion disables the affected evidence profile. Preserve useful output and failed observations; no waiver becomes verified success.
- **Implemented 2026-09-07 — trusted import, protected checking, one Result, and the canonical Checkpoint. 48 fixtures.** `scripts/delivery-v2/checks.mjs` imports the writer's output into an immutable candidate generation and runs pinned checks against it; `results.mjs` owns the Result and the two completion predicates; `contracts.mjs` gains `makeCheckpoint` — Context §5's field list and nothing else.
  - **The four things the candidate cannot do, each stopped at a named place.** *Mint a receipt:* every receipt is bound to the plan's digest, computed from checker inputs fingerprinted before the candidate existed — a forgery carrying the right `produced_by` string still fails on `plan_digest`. *Redefine check selection:* the plan is pinned outside the writer and immutable; a criterion it does not name, or one whose revision has moved, is refused. *Weaken its oracle:* `detectOracleDrift` compares the candidate's manifest against the pinned checker inputs and reports a changed or removed test/config/lockfile; a check that depends on a drifted input refuses to run, one that does not still carries the drift for independent scrutiny. *Publish:* `assertRestrictedEnvironment` refuses to run any check in an environment holding publication credentials, and `restrictedEnv` is an allowlist so a variable nobody anticipated is still excluded.
  - **Trusted import copies rather than validating in place.** Architecture §8's "do not validate a tree the engineer can keep changing" is implemented literally: the writer's output is copied through the trusted loader into `<generations>/C1`, `.git`/secrets/links are refused on the way in, identity is derived from the copied bytes, and an edit to the frozen generation itself makes the next check refuse with `candidate-changed-during-checking`.
  - **F-EVIDENCE / F-RESULT / F-PUBLISH fixtures proven:** exit-zero over zero selected tests is `missing`, never satisfied; an arbitrary proof file's existence is a `source_match` and cannot carry a behavioural claim; a malformed *required* review is `inconclusive` — an obligation, not a verdict; malformed *optional* engineer prose changes neither predicate and does not alter the result's body digest; a surplus edit fails `publicationScope`; a mutated candidate stales its own proof (C1's evidence does not certify C2); a missing referenced artifact blocks completion; a check that selected nothing cannot revive an earlier completion; and a fully verified candidate against a `verified_deployment` contract yields `candidateVerified: true, workComplete: false` with the deployment named as outstanding.
  - **Two predicates, and the gap between them is the deliverable.** `candidateVerified` is false with no candidate, over an empty criterion set, on a waiver, on a stale candidate, outside publication scope, on a missing artifact, and on any unresolved integrity/authority violation. `workComplete` additionally requires `observedDisposition === requestedDisposition` — there is no ordering between dispositions anywhere in the module. A verified candidate with outstanding cost reconciliation stays `candidateVerified: true, workComplete: false`. `recordDisposition` refuses without a named observer, scenario and build; `appendResultVersion` adds a version without touching the candidate or closed outcome, refuses an unattributed receipt and refuses to un-observe; a failed PM projection changes only `projection_status`.
  - **FAST repair policy:** `postCheckRepairPolicy` permits exactly one supervisor-dispatched post-check repair, and only when a check actually *failed* (a `missing` or `inconclusive` criterion is an evidence gap, not a defect) and the current Grant verdict permits it. Native inner test/fix iterations are not counted.
  - **Not established:** no fresh-session DEEP continuation (S2.2), no entry point (S1.4), nothing dispatched or paid. Every check here is executed through an injected runner; the real `spawnExecutor` exists but no product check has been run against a real candidate.

### S1.4 — Connect existing selection to one real product pilot

- **Files:** proposed scripts/delivery-v2/cli.mjs/entry adapter; narrow integration in scripts/delivery/server-routes.mjs and scripts/pm/src/features/delivery/ for dispatch mode, selection and result. Add tests/delivery-v2/pilot-entry.test.ts. Reuse current board/search/styles and result surface; no new app or dashboard.
- **Input → output:** selected item + applicable policy → admitted Run/Job → visible result and concrete outstanding owner action. One local service persists an installation-wide v1|v2 dispatch switch. Store only selected WorkRef mappings, not an adoption registry.
- **Dependency/invariant:** S1.3; F-ID/F-AUTH/F-JOB/F-RESULT/F-COMMAND. Before V2 dispatch, drain/reconcile V1 writers. In V2 mode, stale V1 launch/resume/fork and other write-capable routes refuse server-side; history/read routes remain available. Future implementation authorization identifies the pilot's gate policy explicitly.
- **Fixtures:** duplicate start returns the same admitted outcome; same command ID with another actor/payload conflicts; unauthenticated/cross-origin local requests refuse; stale selected source refuses launch; active V1 writer prevents mode switch; cached V1 client cannot launch/resume/fork after switch; reload shows current unknown/decision/result. Exercise the existing desktop surface at 390px if it changes.
- **Done:** after fake fixtures and boundary qualification, one separately authorized non-tooling product job produces required local evidence and a coherent result after a forced restart. Measure review/application/release effort even if the owner performs those steps. Product work closes only when its requested disposition and evidence are fulfilled.
- **Stop/rollback:** disable new V2 admission; revoke/reconcile active work and preserve candidates/results. Switch back only after V2 writers are confirmed drained/reconciled; do not turn an unknown dispatch into a fresh V1 launch. Direct development remains available, without pretending to control the owner's editor.
- **Implemented 2026-09-07 — entry point, dispatch switch and projection wired into the existing surface. 36 fixtures plus a live smoke against the running server. The product pilot itself is NOT done and is blocked.** `scripts/delivery-v2/entry.mjs` is the entry adapter (switch, `guardV1Route`, `authenticateCommand`, `deliverSelection`, `projectRun`, four HTTP routes); `service.mjs` is the one local service holding the store handle; the integrations are a guard at the top of V1's `routeDelivery`, a lazily-built context plus a `/api/delivery/v2/` branch in `pm-server.mjs`, and a mode signal + chip in the existing `DeliveryHome`.
  - **The switch is enforced server-side, inside V1's own router.** A dashboard tab opened before a switch still holds a launch button and a valid session id; the client is not what stops it. Verified live against the running server: in v2 mode `start`, `resume`, `decision`, `message` and `control` all return 409 `v1-write-route-disabled-in-v2-mode`, while `sessions` and `capabilities` still return 200 — Architecture §10's "historical V1 inspection remains available". The fixture drives V1's real `routeDelivery` rather than a copy of its path list.
  - **Switching refuses in both directions, and asymmetrically.** To v2 while a V1 writer is active (that writer would keep editing the host workspace); to v1 while a V2 job is unreconciled (S1.4's "do not turn an unknown dispatch into a fresh V1 launch"). The live smoke refused the real switch on this machine because the repo genuinely holds a non-terminal V1 session, `s-20260822-093143-t7tm` — the drain rule firing on real data, not on a fixture. A V1 session whose `state.json` cannot be parsed counts as active: an unreadable writer is not a drained one.
  - **A missing or corrupt switch file reads as v1.** That default is a safety property: an installation that never chose, or whose file was truncated, keeps running the engine that is actually qualified.
  - **Loopback is not the authentication story.** Origin is parsed, not prefix-matched (`http://127.0.0.1.evil.example` is not loopback), `Sec-Fetch-Site` is checked independently for the case where Origin is absent, and an actor is required separately so every command receipt binds one. Verified live: an unattributed switch returns `unauthenticated`, a cross-origin switch returns `cross-origin-request-refused`, and neither moved the switch.
  - **Selection → admission → projection.** `deliverSelection` freezes the selected row, re-reads it through its locator, persists **only that row's** WorkRef mapping plus the frozen contract revision, then admits. An ambiguous alias refuses before a command id is spent or a reservation is taken. `projectRun` rebuilds the visible state from persisted records only — a fixture kills the store mid-run, reopens it in a new handle and gets back the same unknown job, the same pending decision, a result that is honestly `candidateVerified: false`, and the owner action `Reconcile 1 outstanding job`.
  - **`deliver` is deliberately unconfigured.** The gate policy — criteria, scratch and publication scope, grant, reservation — is what S1.4 says a future authorization must state explicitly, so `POST /api/delivery/v2/deliver` returns `no-deliver-policy-configured` rather than running under defaults nobody approved. Confirmed live. The store opens on first use, so an installation that never switches never grows one.
  - **NOT done — the slice's own "Done" clause.** It requires one separately authorized non-tooling product job to produce required local evidence and a coherent result after a forced restart, with owner review/application/release effort measured. No product job was dispatched, nothing was paid, and no owner effort was measured. It is blocked on S1.1: the executor profile is inadmissible on its read boundary, and `admitJob` refuses an unadmitted profile — which a fixture asserts. The wiring is proven; the pilot is not.
- **Amended 2026-09-09 — the gate policy is now something the owner can state, and executor selection is part of it.**
  - `scripts/delivery-v2/policy.mjs` defines schema `delivery-v2/execution-policy@1` at `.delivery/v2/execution-policy.json`: permitted executors, permitted effects, resource unit/allowance/strictness, per-job reservation basis, grant expiry, a requested-disposition ceiling and the repair-dispatch limit. Almost none of it is new machinery — `Grant.permitted_executors`, `permitted_effects`, `resource_policy`, `expires_at` and `repairDispatchLimit` already existed and only lacked a place to be written down. The one idea added is the disposition ceiling, which refuses a request above it *before* a contract is frozen rather than downgrading one after (Architecture §5 forbids the quiet rewrite).
  - **Four structural refusals a policy file cannot express:** granting `publish`, `requireConfinement: false`, `requireQualifiedProfile: false`, and automatic retry of a possibly-dispatched job. Each is a validation error naming its reason, not a trimmed field — so nobody can believe they configured it.
  - **`deliver` admits; it does not dispatch.** It produces an authorized Job and its `JobRequest` and stops there, so installing a policy still cannot cause paid work. Which means the honest end of this path today is `profile-not-admitted`: with a valid policy and a selected executor, a live route smoke walks no-executor → unknown-executor refused → selection persisted → no-policy → weakening policies refused → wrong-executor-for-policy refused → **the containment gate**, reached through a fully wired path instead of a missing one.
  - **Executor selection is installation-wide, persisted, and never inferred.** `.delivery/v2/executor.json`; absent, unreadable or unrecognised all mean *no executor* and dispatch refuses. Unlike the v1|v2 dispatch switch — which safely defaults to the engine that already runs — there is no safe default *provider*, so there is none.
  - **The rest of execution policy is deferred as DLV-95:** the authorized route from an admitted job to a dispatched one, scratch provisioning wired into it, a check plan bound to the policy's criteria, and whether a dispatch needs per-job owner confirmation on top of the policy. That last one is an owner decision, not an implementation choice.

## 4. S2 — Recovery and comparative proof

**Stage exit:** three comparable FAST samples in total, plus one interrupted DEEP engagement, support a decision about owner-effort savings and the next actual gap. This is a small operating sample, not a statistical safety claim. One executor and one contract model serve both modes.

### S2.1 — Complete the failure matrix

- **Files:** extend proposed jobs.test.ts, store.test.ts, evidence.test.ts and results.test.ts under tests/delivery-v2; alter production modules only for a reproduced failing contract.
- **Input → output:** fault at each persisted launch/receipt/export boundary → reconciled record or honest unknown with no unintended new authority.
- **Dependency/invariant:** S1; F-AUTH/F-JOB/F-COST/F-EVIDENCE/F-RESULT. Use fake clock/process/event injection and temporary SQLite/artifacts.
- **Fixtures:** crash before dispatch versus after provider acceptance; partial/missing artifact; expired grant during native wait; revoked job emits a late result; repeated start response; changed contract before repair/review; backup restore with unresolved cost. Verify current authority on resume, not only initial launch.
- **Done:** each row names observed state, next permitted action and preserved reservation/artifact; cancellation never promises termination before acknowledgment. Re-run only affected contract suites.
- **Stop/rollback:** unknown external effects keep relevant scope paused; late writers cannot publish. Keep the last qualified profile and preserve incident records.

### S2.2 — Resume DEEP work without a custom context engine

- **Files:** proposed scripts/delivery-v2/continuation.mjs and tests/delivery-v2/continuation.test.ts; adapter changes only for native resume/export. Read scripts/delivery/memory.mjs and context-assembly.mjs for D08/D09 witnesses, not mandatory dossier shapes.
- **Input → output:** current contract/decisions, executionRef, candidate and meaningful source-bound findings → native resume or compact portable checkpoint → authorized successor Job. Include rejected approaches only when they prevent rediscovery, and link raw evidence.
- **Dependency/invariant:** S2.1; F-RESUME/F-AUTH/F-COST/F-EVIDENCE. Native conversation continuity is default. Snapshot what ERA actually passes; do not infer provider occupancy from processed tokens or require every inner read to be indexed.
- **Fixtures:** forced fresh session receives a recorded owner answer, rejected hypothesis, open evidence and current candidate; changed source invalidates its dependent finding; no undocumented transcript dependency is required to find the next safe action. Two outstanding jobs never share writable scratch.
- **Done:** one authorized real DEEP investigation/implementation survives interruption and fresh-session continuation without re-asking a settled decision; repeated work and missing understanding are measured, not declared impossible. Independent review is added only for a concrete semantic uncertainty.
- **Stop/rollback:** improve the checkpoint if it fails; do not automatically add vector storage, paid summarizers or more agents. Inconclusive review remains an obligation.

### S2.3 — Decide whether the supervisor earns expansion

- **Files:** append immutable pilot comparison artifacts and a concise campaign record; no runtime feature is a default deliverable of this slice.
- **Input → output:** native baseline, three FAST samples and interrupted DEEP sample, including failures → explicit continue/narrow/delegate-more decision with measured gaps.
- **Dependency/invariant:** S2.1–S2.2; F-RESULT. Compare the same scope, evidence and requested disposition; separate observed cost/list cost/estimates/subscription usage and unknowns.
- **Fixtures/checks:** comparison rejects a missing failed attempt, omitted owner application time, tooling smoke passed off as product work, or patch-only completion compared with deployed completion. Manual observations may remain unknown without false precision.
- **Done:** record owner-active minutes where observed, avoidable decisions, repeated explanation, recovery effort, verified outcomes, escaped defects, latency and maintenance/setup cost. Choose only the next consequential burden.
- **Stop/rollback:** if value is weak, stop expansion and keep the PM board, contract/check templates and result links. If an external workflow is better, migrate the job adapter/links rather than defend custom ownership.

## 5. S3 — Conditional access, publication and retirement

These slices are independent choices after S2. Implement only the one justified by evidence and separately authorized scope. S3 is not a mandatory platform completion checklist.

### S3.1 — Remove an observed access or availability burden

- **Files:** choose either existing scripts/pm/bridge.mjs, src/features/pm-live/ and src/components/pm-live/ for a narrow mobile projection/decision change, **or** one proposed managed-backend adapter. Do not combine a mobile rewrite with a hosting platform. Add F-COMMAND or backend qualification fixtures as appropriate.
- **Input → output:** authenticated revision-bound decision/command → durable same-ID receipt; alternatively, qualified managed job → the same Job/candidate/result contract. SDK/exec with persisted task-level escalation is default; a live per-tool approval client needs a demonstrated requirement.
- **Dependency/invariant:** S2.3; F-COMMAND/F-AUTH/F-JOB/F-COST. Phone cache must be owner-bound and stale state explicit. Managed hosting needs its own runtime/cost/credential qualification; documented beta capability alone is insufficient.
- **Fixtures:** lost command acknowledgment, same ID/different payload, stale decision, actor switch, duplicate tap, relay restart and full-snapshot replacement; for hosting, lost launch/stop, environment/candidate export and resource-bound conformance. Require owner-provided physical-phone/live-relay evidence where current policy prevents direct inspection.
- **Done:** the chosen owner burden measurably decreases without weaker authority or truthful status. A sleeping local runner remains visibly queued unless qualified hosting actually removes that dependence.
- **Stop/rollback:** keep mobile read-only when provenance is unproven; disable only the unqualified host/profile. Production relay changes require owner-applied manual SQL under existing rules, never a back door.

### S3.2 — Resolve publication policy without building a host integrator

- **Files:** result/pending-obligation projection first; conventional CI/publication configuration only after a separate written policy amendment permits it. No mandatory integration journal or worktree module.
- **Input → output:** verified frozen candidate + explicit permitted publication action → application/release receipt and required environment observation, or precise owner handoff. One owner action may authorize related steps while evidence stays distinct.
- **Dependency/invariant:** S2.3; F-PUBLISH/F-AUTH/F-EVIDENCE/F-RESULT. No Git writes/worktrees under current policy. Production DB writes remain owner-only. Changes to trust/acceptance rules need independent authorization under the previously trusted version.
- **Fixtures:** candidate changed after approval; host/source drift; candidate applied but deployment absent; deployment identifier differs from checked version; owner applies SQL without supplied verification; duplicate receipt; failed PM writeback. Never infer production success from a local build.
- **Done:** either manual handoff is accepted as the operating boundary with measured effort, or an explicitly authorized established publication path proves the requested disposition. Required deployment stays pending until observed.
- **Stop/rollback:** retain pending obligations and last valid evidence; reconcile uncertain publication before retry. Do not overwrite owner edits, weaken disposition, or create a bespoke integrator to avoid a policy decision.

### S3.3 — Retire only demonstrated redundancy

- **Files:** selected V1 entry/runner/UI paths and compatibility tests, named from actual references at cutover. Preserve .delivery/sessions/ originals and useful readers/exports.
- **Input → output:** supported-path inventory and active-session list → bounded retirement diff plus restored-fallback drill.
- **Dependency/invariant:** applicable S3 choice or S2 evidence alone; F-ID/F-JOB/F-RESULT. No active work depends on removed code; known cached desktop/phone server routes cannot bypass retirement.
- **Fixtures:** active V1 session blocks deletion; old client tries retired launch; historical result still opens; export includes raw pointers; restore recovers artifacts and unknown reservations. Test affected paths rather than claim all providers/devices are qualified.
- **Done:** obsolete phase/summary/status machinery disappears for the supported path and owner effort is remeasured. No bulk session conversion or raw-history deletion.
- **Stop/rollback:** disable new successor launches, reconcile/drain active jobs, restore the supported dispatch mode/profile and preserve artifacts. Never replay an unknown launch to make rollback look complete.

## 6. Scope and completion discipline

S0 and the S1 vertical path are the only initial investment. S2 decides what comes next; S3 choices are conditional. A product pilot must happen before provider parity, full PM migration, a new dashboard, generic context/effect infrastructure or a universal observer library. Existing V1 hazards that block continued use may receive narrow fixes independently.

For future implementation, finish each slice with: exact changed files; applicable invariant/fixture families; actual command/observer receipts; missing live/device/provider evidence; useful product artifact; owner effort and cost basis; stop/rollback state. A green unit suite is evidence for its tested boundary, not delivery success.

This refactor changes planning documents only. The prior study's test counts and pure probes remain historical receipts in Diagnosis. None of the proposed fixtures, backend qualification, pilots, migrations or release paths above ran as part of the 2026-09-06 plan update.

## 7. Implementation log

Kept separate from the plan above so that what is built stays distinguishable from what is designed. Each entry states its authorization and, more importantly, what it did **not** establish.

| Date | Authorized scope | Landed | Explicitly not established |
|---|---|---|---|
| 2026-09-09 | Second executor + execution-policy wiring (owner-requested, outside the S1 sequence) | `adapters/claude.mjs`, `adapters/registry.mjs`, `probes/claude-qualification.mjs`, `profiles/claude-local.json`, `policy.mjs`; executor selection + `/api/delivery/v2/executor` in `entry.mjs`, lazy policy-built `deliver` and per-backend adapter resolution in `service.mjs`, cross-backend refusals in `jobs.mjs`, `backend_id` on `admitProfile`; executor chip in `scripts/pm/src/features/delivery/`; `tests/delivery-v2/executor-selection.test.ts`. `tsc --noEmit` exit 0, `eslint` exit 0 on `scripts/delivery-v2`, PM bundle builds, `pnpm pm:lint` clean. | **Neither executor became admissible and no containment control was newly observed.** Codex still fails three controls; Claude's seven are unobserved, because its confinement can only be observed by a paid dispatch. Nothing was dispatched, nothing was paid, no product job ran. `deliver` admits and does not dispatch, so no authorized path from an admitted job to a running one exists yet (DLV-95). The read boundary is untouched and deferred (DLV-94). The new fixtures were written in this session and **not run** — validation is the owner's separate phase. |
| 2026-09-07 | S0.1 and S0.2 only | `scripts/delivery-v2/{contracts,work-ref,criteria,baseline}.mjs`, `profiles/fast-local.json`, `tests/delivery-v2/` (72 fixtures, all passing; repo suite 2106 → 2178, `tsc --noEmit` exit 0, `eslint` exit 0) | No executor adapter, store, job, candidate, check runner or entry point exists. No backend was installed, run, qualified or paid. No isolation, cost bound or dispatch recovery was tested. No ordinary-native baseline was performed and no product item was selected. V1 is untouched and remains the operating system of record. |
| 2026-09-07 | S1.4 only (wiring; no pilot) | `entry.mjs`, `service.mjs`, `store.mjs` +`work_refs`/`contracts` (schema 2); guard in `scripts/delivery/server-routes.mjs`, v2 branch in `scripts/pm-server.mjs`, mode signal + chip in `scripts/pm/src/features/delivery/`; `tests/delivery-v2/pilot-entry.test.ts` (36 fixtures; delivery-v2 suite 242 → 278, repo 2348 → 2384, `tsc` 0, `eslint` 0, PM bundle builds). Live smoke against a running pm-server confirmed the V1 guard, the auth refusals, the drain rule and the unconfigured-policy refusal; repo left in v1 with no switch file and no store. | **The product pilot did not happen.** No product job was dispatched, nothing was paid, no owner review/application/release effort was measured, and no forced-restart product run exists. Blocked on S1.1's read boundary: the profile is inadmissible and `admitJob` refuses it. The `deliver` gate policy is intentionally unconfigured, so the deliver route refuses on every installation until one is separately authorized. |
| 2026-09-07 | S1.3 only | `checks.mjs` (trusted import, pinned plan, oracle-drift detection, receipts), `results.mjs` (Result, `candidateVerified`, `workComplete`, disposition receipts, FAST repair policy), `contracts.mjs` +`makeCheckpoint`; `tests/delivery-v2/{evidence,results}.test.ts` (48 fixtures; delivery-v2 suite 190 → 242, repo 2106 → 2348, `tsc` 0, `eslint` 0) | No fresh-session DEEP continuation (S2.2) and no entry point (S1.4). No product check has been executed against a real candidate — every check runs through an injected runner. Nothing dispatched, nothing paid. The executor profile is still inadmissible on its read boundary, so none of this has been exercised end to end on real work. |
| 2026-09-07 | S1.2 only | `store.mjs` (node:sqlite, WAL + synchronous=FULL, no ORM), `jobs.mjs` (admission, dispatch, reconciliation, stop, resource summary); `tests/delivery-v2/{store,jobs}.test.ts` (51 fixtures; delivery-v2 suite 138 → 190, `tsc` 0, `eslint` 0) | No dispatch to a real provider; every fault is injected through a fake. No candidate verification, check execution, Result or entry point. The store's identity and accounting invariants are proven against fakes, not against a provider's real billing. |
| 2026-09-07 | S1.1 only | `adapters/{adapter,codex}.mjs`, `scratch.mjs`, `candidate.mjs`, `probes/{canary,codex-qualification}.mjs`, `contracts.mjs` +Grant, `profiles/fast-local.json` rev 2; `tests/delivery-v2/{backend-profile,isolation}.test.ts` (66 fixtures; repo suite 2178 → 2244, `tsc` 0, `eslint` 0). One qualification record written from a battery that actually ran under the backend's sandbox. | **The profile did not qualify.** Reads are not confined: host-private secrets, a junction out of the workspace and the supervisor store were all readable from inside the sandbox; descendant survival after stop is unobserved. No strict monetary bound exists at this SDK version. No store, job, check runner or entry point exists. The SDK cannot resolve its binary in this checkout. Nothing was dispatched, nothing was paid, no product item was selected, and no ordinary-native baseline was performed. V1 is untouched. |

Green fixtures are evidence for the boundary they test — record identity, refusal behaviour and evidence eligibility over synthetic inputs. They are not provider qualification, not a pilot, and not delivery success.
