---
created: 2026-07-24
updated: 2026-07-25
type: checklist
status: active
owner: Elio
tags: [pm/checklist, tooling/delivery]
---

# Delivery 10x — Checklist

> Grammar per [_Conventions](<../_Conventions.md>) (prefix `DLV`). Design detail per item: [3 · Action Plan](<3 - Action Plan.md>). Evidence: [5 · Session Postmortem](<5 - Session Postmortem (s-20260722-225601-whdv).md>).

## Now

**M1 — Governed Start** — the owner sets the envelope before a token is spent; the floor never crashes silently.

- [x] **DLV-1** Budget governance: owner-set cost/token envelope in the packet, between-turn enforcement, warn threshold, graceful cap-hit pause with finish package → `scripts/delivery/budgets.mjs` _(blocker - M)_
- [x] **DLV-2** Preflight Flight-Check screen: one pre-launch panel with ACs, lane/model recommendation, required budget fields, baseline/dirty acknowledgments, context preview; snapshot persisted into the packet → `scripts/pm/src/features/delivery/DeliveryHome.jsx` _(blocker - M)_
- [x] **DLV-3** Config hardening: schema-validated `.delivery/config.json` with last-known-good fallback + dashboard banner, atomic writes, runner crash-loop backoff → `scripts/delivery/config.mjs` _(blocker - S)_
- [x] **DLV-4** Error taxonomy + retry escalation: close the "monthly spend limit" pattern gap, quota/auth never retried (paused + resumable), max auto-retries per gate then NEEDS_DECISION with notification → `scripts/delivery/quota.mjs` _(blocker - S)_
- [x] **DLV-5** Baseline & change-ownership gate: typed acknowledgments for dirty tree and red baseline, delta-vs-baseline validation semantics, pre-existing edits recorded as not-session-owned → `scripts/delivery/validation-baseline.mjs` _(friction - M)_

**Mobile Command Surface** — `/pm/live`, a phone-installable checklist + delivery command surface. Outbound-only Supabase relay from `pnpm pm --bridge`; amends (does not reopen) the "Remote decision controls" rejection in [6 · Design Debates](<6 - Design Debates & Rejected Ideas.md>) — gate approval (spec/plan/uat/blocked) stays laptop-only.

- [x] **DLV-20** Mobile command channel: bridge drainer + allowlist mapping every phone command onto the existing `routeDelivery()` gates (capture/undo/preflight/launch/pause/abort-turn/resume/cancel/answer/ask); `set-budget`/`set-config`/`rotate`/`fork` and any spec/plan/uat/blocked gate decision are never exposed → `scripts/pm/bridge.mjs`, `migrations/2026-07-25_pm-mobile-relay.sql` — `tick` removed the same day, see **DLV-23** _(blocker - M)_
- [x] **DLV-21** Mobile launch flow: flight-check + mandatory budget envelope, no default; refuses on a dirty tree or red baseline because mobile never forwards the typed `DIRTY TREE`/`RED BASELINE` ack → `scripts/pm/bridge.mjs`, `src/components/pm-live/LaunchSheet.tsx` _(blocker - S)_
- [x] **DLV-23** Mobile checklist is read-only and a row launches a delivery session instead of ticking a checkbox: `tick` refused server-side (`REFUSED_TYPES`) + dropped from the `pm_commands.type` CHECK, every bridge write journaled with a restorable pre-image under `.delivery/pm-undo/`, `undo` command + Undo strip on the phone (refuses instead of clobbering a later laptop edit). Done 2026-07-25 (same-day revocation of the tap-to-tick shipped in DLV-20). → `src/components/pm-live/board/TaskRow.tsx`, `migrations/2026-07-25_pm-commands-drop-tick.sql` _(blocker - S)_
- [x] **DLV-22** Push notification consumer for the `notification.requested` event (emitted since DLV-1, unconsumed until now) — the web-push half of **DLV-16**: gate transitions, budget events, terminal states, and runner death (60s debounce) all deep-link into `/pm/live`. DLV-16's dashboard-toast half is still open. → `scripts/pm/bridge.mjs`, `src/app/api/pm/notify/route.ts` _(friction - S)_
- [x] **DLV-24** `/pm/live` promoted from a 3-tab relay to a responsive PM application: five views (Overview / Board / Delivery / Usage / Campaigns), bottom nav under `lg` and a side rail + widget grid above it. Two new `pm_live` row kinds (`rollups`, `history`) derived laptop-side from the existing `severityItems`/`sumSeverity`/`lintChecklist` helpers — no migration, they ride the existing `(id, kind, payload jsonb)` shape. Board gains the desktop board's filter grammar plus `lane:`/`e:`, group-by, sort-by, virtualized rows and a detail sheet; a Zustand store confines the 10s heartbeat to the status chip instead of re-rendering ~480 rows. → `src/features/pm-live/derive.ts`, `src/components/pm-live/PmLiveApp.tsx` _(friction - L)_
- [x] **DLV-25** The three orphaned bridge commands get an interface: `capture` (Inbox quick-add — which also makes the DLV-23 Undo strip reachable for the first time), `ask` (free-form guidance to a running agent) and `abort-turn` ("Stop turn", beside Pause). All three were already implemented, allowlisted and tested server-side with no way to issue one. Gate approvals stay laptop-only, unchanged. → `src/components/pm-live/CaptureSheet.tsx`, `src/components/pm-live/views/DeliveryView.tsx` _(friction - S)_
- [x] **DLV-26** Two drift fixes found while refactoring: `LANE_HINTS` was a hand-copied constant that silently diverged from `DEFAULT_CONFIG.budgets.laneDefaults` whenever `.delivery/config.json` was customized — the bridge now publishes the real values on the `fleet` row; and finished `session:<id>` rows were never deleted, so the bridge now prunes terminal sessions older than 7 days. → `scripts/pm/bridge.mjs`, `src/components/pm-live/LaunchSheet.tsx` _(annoyance - S)_
- [x] **DLV-27** PM Live renders a missing provider USD cost as unavailable, rather than crashing or showing a false zero; gate notifications use the same safe fallback. Done 2026-07-25. → `src/features/pm-live/chartTheme.ts`, `src/components/pm-live/views/DeliveryView.tsx`, `scripts/pm/bridge.mjs` _(blocker - S)_

**D3 smoke test** — a real end-to-end launch to verify the pipeline itself, not a feature. Run 2026-07-25: two real sessions stranded by DLV-29 (cancelled), then a third reached ACCEPTED after the fix — the first delivery session ever to ship end-to-end.

- [x] **DLV-28** Add a "Smoke Tests" log entry to the campaign index recording a real end-to-end FAST lane / Haiku / low-effort launch (date, session id, outcome) → [_index](<_index.md>) _(annoyance - S)_
- [x] **DLV-29** Fix the budget-exhausted/manual-gate collision that permanently strands a session: `enforceBudgetBoundary` must not clobber an existing phase-gate `awaiting` (or the `set-budget` resume must re-derive `awaiting = {gate: GATES[state.state]}` for SPEC_READY/PLAN_READY/UAT_READY), and add a fake-driver regression test for the collision → `scripts/delivery/run-session.mjs` _(blocker - S)_

**BUD-14 FAST-lane smoke test** — 2026-07-29, owner-run: launch a genuinely trivial S-effort item (mobile expense form quick-amount chip, $25→$20) through the full flight-check → FAST lane → Haiku 4.5 → $2 cap pipeline and observe. Session `s-20260729-121840-pdhx` never reached BUILDING — DISCOVERY exhausted `maxInternalTurns`, retried twice, escalated to NEEDS_DECISION; owner guidance triggered a resume-crash loop; owner cancelled after a second retry-exhaustion. No code touched. Full log: session artifacts under `.delivery/sessions/s-20260729-121840-pdhx/`. See also the Smoke Tests table in [_index](<_index.md>).

- [x] **DLV-42** ✅ **2026-07-30** — money-domain no longer fires on a money word embedded in a UI noun, and scope hints stop discarding the item's own file pointer. Root cause ran deeper than the bare regex: `computeScopeHints` threw away BUD-14's explicit `→ src/components/expense/MobileExpenseForm.tsx:1144` and substituted **all six** Budget campaign globs, so (a) `scoreComplexity`'s "broad launch scope (≥4 globs, +2)" fired — as it did for *every* Budget item ever launched, since the count came from a constant table — and (b) `frontend-impl` was attributed to a directory the change never touches. With the false money flag (+1) that scored a one-token chip edit at **3 → premium → a DEEP lane recommendation**. Now: money-domain requires a money-logic path in scope or a money *operation* named in the item (bare `amount` removed); scope = the paths the item names, with the campaign table only as a prose-item fallback (`scopeSource: "item-paths"`); and glob count only scores complexity when it is item-derived. Verified: BUD-14 now scores **0 → economy → FAST**, rationale `["S-effort item (+0)"]`, zero risk flags. → `scripts/delivery/classify.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/delivery/recommendation.mjs` _(friction - S)_
- [x] **DLV-43** ✅ **2026-07-30** — a turn that dies after the provider answered now banks what it spent. Drivers attach the observed usage to the error they raise (`withObservedUsage`/`readObservedUsage` in `drivers/driver.mjs`); `runGuardedTurn` accumulates it **across every retry attempt**, seals the turn with it, and returns it; `blockFrom` — the single choke point every phase funnels a failed turn through — banks it into `state.usage` under the same per-phase key a successful turn would use, so `enforceBudgetBoundary` can finally see spend from a phase that fails rather than succeeds. Found and fixed en route: `addUsageV2` on a pre-DLV-37 legacy-shaped total produced **NaN** (missing `cachedRead`/`cacheCreation` keys + `+=`), and `NaN > cap` is false — a resumed old session's caps would have been silently dead for the rest of its life (`toUsageTotalsV2`). **Correction to this item's own numbers:** the cited "512,752 tok / ~$0.34" was inflated ~2.4x by counting duplicate records in the raw jsonl — see DLV-47. Deduplicated, that failed DISCOVERY cost **$0.1026 / 210,193 processed tokens** and produced nothing. The bug (zero recorded) was fully real; only its magnitude was overstated. → `scripts/delivery/run-session.mjs`, `scripts/delivery/usage.mjs`, `scripts/delivery/drivers/driver.mjs` _(blocker - M)_
- [x] **DLV-44** ✅ **2026-07-30** — **the hypothesis in this item was wrong, and the real cause is narrower and worse.** Resuming after `error_max_turns` is perfectly healthy; it was simply never attempted. `runTurn` set `ref.established = true` *after* its message loop, but the SDK **throws from inside** that loop on a non-success result — so the flag was never set precisely when it mattered, and because `tickOnce` reloads state from disk each tick, every retry re-sent `sessionId` (create) for a session Claude Code had already written to `~/.claude/projects/`. Proven by probe, with the stderr the runner never captured: `Error: Session ID bffaad4e-… is already in use.` → exit code 1 in ~400 ms with zero agent activity, deterministically, forever. A second probe confirmed the fix direction: same session forced to `error_max_turns`, then retried with `sessionId` (crash) vs `resume` (clean `result/success`, $0.0009). **This bricked any turn that failed after SDK init, not just max-turns.** Fixed by flipping `established` the instant `system/init` is observed. Also: `error_max_turns` gets its own non-retryable `max-turns` error kind — retrying an identical prompt into an identical ceiling can only fail again, so the 2 automatic retries were guaranteed waste — and escalates to an owner decision naming the two real remedies (raise the lane cap, or narrow the mandated reading). → `scripts/delivery/drivers/claude.mjs`, `scripts/delivery/quota.mjs`, `scripts/delivery/run-session.mjs` _(blocker - M)_

**Green-baseline & FAST-lane rebuild** — 2026-07-30, found while making the BUD-14 smoke test reproducible. The pipeline's own launch preflight runs the full validation ladder, so anything red there taxes *every* future session.

- [x] **DLV-45** ✅ **2026-07-30** — FAST is a real fast lane now, not just a lane with a tighter turn cap. It was a lane in name only where *context* was concerned: every lane handed DISCOVERY the same mandated reading list — 4 campaign strategy docs ("read by path"), every attached skill ("do not skip"), and CLAUDE.md ("Do not skip it") — ~8 tool calls and **~33,151 tokens** of doctrine before the agent could look at the one line it was asked to change, against FAST's own `maxInternalTurns: 8`. **The phase was structurally unable to finish**, and the real session was cut off at internal turn 9 in the very turn where it had already found `QUICK_AMOUNTS` at line 1144. FAST wasn't expensive *and* broken by coincidence; it was broken *because* it was expensive. On FAST: campaign docs dropped, skills narrowed to those a **risk flag** implicates (the always-on `code-review` row's `finish-task` was being read a phase early — REVIEWING reads it by its own path anyway), and the doctrine pointer dropped in DISCOVERY/PLAN but **always kept in BUILDING**, the phase that writes code. Turn cap 8 → 12. STANDARD/DEEP untouched. Measured: DISCOVERY completed in **6 tool calls / 7 internal turns** (was 13 / ceiling hit) using **149,760 tokens for $0.1048**, and produced a complete spec. → `scripts/delivery/run-session.mjs` (`phaseContextPolicy`), `scripts/delivery/prompts.mjs`, `scripts/delivery/config.mjs` _(friction - M)_
- [x] **DLV-46** ✅ **2026-07-30** — `pnpm lint` was not failing, it was **running out of memory**: flat config's built-in ignores cover `node_modules`/`.git` and nothing else, so a bare `eslint` walked build output too — `.next` alone held ~5,130 emitted JS files, all linted with the type-aware `next/typescript` rules. Result: `FATAL ERROR: Reached heap limit ... JavaScript heap out of memory`, exit 134, after **452 seconds**, every time. The governance cost is the real damage: the launch preflight runs the full ladder, so **every delivery session saw a red baseline on lint** and demanded the owner's red-baseline acknowledgment for something that was never about the code — a guard that always fires teaches you to wave it through — and it also could not produce comparable diagnostics for delta-validation (you cannot diff a crash dump). Added an `ignores` block: lint now finishes in **14 s** (32x faster) and emits real, comparable diagnostics. → `eslint.config.mjs` _(blocker - S)_
- [ ] **DLV-52** ◐ **PARTIAL 2026-07-30** — **672 → 636 errors.** Every rule except the two big ones is now at zero, and each was a real fix rather than a suppression: `prefer-const` (16, autofix); `no-require-imports` (11) — the three standalone Node build scripts are CommonJS *by design*, so the rule is scoped off for those three paths by name in `eslint.config.mjs` (a false positive about the module system they are supposed to use, not debt), while `SettingsDialog.tsx`'s two `require()` calls in a render body were replaced with module-scope `next/dynamic` — the comment claiming they avoided a circular dependency was stale (neither statement-import module imports settings/), and a `dynamic()` inside a component body would have remounted the dialog on every render; `no-assign-module-variable` (6) — five recycle-bin routes bound a local `module`, shadowing the CommonJS global, renamed to `binModule`. **Found while renaming:** three of those references sat inside template-literal interpolations (`` `… ${module.id} …` ``) where the shadow resolved to Node's global `NodeModule`, which has a real `id` — so they type-checked and would have logged a webpack module id instead of the recycle-bin module's; `no-non-null-asserted-optional-chain` (2) — `account_id: defaultAccount?.id!` in both watch views could render a cached balance against `account_id: undefined` before the account loaded, now guarded properly; `no-empty-object-type` (1). **Still open, and deliberately not rushed: `no-explicit-any` (586) and `react-hooks/exhaustive-deps` (50).** These are not one batch — measured by pattern, the `any`s are 203 callback params, 197 miscellaneous `as any`, 66 typed declarations, 35 `catch (e: any)`, 25 `any[]`, 20 `Record<string, any>`, 13 error-property casts, spread over ~200 files; each needs the *correct* type from its own call site, and `exhaustive-deps` sits in `SyncContext`, `HubPage` and `MobileExpenseForm`, where adding a dependency can produce an infinite render loop in the offline sync engine or a money form. Both classes need per-site judgment plus real in-app verification, which no automated pass can supply. The baseline therefore stays red on lint. → `eslint.config.mjs`, `src/components/settings/SettingsDialog.tsx`, `src/app/api/recycle-bin/**`, `src/components/watch/*.tsx`, `src/types/trips.ts` _(friction - L)_
- [x] **DLV-47** ✅ **2026-07-30** — **the raw SDK transcript is not usable as ground truth without deduplication, and this campaign's own cost forensics were inflated by it.** Claude Code's `~/.claude/projects/<slug>/<id>.jsonl` repeats each assistant record — the BUD-14 session wrote **12 usage-bearing records for 5 unique `message.id`s** (one id appeared 4x). Summing records therefore double-counts real API calls. Deduplicated by `message.id`, the new session's raw totals are `input 34 / cachedRead 106,165 / cacheCreation 41,753 / output 1,808` — **exactly** what `state.usage` recorded, i.e. post-DLV-37 accounting fidelity is **100%**, not the "1.9x under-report" recorded in [7 · Cost Anatomy](<7 - Cost Anatomy (DLV-28 smoke tests).md>) §4. `smoke-launch.mjs cost` reconciles deduplicated raw vs recorded on demand. Cost Anatomy §4 and DLV-43's cited figures corrected. → `scripts/delivery/smoke-launch.mjs` _(blocker - S)_
- [x] **DLV-48** ✅ **2026-07-30** — the failing test the PM notes carried for days as "the pre-existing DLV-34 handoff failure" **was not DLV-34** (that item is about the *success* path writing the new provider's ref onto the old provider's driver, which the test never exercises). It was a collision with DLV-4: once retry-exhaustion got its own escalation path, an exhausted handoff-verification turn lands in NEEDS_DECISION rather than BLOCKED. Both are owner-facing stop states and either one's retry re-establishes the provider from scratch, so the behaviour is fine — the assertion was pinned to a *state name* instead of to the contract the test is named for. It now asserts that contract (stopped and awaiting, failure on the record, provider **not** silently switched, no stale ref). Mislabelling it hid a green suite behind a red one. → `tests/delivery/run-session-handoff.test.ts` _(friction - S)_
- [x] **DLV-49** ✅ **2026-07-30** — `smoke-launch.mjs`: a CLI harness that launches and inspects a real delivery session through `routeDelivery` (the same code path the dashboard uses, so a smoke test can never validate a path the real UI does not take) instead of re-driving the browser by hand for every reproduction. Subcommands: `preflight`, `flightcheck`, `launch`, `status`, `cost`, `decide`. It **cannot author a gate decision** — `decide` only relays a decision the owner supplies explicitly. → `scripts/delivery/smoke-launch.mjs` _(annoyance - S)_
- [x] **DLV-50** ✅ **2026-07-30** — repo typecheck restored to **0 errors** (was 11) and delivery lint to clean. All 11 were JSDoc typedefs that under-described what the function actually accepts, so correct call sites type-errored: `checkSessionBudget`'s third `scope` argument was undocumented, `loadConfig` returned a bare `object` (new `DeliveryConfig` typedef derived from `DEFAULT_CONFIG`), `buildPacket` had no `@param` at all so every `= null` field inferred as `null|undefined`, `scoreComplexity`/`recommendAgentConfig` omitted `scopeHints`, and `classifyValidationFailure` had no `@returns`. Fixed at the source rather than cast away in tests. Also two `eslint-disable` directives that silenced nothing: one named the core rule where this repo enables the TS-aware one, and both sat in multi-line `//` blocks where `disable-next-line` targets the comment's own next line. _(friction - S)_
- [x] **DLV-51** ✅ **2026-07-30** — the last red test in the repo was a **false-positive drift guard**, not a Schedule bug. `expandOccurrences.test.ts` demanded a literal `recurrence_rule?.is_flexible` in all six documented placement views; `WebTodayView` legitimately has none because it delegates to `expandOccurrencesInRange`, which already owns the skip (`is_flexible && !includeFlexible → continue`) and takes the scheduled rows as a parameter — the *better* pattern, since the rule lives in one place instead of being re-implemented per view. The guard now accepts either inline-guard or delegation, so it still catches the drift that matters (a view expanding on its own with no guard). Whole repo: **1253/1253 tests green**. → `src/lib/schedule/expandOccurrences.test.ts` _(friction - S)_

**BUD-14 re-run findings (s-20260730-104900-9mfu)** — 2026-07-30, owner-driven with all three gates honoured. DISCOVERY and PLAN both completed; the owner rejected the plan for over-decomposition, and the reject path itself surfaced these. Session paused at the budget gate, $0.5317 spent, **zero lines of code changed**.

- [x] **DLV-53** ✅ **2026-07-30** — a rejected plan now returns to **SPEC_READY** with the spec gate re-armed (`awaiting: {gate: "spec", reason: "plan-rejected"}`), not to DISCOVERY. Approving from there runs a fresh PLAN turn, which is what "revise the plan" means, and the rejection note rides along as `pendingGuidance` so the re-planning turn actually sees why the last plan was refused. Rejecting the *spec* still goes to DISCOVERY, where the phase and the artifact do line up. Note the second half of the original bug: `consumePlanDecision` also hardcoded `awaiting: null`, which on a gate state parks the session with nothing asking the owner for anything — so the destination fix alone would have replaced a wrong phase with a dead one. Originally filed as: Rejecting a plan throws the session all the way back to **DISCOVERY**, not back to PLAN. `state-machine.mjs`: `PLAN_READY + decision.reject → {to: "DISCOVERY", effects: ["revisePlan"]}` — the effect is named `revisePlan` but the destination phase's prompt asks for a **spec**, and the spec had just been approved and was not in question. Observed consequences on the re-run, in order: the owner's note ("re-plan with exactly ONE step, fold verification into validationHint") was handed to a phase that cannot produce a plan; the agent tried to comply anyway ("I'm re-entering PLAN phase"), emitted plan-shaped output, and was rejected by `SPEC_OUTPUT_SCHEMA`; it then mis-inferred its phase from the packet (see DLV-54) and emitted a third shape. Net: **$0.1683 spent, the plan never revised, and the approved spec needlessly re-authored.** A plan rejection should return to `SPEC_READY` — spec still approved, awaiting a fresh PLAN turn — or DISCOVERY's prompt must be parameterised by the revision effect. → `scripts/delivery/state-machine.mjs`, `scripts/delivery/run-session.mjs` _(blocker - M)_
- [x] **DLV-54** ✅ **2026-07-30** — renamed `packet.mode` → `packet.deliveryMode`, value `"uat"` → `"uat-gated"`. The field had **zero readers** anywhere in the runner, the routes or the PM UI — it was write-only, and its only effect in practice was to mislead the agent that reads `packet.json` by path. A test now asserts that no string field in the packet holds a bare phase name, so this class of collision cannot come back. Originally filed as: `packet.mode` is read by the agent as its **current phase**. The field carries the session's delivery mode and its default value is the string `"uat"` (`buildPacket`'s `mode = "uat"`), which collides with a phase name — and every phase prompt invites the agent to read `packet.json` by path (DLV-8). Caught verbatim in the event log: *"I see — the mode is \"uat\", which means I'm now in the UAT phase, not PLAN."* The agent then emitted UAT-shaped output during a spec turn. Rename the field (`deliveryMode`, or values `gated`/`autonomous`), or drop it from the header the agent sees; a field whose value looks like an instruction will keep being followed as one. → `scripts/delivery/packet.mjs`, `scripts/delivery/prompts.mjs` _(blocker - S)_
- [x] **DLV-55** ✅ **2026-07-30** — the step budget is now stated to the planner **at generation time** (`renderPlanStepBudget` in `prompts.mjs`): the ceiling, why fewer is cheaper (each step is its own full-context turn), and the two failure modes actually observed — no steps that re-verify what the spec already establishes (fold into `validationHint`), none for work a human must do by hand (that is the UAT package). The agent had never been told a ceiling existed, which is why it produced 6 steps for a one-line change; telling it costs no extra turn and no rejection loop. The post-hoc check stays as a backstop but is no longer buried in `events.ndjson` — it rides on `awaiting.warnings` so it is unmissable at the plan gate, the last moment it can be acted on for free. Deliberately still not a hard block: a long plan can be legitimate, and blocking would strand the session. Originally filed as: `maxPlanSteps` is advisory and was ignored on exactly the item it exists for. The runner emitted `plan.step_count.warning {stepCount: 6, maxPlanSteps: 5}` for a **one-line string replacement**, then proceeded. Of the 6 steps only STEP-2 was executable: STEP-1/3/4 re-verified what DISCOVERY had already established (line 1144's contents, the 2 repo-wide `QUICK_AMOUNTS` references, the no-`ui/**` constraint) and STEP-5/6 were manual browser checks BUILDING cannot run at all. At the measured ~$0.10–0.19 per full-context turn, a 6-step plan is a ~$0.6–1.1 BUILDING phase for one character of change. [7 · Cost Anatomy](<7 - Cost Anatomy (DLV-28 smoke tests).md>) deliberately deferred enforcement until DLV-30 landed; DLV-30 has landed and the problem did not dissolve. Enforce it as a hard gate on FAST (and consider deriving the cap from lane rather than one global number). → `scripts/delivery/run-session.mjs`, `scripts/delivery/config.mjs` _(blocker - M)_
- [x] **DLV-59** ✅ **2026-07-30** — the triage gate can finally refuse the items it was written for. The DLV-39 rule was `S-effort && riskFlags.length === 0`, which **could never be true for a UI item**: every S-effort UI tweak in this repo carries `frontend-impl`, so BUD-14 — a one-character preset change — launched and spent **$0.5317 without writing a line of code**. Two facts resolved it without a new judgment call. First, the Agent Catalog already draws the distinction: `money-domain` is `blocking`, `product-ba-refinement` is `advisory`, and `frontend-impl`/`backend-impl` are declared as **neither** — because their reasons are literally `"frontend glob"`/`"api glob"`, derived from where a file lives and never from what the change does; they are routing signals whose job is attaching a skill. Second, DLV-39's own stated blocker — *"real changed files don't exist yet at launch time, so 'single-file' isn't directly knowable pre-launch"* — stopped being true at DLV-42: scope now comes from the paths the item's own text names. The rule is now S-effort, no veto capability, and **either** zero optional flags (the original signal, still the only one available for a prose-only item — narrowing to the new one alone silently stopped refusing the DLV-28 profile) **or** exactly one file named by the item itself. Absence of named paths is never treated as evidence of triviality. `"LAUNCH ANYWAY"` still overrides, and the refusal message now quotes the measured $0.53. → `scripts/delivery/classify.mjs`, `scripts/delivery/server-routes.mjs` _(blocker - M)_
- [x] **DLV-56** ✅ **2026-07-30** — the forecast's unit is a phase traversal now, not a session. `forecastByPhase` estimates each turn-producing phase separately and sums them, with BUILDING multiplied by the plan steps the item's effort implies (S→1, M→3, L→5, capped at `maxPlanSteps`) — which is precisely what a per-session shape could not express, and why DLV-38's $0.175 was quoted for a session that spent $0.5317 before writing a line. The static per-phase table is anchored on this campaign's own measurements (DISCOVERY post-DLV-45 at 149,760 tok / $0.1048; a `medium` PLAN turn at $0.1857), and `estPhaseUsage` replaces it with the real median from `usage.perPhase` once three same-tier sessions have completed — data recorded on every session since DLV-37 and, until now, never read. The Flight-Check gets the per-phase breakdown, so a cap is set against something itemised rather than one opaque number. Fix loops are excluded from the headline and priced separately as `perFixLoop`: their count is genuinely unknowable at launch, and folding in a guess would make the forecast wrong in the one direction that matters while looking more precise. **Found and fixed while writing the tests:** `priorSource` reported `measured-median` whenever any history row carried a `perPhase` object — including `{}` — so a forecast that was entirely static could label itself measured. It is now derived from the same predicate `estPhaseUsage` uses, per phase, and reports `mixed` when only some phases have real priors. The old per-session estimate is kept as `legacySessionEstimate` rather than deleted, because every session already on disk was launched against it and `computeForecastActual` still compares to it. → `scripts/delivery/recommendation.mjs`, `scripts/delivery/server-routes.mjs` _(friction - M)_
- [x] **DLV-57** ✅ **2026-07-30** — economy tier's `plan` effort `medium` → `low`. Measured: a `medium` PLAN turn cost **$0.1857** to decompose a one-line edit — more than the DISCOVERY turn that had to actually locate the code, and more than the entire session's $0.175 forecast. `building` stays `medium`: it is the phase that writes the code, and reasoning there is the difference between a correct edit and a plausible one. STANDARD/DEEP untouched. Originally filed as: FAST's per-phase effort map spends `medium` on PLAN and BUILDING (`EFFORT_BY_TIER.economy = {discovery: low, plan: medium, building: medium, review: low}`). PLAN at `medium` cost **$0.1857** to decompose a one-line edit into 6 steps — more than DISCOVERY, which had to actually find the code. For the lane whose entire purpose is trivial work, `low` across the board is the defensible default; keep `medium` for STANDARD. Cheap to change, and the measurement to justify it now exists. → `scripts/delivery/recommendation.mjs` _(friction - S)_
- [x] **DLV-60** ✅ **2026-07-30** — `renderReadWindowGuidance` derives a bounded window (`offset: line-40, limit: 120`) from the pointer the item already carries, and states *why*: tool output is re-sent to the model on every internal turn that follows it, so one unbounded Read is charged many times over. Wired into DISCOVERY **and** BUILDING — BUILDING re-reads the file it is about to edit, at `medium` effort over more internal turns, and the Edit tool needs the target text, not the file. An item with no pointer gets the general locate-then-read guidance and never an invented offset; the offset clamps at line 1. Applied in **every lane**, unlike DLV-45's FAST-only narrowing: that traded context for speed and is a real tradeoff, while reading 3,099 lines to change one is never the better choice — a DEEP session that needs the whole file is still free to widen, since this is guidance with a stated escape rather than a tool restriction. Cost **+1,518 chars (~380 tokens) per turn** against ~96K saved on the measured turn (~240:1). 12 new tests; repo 1276/1276 green, typecheck 0, lint clean. Originally filed as: when the work item names a `file:line`, DISCOVERY reads a window around that line instead of the whole file. Measured on `s-20260730-104900-9mfu` turn 0001 — the item ended in `MobileExpenseForm.tsx:1144` and the agent's *first* code read was `Read {file_path, limit: 2000}` with **no offset**, returning **69,204 bytes** of a 3,099-line / 134 KB component; its very next call was `Read {offset: 1140}`, which is what it should have done first. That read is ~18K tokens, and because the turn ran 7 internal model calls it was re-sent on all 5 subsequent ones — **~90K of the turn's 147,952-token occupancy (74% of a 200K window) for a one-character change**. DLV-45 narrowed what the *runner* sends; nothing tells the *agent* how to read. Applies to every lane — reading 3,099 lines to change one is never the better trade. → `scripts/delivery/prompts.mjs` _(friction - S)_
- [x] **DLV-61** ◐ **PARTIAL 2026-07-30** — measured and instrumented; **no lever exists at the TTL level**, so the remedy is DLV-60 + DLV-62 rather than anything in this item. `extractCacheCreationTtl` now records the split per turn (rides to `turns.ndjson` via `buildTurnEntry`), deliberately **outside** `UsageTokensV2`: accumulators read named keys and `toUsageTotalsV2` never spreads, so the diagnostic is inert everywhere a cap is computed — which is what keeps it clear of the NaN class of bug DLV-43 hit in this same file. `null` when unreported is distinguished from a real `{0,0}`. Still open, folded into **DLV-56**: modelling forecast cost on writes-per-phase-traversal rather than a per-session read-shaped estimate. Findings as filed: On `s-20260730-104900-9mfu`: cache **writes** were **231,567 tokens = $0.4631 = 87.4%** of the $0.5302 session, against cache *reads* of 239,959 tokens = $0.0240 = 4.5%, output 8.1%, fresh input 0.0%. Two things verified from the raw transcript, deduplicated by `message.id` per DLV-47: (a) **100% of writes are 1-hour TTL** (`ephemeral_1h_input_tokens` 231,567 vs `ephemeral_5m` **0**), so `cacheWritePerMTok: 2` is correct and the spend is real, not an accounting artifact — this closes DLV-37's deferred diagnostic, now recorded per-turn rather than re-derived by hand; and (b) the 1h premium buys nothing across turns — turn 0002 opened with `cache_read_input_tokens: 0` against turn 0001's live 106K cached prefix, i.e. **each runner turn starts a fresh prefix and re-writes it at 2x**. Session-wide read:write is **1.04:1**, barely past the ~1.1:1 break-even where caching pays for itself at all. The SDK exposes no TTL knob (`@anthropic-ai/claude-agent-sdk` has no cache-ttl option), so the premium is not avoidable — which makes prefix *volume* worth roughly twice what it appears, and is the strongest argument for DLV-60 and DLV-62. Model cost per phase-traversal on writes, not reads (feeds DLV-56). → `scripts/delivery/usage.mjs`, `scripts/delivery/drivers/claude.mjs` _(friction - M)_
- [x] **DLV-62** ✅ **2026-07-30** — **owner decision recorded: options (a)+(b), NOT (c).** The standing "3 gates always" rule is untouched and unamended. (a) was already shipped as DLV-59's triage gate. (b) is now built: on FAST, when the item names **exactly one file**, DISCOVERY and PLAN run as a single turn — `resolveMergedDiscoveryPlan` decides eligibility at launch and records both the verdict and its reason on `packet.lanePolicy`, and `MERGED_SPEC_PLAN_SCHEMA` is *composed from* the two existing schemas rather than restating them, so the spec the gate approves is byte-identical in shape to the two-turn path's. The single-file condition is the safety rail, not a convenience: a change confined to one file the item itself names is one whose spec and plan cannot meaningfully diverge. Measured on the fixture: a full run to UAT_READY takes **4 turns instead of 5**, and reaching the plan gate takes 1 turn instead of 2. All three approvals still fire — there is a test that walks spec → plan → uat explicitly, because (c) not being authorized is the load-bearing constraint here. Two details that would each have been a bug: the merged plan is persisted only once the spec is actually going to the gate (a discovery turn ending in open questions is re-run, and a plan written from a superseded spec is worse than none), and `mergedPlanPending` is cleared on a plan rejection so "revise the plan" runs a **real** PLAN turn instead of re-approving the same merged plan forever. A merged turn that omits `plan` blocks rather than half-accepting. Owner-switchable via `pipeline.mergeDiscoveryPlanOnFastSingleFile`. → `scripts/delivery/recommendation.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/prompts.mjs`, `scripts/delivery/config.mjs`, `tests/delivery/run-session-merged.test.ts` _(blocker - M)_
  <br>*Original analysis:* FAST is a cost-tuning lane wearing the name of a shorter one, and that is the root cause behind DLV-45/55/57/59 rather than a sibling of them. FAST changes five dials — model, per-phase effort, budget cap, context reading list, validation rungs (`["typecheck","test"]` vs `["typecheck","lint","test"]`) — and **changes nothing about the pipeline's shape**: BUD-14 carried the same three always-on blocking capabilities (`automated-testing`, `code-review`, `uat-generation`, which `applyCapabilityDrops` *throws* on any attempt to drop, in every lane) and the same three approval gates (`SPEC_READY`, `PLAN_READY`, `UAT_READY`) that a DEEP multi-file refactor carries. For a one-line change the pipeline **is** the cost: $0.5302 across 4 turns produced a spec and a rejected plan and zero lines of code. **Needs an owner decision before any code — this touches the standing "3 gates always" rule** ([6 · Design Debates](<6 - Design Debates & Rejected Ideas.md>)), and DLV-39's own text is explicit that triage "refuses entry only — it never reduces oversight inside a running session". Options, cheapest first: (a) leave the shape alone and let DLV-59's triage gate keep trivial items out entirely — no rule change, already shipped; (b) merge DISCOVERY+PLAN into one turn on FAST when the item names a single file, keeping all three gates but halving the traversals; (c) collapse the SPEC and PLAN gates into one approval on FAST — genuinely weakens oversight, and the one option the standing rule forbids without an explicit amendment. Recommend (a)+(b); (c) needs the rule amended in writing first. → `scripts/delivery/config.mjs`, `scripts/delivery/state-machine.mjs` _(blocker - M)_
- [x] **DLV-58** ✅ **2026-07-30** — verified in flight, no fix needed: the budget cap **held**. `budget.exhausted` fired at $0.5317 against the $0.50 authorization, the session paused at a `budget` gate, and `awaiting.priorAwaiting = {gate: "spec"}` correctly preserved the phase gate underneath it — DLV-29's collision fix working on real spend rather than a fake driver. The $0.0317 overrun is expected and by design: enforcement is *between* turns, so an in-flight turn can cross the line before anything can stop it. Worth stating in the owner-facing budget copy so the cap reads as "stop after exceeding" rather than "never exceed". _(annoyance - S)_

**Cost anatomy** — forensics on the smoke-test spend: the same one-line doc edit cost $0.60/$1.95/$2.40 depending only on how far it travelled. Full analysis in [7 · Cost Anatomy](<7 - Cost Anatomy (DLV-28 smoke tests).md>).

- [x] **DLV-33** Per-spawn overhead floor: `strictMcpConfig` + `mcp__*` denial (16-36 unused Gmail/Supabase/Chrome tools rode along on 10 of 13 turns), explicit skills filter, and a build-mode tool allowlist → `scripts/delivery/drivers/claude.mjs` _(friction - S)_
- [x] **DLV-35** Overhead probe script: one-shot `maxTurns:1` query reporting fixed per-spawn context so driver-option changes are A/B-measurable for a fraction of a cent instead of a real session → `scripts/delivery/probe-overhead.mjs` _(annoyance - S)_
- [x] **DLV-37** Usage accounting tells the truth: real drivers now populate `usageV2` (`cache_creation_input_tokens` was extracted nowhere before — `extractUsageV2`/Codex `normalizeUsageV2` wiring), `state.usage` is v2-shaped end-to-end (`accumulateUsage`/`addUsageV2`) so `totalProcessedTokens` — and every budget cap it drives — includes cache-creation tokens, `.delivery/config.json` `cacheWritePerMTok` corrected to the 1-hour rate (2x input, not 1.25x), and the PM UI's three under-reporting spots (session header chip, UsageMeter, UsageView processed-token calc, fleet list) all fixed with legacy-`cachedInput` fallback for old sessions. Done 2026-07-28; `pnpm test tests/delivery/` green except the pre-existing DLV-34 handoff failure. Deferred as a non-blocking diagnostic nicety: recording the raw `ephemeral_1h_input_tokens`/`ephemeral_5m_input_tokens` split — every observed session is 100% 1h, so it has no accounting consequence yet, only audit value. → `scripts/delivery/usage.mjs`, `drivers/claude.mjs`, `drivers/codex.mjs`, `run-session.mjs`, `server-routes.mjs`, `budgets.mjs`, `scripts/pm/src/features/delivery/{SessionDetail,UsageView,DeliveryHome}.jsx` _(blocker - S)_
- [x] **DLV-31** Drivers survive a rotation. Added `reset()` to every driver's interface (claude/codex/fake), called from `getHandle` right before any `startSession` — the single choke point regardless of *why* `state.driver.ref` went null (rotate control, quota-paused retry, or any future path), rather than chasing every ref-nulling call site individually. Idempotent, so the normal first-tick case (never started) is a harmless no-op. Regression test in `run-session-context.test.ts` reuses one driver instance across DISCOVERY → rotate → PLAN (the prior version of this test built a fresh `planDriver` for the post-rotation turn, which is exactly what hid the bug — a fresh instance's `started` flag is always false); verified red-then-green by temporarily disabling the `reset()` call (session went `BLOCKED` instead of reaching `PLAN_READY`) before restoring it. Done 2026-07-28. → `scripts/delivery/drivers/{claude,codex,fake,driver}.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/run-session-context.test.ts` _(blocker - S)_
- [x] **DLV-32** Owner answers and rejection notes now reach the agent in every phase, and PLAN/REVIEWING/BUILDING can stop and ask for the first time. `pendingGuidance` is read in `handleBuilding`, `handleReviewing`, the UAT-prep sub-turn, and `consumeSpecDecision` (new — see below), not only `handleDiscovery`; a UAT rejection note is written as guidance (`consumeUatDecision`), not ledger-only. `PLAN_OUTPUT_SCHEMA` gained an optional `openQuestions` field (not required — SPEC's is required, but making PLAN's required would have failed every pre-existing plan-turn fixture across 7 test files; `parsePlanOutput` defaults it to `[]`), and `memory.mjs`'s `applyPlan` now also raises a ledger-tracked question per entry (own function, not a reuse of `applySpec` — that would have wiped `ledger.requirements` on a plan object that has no `acceptanceCriteria`). REVIEWING's output schema was already permissive (`{type:"object"}`, no `required`), so `openQuestions` needed only a prompt instruction, not a schema change. BUILDING has no structured output contract at all (free text appended to `build-log.md`) — extended with a `BLOCKING QUESTION: <text>` sentinel line (same house pattern REVIEWING's own "VERDICT: PASS" line already uses), exported as `BLOCKING_QUESTION_MARKER` from `prompts.mjs` so the string is never hand-copied (DLV-26's own lesson about drift). **Found and fixed a correctness bug while wiring PLAN's question-raising**: `SPEC_READY` is a GATE state (unlike `DISCOVERY`), so answering a PLAN-raised question with `returnTo:"SPEC_READY"` and the pre-existing `awaiting:null` would have stranded the session with no visible gate panel — the exact DLV-29 failure shape, reproduced by my own new code before I caught it. Fixed by making `consumeQuestionDecision` re-arm the gate exactly like `consumeBlockedDecision` already does for `returnTo`. Also found, while writing the PLAN-question regression test, that `consumeSpecDecision`'s own `ownerMessages` draining never read `pendingGuidance` either — fixed for consistency, or an answer to PLAN's own question would have been dropped the same way BUILDING/REVIEWING's used to be. Verified end-to-end with 3 new integration tests reading the actual `transcript/prompts/*.md` files (not just state) to prove guidance text reaches the model, not merely gets cleared: PLAN raises a question and the gate re-arms correctly, the retried PLAN turn's prompt contains the answer; BUILDING's marker sentinel raises a question without advancing the step or writing to `build-log.md`, the retry's prompt contains the answer; a UAT rejection note appears in the fix-loop BUILDING turn's prompt (DLV-32's own flagship complaint: "owner-visible as 'I answered and it ignored me'"). `pnpm test tests/delivery/` green (930/931 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set. Done 2026-07-28. → `scripts/delivery/run-session.mjs`, `scripts/delivery/prompts.mjs`, `scripts/delivery/memory.mjs`, `tests/delivery/{run-session-memory,memory,prompts}.test.ts` _(blocker - S)_
- [x] **DLV-41** Close the Bash escape hatch to the live Supabase project — found during the 2026-07-28 forensic review: `canUseTool`'s Bash screen already denied git-mutating commands and secret-path references, but let through any other shell text, including the `supabase` CLI, `psql`, `curl`/`wget` against a Supabase REST/RPC endpoint, or a script referencing `SUPABASE_SERVICE_ROLE_KEY`/`NEXT_SUPABASE_SERVICE_ROLE_KEY`/`supabaseAdmin()` — the exact "equivalent back doors" Hard Rule #26 names, and MCP being sealed (`strictMcpConfig` + `mcp__*` denial, DLV-33) never covered this route. New `containsDbWriteEscape()` denies unconditionally (no read-only carve-out, unlike git — there's no legitimate delivery-session reason to touch the live project at all): tokenizes each shell segment and flags the `supabase`/`psql` executable name anywhere in it (catches runner-prefixed invocations like `npx supabase ...`, not only a bare leading command), flags `curl`/`wget` whose target matches a Supabase domain or `/rest|auth|storage/v1/`/`/rpc/` path, and flags the two service-role-key env var names plus `supabaseAdmin` as a whole-command text match. Wired into the existing Bash branch of `canUseTool` right after the secret-reference check. `pnpm test tests/delivery/` green (938/939 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set (no new errors). Done 2026-07-29. → `scripts/delivery/drivers/claude.mjs`, `tests/delivery/drivers-claude.test.ts` _(blocker - S)_
- [x] **DLV-39** Hard triage gate at launch — D11 of the owner's remediation backlog. Refuses to launch an S-effort item with zero risk flags through the full pipeline without an explicit typed override, on the grounds (Cost Anatomy §1) that such an item cannot fail in an interesting way and the pipeline's own overhead dwarfs the edit itself. **This refuses entry only — it never reduces oversight inside a running session**; all three approval gates plus the owner SHIPPED mark are unaffected, in every lane, always. New `isTrivialLaunchCandidate(item, riskFlags)` in `classify.mjs` — since real changed files don't exist yet at launch time, "single-file"/"docs-only" from the plan's own wording isn't directly knowable pre-launch; the best available proxy reuses the exact `riskFlags` vocabulary the Flight-Check panel already computes and shows the owner (`buildLaunchPreview`'s `riskFlags` — capabilities minus the always-on set: money-domain, frontend-impl, backend-impl, product-ba-refinement). Deliberately, a vague item (product-ba-refinement) is never triage-refused even at S-effort — it genuinely needs DISCOVERY's clarifying pass, not a shortcut around it. New typed override `TRIAGE_OVERRIDE_ACK = "LAUNCH ANYWAY"` in `validation-baseline.mjs`, same house pattern as `DIRTY_TREE_ACK`/`RED_BASELINE_ACK`; recorded into `packet.workspace.acknowledgments.triage` and `packet.flightCheck.baseline.acknowledgments.triage` when used, for audit. The refusal message surfaces the item's own D8-corrected forecast (`recommendation.estCostUsd`/`estTokens`) rather than a fixed illustrative number. Wired into both the server (`startSession`, refuses with 400 + the typed-override instruction) and the launch UI (`DeliveryHome.jsx` imports the *same* `isTrivialLaunchCandidate`/`TRIAGE_OVERRIDE_ACK` the server enforces — both already bundle cleanly into the browser build since they're zero-dependency modules — so the UI can never drift from what the server actually enforces; a conditional input + forecast warning block appears only when the gate would fire, mirroring the existing dirty-tree/red-baseline UI exactly). 8 new tests: `classify.test.ts` unit tests for the trivial-candidate predicate (fires on S+zero-flags, never on M/L-effort, never with any risk flag present including product-ba-refinement, handles null/malformed input); `server-routes.test.ts` integration tests using the real DLV-28 item's own campaign ("Delivery 10x," not in `CAMPAIGN_MODULE_GLOBS`, so genuinely zero risk flags) proving the 400 refusal, the typed-override launching successfully with the ack recorded in the packet, and an M-effort item launching unimpeded even with zero risk flags. **Found while writing the test fixture**: every `Budget`-campaign item unconditionally gets `frontend-impl` flagged (`CAMPAIGN_MODULE_GLOBS.Budget` includes `src/features/**` paths, all under `FRONTEND_GLOB_ROOTS`) regardless of what the item's own text says — meaning no Budget-campaign item, however trivial, can ever trigger this gate. Judged correct, not a bug: Budget items are money-adjacent product code far more often than not, and the real DLV-28-shaped triviality this targets lives in PM/docs-only campaigns like Delivery 10x itself, not in a money module. `pnpm test tests/delivery/` green (969/970 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set (one new error from an under-typed test input was fixed at the source — `isTrivialLaunchCandidate`'s own JSDoc `@param` widened to accept `null`, matching what the function already handled correctly at runtime). Done 2026-07-29. → `scripts/delivery/classify.mjs`, `scripts/delivery/validation-baseline.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/pm/src/features/delivery/DeliveryHome.jsx`, `tests/delivery/{classify,server-routes}.test.ts` _(friction - S)_

## Next

**M2 — Right-Sized Delivery** — session weight matches task weight.

- [x] **DLV-6** ◐ **PARTIAL 2026-07-29 (D9)** — lanes are a real, packet-resolved policy bundle now: effort-per-phase and budget-envelope defaults (previously fixed sub-parts of DLV-6). New `resolveLanePolicy(lane, config)` in `recommendation.mjs` resolves FAST/STANDARD/DEEP into `{lane, tier, effortByPhase, budget, maxInternalTurns}` at launch, recorded verbatim as `packet.lanePolicy` for audit. Effort: an omitted or partial `effort` in the launch request now defaults per-phase from the lane (merge, not all-or-nothing — a single overridden phase no longer silently drops the other three to the old static default); an explicit owner value always wins for that phase. `maxInternalTurns` (new config field, `budgets.laneDefaults.<lane>.maxInternalTurns`, defaults 8/20/40) is mapped by the claude driver onto the SDK's own `Options.maxTurns` — the missing cap that let one runner turn silently become ~40 model calls (Cost Anatomy §5) — via new `packetMaxInternalTurns()` in `run-session.mjs`, threaded through all 5 phase-producing `runGuardedTurn` call sites. **Also found and fixed**: `DeliveryHome.jsx`'s Flight-Check budget prefill was hardcoded to `laneDefaults.standard` regardless of the lane the owner actually selected in the UI — a FAST launch pre-filled the STANDARD envelope ($2/2M, not FAST's $0.50/500K). `grep -i "lane" scripts/delivery/run-session.mjs` no longer returns 0 matches. **Still open** (deliberately not folded into D9): per-lane context budgets and the validation-contract ladder (queued as D10, needs D5+D9 — both now satisfied), and the "route trivial work around the pipeline" triage gate (D11, needs D8 — now satisfied). 9 new tests across `recommendation.test.ts` (`tierForLane`/`resolveLanePolicy`), `config.test.ts`, `packet.test.ts`, `server-routes.test.ts` (FAST launch resolves correctly; partial owner override merges with the lane default; explicit override wins), `drivers.test.ts`/`drivers-claude.test.ts` (`maxTurns` threads onto the ref / `buildSessionOptions`), and `run-session.test.ts` (end-to-end: `packet.lanePolicy.maxInternalTurns` reaches `state.driver.ref.maxTurns` via the fake driver; a pre-D9 packet with no `lanePolicy` stays uncapped, unchanged). Codex's `runTurn` accepts `maxTurns` for interface parity but does not forward it — no verified `@openai/codex-sdk` equivalent to `Options.maxTurns`, and every observed real session used the claude provider, so guessing at an unverified field was judged worse than an honestly-documented gap. `pnpm test tests/delivery/` green (954/955 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set (three new inference-quirk errors matching the pre-existing `budget`/`flightCheck` pattern were fixed at the source — `resolveLanePolicy`'s JSDoc `@returns` typed `effortByPhase` as bare `object` instead of its real shape). → `scripts/delivery/recommendation.mjs`, `scripts/delivery/config.mjs`, `scripts/delivery/packet.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/drivers/{claude,codex,fake}.mjs`, `scripts/pm/src/features/delivery/DeliveryHome.jsx` _(friction - M)_
- [x] **DLV-7** ✅ **2026-07-30** — the scope contract, end to end. DISCOVERY must now return `scopeEstimate {files, occurrences, modules}` (required by `SPEC_OUTPUT_SCHEMA`, so real generations are bound by it), and the runner — not the agent — derives the size class from owner-set `scope.thresholds`. That split is the point: BUD-11's own spec turn measured 72 occurrences across 25 files on an item filed as S, in an artifact it wrote itself, and never revisited the label. A model grading its own homework is not a tripwire. On a mismatch the SPEC gate carries the verdict *and* a 2–4 slice decomposition the same turn produced (asked for conditionally, so it costs nothing in the common case and needs no rejection round-trip when it fires); the owner either takes a slice — narrowing this session's ACs, with the rest written to `decomposition-proposal.json` as candidates the owner files, never auto-filed — or types `FULL SCOPE`. Enforcement is server-side alongside the dirty-tree/red-baseline/triage acks, deliberately **not** in the runner: a runner that refuses an already-written decision parks the session at a gate with nothing left to answer it, which is the DLV-53 failure. Post-PLAN, `buildScopeLock` freezes the AC ids and the union of the plan's declared paths, and a BUILDING turn that writes outside them raises a blocking question rather than sprawling silently — BUD-11 did both, growing to 25 files *and* quietly shipping 5 hooks' worth. A plan that declared no paths never trips it. An absent estimate degrades to "not reported", never to a fabricated zero; a malformed one blocks. `memory.mjs` gained a `runner` question source so the ledger can tell a governance question from one the agent asked. → `scripts/delivery/scope.mjs`, `run-session.mjs`, `prompts.mjs`, `config.mjs`, `server-routes.mjs`, `validation-baseline.mjs`, `tests/delivery/scope.test.ts`, `tests/delivery/run-session-scope.test.ts` _(blocker - M)_
- [x] **DLV-8** ✅ **2026-07-30** — the remaining half. New `context-budget.mjs` applies a per-lane, per-phase token budget to the mandated reading list, dropping lowest-priority sources first (item > doctrine > skill > campaign — ordered by how *item-specific* a source is, so the campaign strategy docs are the first thing worth losing and the work item is never droppable) and recording every drop as a `context.dropped` event. This is deliberately a second, separate step from DLV-45's lane narrowing: that rule is editorial ("a FAST session has no business reading campaign docs") and fires however small those docs are, while the budget is arithmetic and catches what the editorial rule cannot see — a STANDARD session whose skills have quietly grown past what a phase can afford. Sources are sized with a `stat`, never a read: measuring a file must not cost what reading it would. Every turn writes a loaded-context manifest under `transcript/context/<turnId>.json`, with the packet listed as `referenced` and pointedly **not** summed into the totals, because referenced-by-path is exactly the cost DLV-8's first half stopped paying per turn. The Flight-Check preview is now lane-aware (it was describing an unnarrowed session nobody was about to launch) and shows the per-phase reading list including anything the budget would drop, at the one moment changing lane is still free. Replayed tool output is bounded head-and-tail (`context.maxReplayedOutputChars`, default 8,000) — the fix-loop validation excerpt was unbounded, and a 48-error typecheck dump is re-sent on every internal turn that follows it. FAST is the only lane budgeted by default; STANDARD/DEEP are `null` throughout, because a lane that exists to be thorough should not be silently thinned. → `scripts/delivery/context-budget.mjs`, `run-session.mjs`, `config.mjs`, `server-routes.mjs` _(friction - M)_
- [x] **DLV-9** ✅ **2026-07-30** — `reassessFitAfterDiscovery` re-scores the item with DLV-7's **measured** size class standing in for the declared effort, leaving every other signal untouched so the delta it reports is attributable to scope alone. A measured scope needing a richer tier than the model actually running lands as a warning on the SPEC gate; approving through it *is* the acknowledgment, and it is recorded (`recommendation.acknowledged` + `scope.fitAcknowledgedAt`) rather than merely dismissed, so a session that overran on an under-powered model can be traced to the moment it was allowed to. Deliberately silent in two cases: when the running model is not in the owner's catalog it has no tier to compare against, and a warning derived from an unknown baseline is noise; and it never fires in the flattering direction. This is the guard BUD-11 lacked — the measurement existed, and nothing ever compared it back to the Haiku/low choice. → `scripts/delivery/recommendation.mjs`, `run-session.mjs` _(friction - S)_
- [x] **DLV-30** ◐ Phase-boundary context rotation. `decideContextStrategy` — previously imported by nothing but its own unit test, so `rotateAtTokens: 150000` was dead config — is now called by the runner at every phase boundary (`maybeRotateAtPhaseBoundary`, new), and every evaluation (including "no rotation needed") is emitted as a `context.strategy` event, matching the item's own acceptance bar. `isPhaseBoundary` compares the canonical phase key (discovery/plan/building/reviewing/uat, matching `accumulateUsage`'s existing keys) against `state.context.lastTurnPhase` — a multi-step BUILDING phase or a fix-loop re-entry into the same phase is correctly NOT a boundary (rotating away mid-plan-execution or mid-fix would lose the context the agent needs to finish); occupancy is tracked per-turn (`withContextOccupancy`, new), and the model's context window (`config.mjs` catalog's `contextWindow`, previously never looked up — the mid-phase hard-ceiling branch was permanently dead for lack of it) is now threaded through `turnContext`/`runGuardedTurn` too, so that branch is reachable for the first time as well. When the threshold trips, `performRotation` (already correct — DW-7) fires exactly as it does for the owner `rotate` control, and D2's `driver.reset()` fix is what lets the SAME reused driver instance survive the automatic path, not only the manual one. Verified end-to-end with two new fake-driver tests: a small-occupancy session makes zero rotations and still emits `resume-native` decisions for audit; a session whose DISCOVERY turn clears `rotateAtTokens` auto-rotates at the PLAN boundary, writes the same compaction/snapshot artifacts as a manual rotation, and the fresh PLAN turn still completes to `PLAN_READY`. `pnpm test tests/delivery/` green (925/926 — the one failure is the pre-existing unrelated DLV-34 handoff bug). **Marked ◐ partial, not fully done**: the fresh session after a rotation is NOT yet seeded with `buildContextPackage`'s rendered digest/ledger/pins (`performRotation` already builds this internally but only persists it to `context/snapshots/`, never passes it into the next phase's prompt) — it relies on the same artifact-by-path mechanism every phase already uses (D4), which the runner's own `spec.md`/`plan.md` artifacts and (unrelated to rotation) `pendingGuidance`/owner messages already cover, but not the raw prior-phase exploration digest. Tracked as a scoped follow-up rather than expanded in this change. → `scripts/delivery/run-session.mjs`, `context-policy.mjs`, `tests/delivery/run-session-context.test.ts` _(blocker - M)_
- [x] **DLV-34** ✅ **2026-07-30** — confirmed exactly as filed, and fixed at the resolution layer rather than by threading a variable. `runLoop` builds one driver from `packet.agent` and hands it to every tick; a handoff switched `execution.provider` and wrote the new provider's ref into `state.driver.ref`, so the next phase turn called `claudeDriver.resume(codexRef)` — the old provider's SDK resuming a session id it never created. The verification turn always looked fine because it ran on the correctly-built local instance that `performHandoff` then threw away. New `resolveProviderDriver` picks the driver that owns the current ref, by driver **kind**, so it is right however the provider changed (control, a resumed session, any future path); `runLoop` keeps a per-provider cache so each ref is resumed on the instance that created it. It refuses to swap a driver whose kind is not a real provider name — otherwise any fake-driver test would be one handoff away from instantiating a live SDK driver. Also fixed: `performHandoff` built the new driver bare, silently dropping `forbiddenPaths` and the `sessionDir` the claude driver needs for raw-transcript linkage. Verified red-then-green: with the fix reverted the new regression test fails with `NEEDS_DECISION` instead of `PLAN_READY`. → `scripts/delivery/run-session.mjs`, `tests/delivery/run-session-handoff.test.ts` _(friction - S)_
- [x] **DLV-36** `settingSources: []` added to `buildSessionOptions` — the SDK requires `'project'` in that list to auto-load CLAUDE.md at all, so this is the only way to stop the ~10.2K-token forced load on every one of ~40 model calls a session makes. Replaced with `prompts.mjs`'s new `DOCTRINE_POINTER`, referenced by path (same doctrine as skills) in the phases that shape or execute the change — DISCOVERY, PLAN, BUILDING — and deliberately omitted from REVIEWING/UAT/HANDOFF, which work over already-produced artifacts against a narrower checklist. **Found to be a correctness fix, not only a cost one**, while verifying the claim: `.claude/settings.json`'s `Stop` hook (`check-pm-update.sh`) returns `{decision:"block"}` whenever a session has edited `src/` without touching `ERA Notes/10` — since it fires on *any* Claude Agent SDK session rooted at this cwd, it was forcing an extra, tool-free internal model call on delivery's own BUILDING turns to react to a rule that delivery's own governed PM-trace step (not this interactive hook) is responsible for. This is a plausible contributor to Cost Anatomy §5's "13 runner turns became ~40 internal model calls" finding — Turn 7's "one model call, zero tool uses, 96,535 input tokens" is consistent with a hook-forced re-prompt, though not confirmed as the specific cause. The `PostToolUse` hook `update-atlas.sh` (runs `pnpm atlas`, writing `public/atlas/atlas.json`) was also firing on every BUILDING edit under `src/app|features|components` — an uncontrolled file write the packet's own change tracking never expected. `pnpm test tests/delivery/` green (919/920 — the one failure is the pre-existing unrelated DLV-34 handoff bug); two new tests added (`settingSources` on both modes; `DOCTRINE_POINTER` present in DISCOVERY/PLAN/BUILDING, absent from REVIEW/UAT/HANDOFF). Live measurement via `probe-overhead.mjs` deferred to the owner — it is a real, cost-bearing SDK call, however small. Done 2026-07-28. → `scripts/delivery/drivers/claude.mjs`, `scripts/delivery/prompts.mjs`, `tests/delivery/{drivers-claude,prompts}.test.ts` _(friction - M)_
- [x] **DLV-38** Flight-check forecast is now closed-loop instead of a fixed static guess forever. **Found while investigating the "17,040 vs 2,386,471, ~140x" claim from the original review: it conflated two different, already-separate estimates.** `contextManifest.estimatedTokens` (17,040 for `xdl9`) is `context-assembly.mjs`'s chars/4 sum over six *context-load* files — honestly labeled `"rough chars/4 if every read-by-path input is loaded"`, scoped to DLV-8/D10, not touched here. The actual owner-facing budget forecast is a *separate* field, `packet.flightCheck.recommendation.{estTokens,estCostUsd}` (400,000 tok / $0.17 for `xdl9`'s economy tier) — off by ~6x against the recorded (still D1-corrected-but-real) 2,386,471, not 140x, but still a static per-tier guess (`STATIC_USAGE_BY_TIER`) with no path to improve, because `recommendAgentConfig`'s own `history` parameter — already built, already doing a real median-of-3+-same-tier-samples calculation (`estUsageForTier`) — was called with no `history` argument at both of its two call sites (`getRecommendation` preview, the launch route), making that code permanently dead regardless of fleet size. Fixed by adding `loadRecommendationHistory(ctx)` (new, in `server-routes.mjs`, reusing the existing `listSessionIds`/`readSession` helpers already used for the build-lock and active-session checks) — scans on-disk sessions, keeps only ones that reached **ACCEPTED or SHIPPED** (deliberately excludes CANCELLED/BLOCKED/in-flight: a session killed early by a quota/budget/git-guard stop would skew the median toward "cost before dying," not "cost to actually finish," which is the number the forecast needs), and maps to `{tier, usage}` — wired into both `recommendAgentConfig` call sites. The static per-tier table remains the fallback until 3+ same-tier completed sessions exist (unchanged, correct behavior — `estUsageForTier`'s own pre-existing threshold). Also added, addressing the plan's other explicit D8 ask ("record forecast-vs-actual"): `computeForecastActual(packet, state)`, a small **derived-on-read** field (not new persisted state — both halves, `packet.flightCheck.recommendation` and `state.usage.total`, already exist on disk) returned from `GET /api/delivery/session` as `forecastActual: {tier, estTokens, estCostUsd, actualTokens, actualCostUsd, tokenRatio}`, reusing `budgets.mjs`'s `totalProcessedTokens` for the actual-tokens figure (same v2-aware formula the DLV-37 fix already made correct) rather than writing new math. A DLV-12 finish package doesn't exist yet to write this into, so the API is the honest scope boundary — UI surfacing in `SessionDetail.jsx` deliberately left as a follow-up, not expanded into this change. 5 new tests: 3 in `recommendation.test.ts`-adjacent style already covered by `estUsageForTier`'s pre-existing unit tests (unchanged, still passing), 1 integration test in `server-routes.test.ts` proving 3 fixture ACCEPTED sessions' median (not the static per-tier fallback) reaches a fresh `GET /api/delivery/recommendation` call, and 2 more covering `forecastActual` (present + correctly `null` before any usage is recorded). `pnpm test tests/delivery/` green (941/942 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors. Done 2026-07-29. → `scripts/delivery/server-routes.mjs`, `tests/delivery/server-routes.test.ts` _(friction - S)_

**M3 — Truthful Finish** — every session ends deliberately, honestly, recoverably.

- [x] **DLV-10** ✅ **2026-07-30** — the matrix is real state now, seeded at spec approval (the one moment the AC list is final, after DLV-7's narrowing) with every criterion `unmet`. The rule is neither "believe the agent" nor "ignore the agent" but **the agent may claim, the runner confirms**: a `met` claim survives only if its evidence resolves against something the runner itself owns — a file this session actually changed, a file that exists, or a validation rung the runner recorded as *passing*. "The tests prove it" is evidence exactly when the runner watched the tests pass and worth nothing when it watched them fail. A `met` claimed while validation is red is recorded `failed`, which is the specific lie BUD-11 told ("✅ COMPLETED" over a red typecheck with ACs 6/7/8 silently never done). VALIDATING re-feeds the existing rows as claims on every pass, so an AC that went green on a passing suite is demoted the moment that suite goes red. UAT_READY is blocked while anything is unsatisfied — an additive check on an existing transition, no new state — and the owner either waives (audited, `updatedBy: "owner"`, never available to the agent) or sends it back. **The waiver resumes at UAT_READY, not at the REVIEWING that raised it**: the UAT package was already assembled, and returning to REVIEWING would re-run a review turn *and* a UAT turn — two full-context turns at ~$0.10–0.19 each — to re-produce artifacts already on disk. The reconciliation rule is stated in the prompt, deliberately: an agent that knows an unevidenced `met` will be demoted has a reason to supply the pointer. **Found while wiring it:** `run-session-memory.test.ts`'s fixture claimed `evidence: "tests"` while its validation stub ran typecheck only — the fixture was asserting the exact claim this item exists to reject, and was corrected. → `scripts/delivery/acceptance.mjs`, `run-session.mjs`, `prompts.mjs`, `tests/delivery/acceptance.test.ts`, `tests/delivery/run-session-scope.test.ts` _(blocker - M)_
- [x] **DLV-11** ◐ **PARTIAL 2026-07-29 (D10)** — risk-based validation ladder shipped; the AC-coverage half of the original Cluster C complaint (DLV-10) stays open. **Found while scoping this: delta-vs-baseline at each rung was already fully built** (`validation-baseline.mjs`'s `classifyValidationFailure`, shipped under DLV-5, wired into `handleValidating` since before this session) — per-command `commandDeltas` with a `baseline-equivalent`/`regressed` verdict per rung already existed; nothing to add there. What was actually missing: a lane-defined ladder (VALIDATION_COMMANDS ran unconditionally for every session regardless of lane) and a way for a skipped rung to be anything other than **silently absent** from `results` — the literal DLV-11 complaint, "`whdv` logged `lint (skipped)` / `test (skipped)` under a '✅ COMPLETED' build log," was the *agent's own unverified prose*, not the runner's own record, because the runner's record had no concept of a governed skip at all. New config section `validation.laneLadder` (FAST: typecheck + targeted test, lint skipped; STANDARD/DEEP: unchanged full ladder — typecheck always runs everywhere, it's cheap and correctness-critical enough that skipping it is never worth the saved time), resolved into `packet.lanePolicy.validationLadder` by `resolveLanePolicy` (D9's own function, extended). `runValidationCommands` now records **every** `VALIDATION_COMMANDS` rung explicitly — a lane-ladder skip as `{ok:true, skipped:true, reason:"not in this lane's validation ladder"}` *before* any command runs, and a fail-fast skip (an earlier rung already failed) as the same shape with `reason:"not run — an earlier validation command failed"` (previously: simply absent from `results`, relying on a `renderValidationReportMd` branch that treated absence as `(skipped)` text with no reason). `renderValidationReportMd` now renders a distinct `SKIPPED — <reason>` label, never conflatable with PASS/FAIL. Targeted test mode: FAST's `test` rung runs `pnpm exec vitest related <state.workspace.changedFiles> --passWithNoTests` instead of the full suite — verified live against this real repo before wiring it in (not guessed at): `npx vitest related scripts/delivery/recommendation.mjs` correctly narrowed 955 tests/33 files down to 117 tests/3 files by real dependency graph, and `--passWithNoTests` confirmed exit-0 (not a false failure) for a file with zero related tests, e.g. a docs-only PM edit. "Authorized" is interpreted as the owner's own explicit lane selection at Flight-Check (DLV-2's existing panel, itself reviewed and typed-acknowledged) — not a second, separate typed confirmation on top of it; picking FAST already *is* authorizing FAST's lighter ladder. 9 new tests across `config.test.ts`, `recommendation.test.ts` (ladder resolution + full-ladder fallback), `run-session.test.ts` (`runValidationCommands` lane-skip / targeted-invocation / untargeted-fallback unit tests, plus 2 full `advanceSession` integration tests proving `packet.lanePolicy.validationLadder` reaches `handleValidating` and a pre-D9 packet with no `lanePolicy` still runs everything, unchanged); 4 pre-existing tests updated for the new honest behavior (fail-fast-skipped rungs are now a structured result, not `undefined`). `pnpm test tests/delivery/` green (962/963 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors (fixed 8 transient inference errors at the source — 6 from an under-typed test callback, 2 from `excerpt` becoming optional in the broadened result shape — rather than left to accumulate). **Still open, not attempted here**: DLV-10's AC coverage matrix, and DLV-8's other half — per-lane *context* budgets with priority-ordered dropping of campaign/skill sources (see DLV-8's own line below; genuinely separate work, `context-assembly.mjs`/`server-routes.mjs`'s `buildLaunchContextManifest`, not touched by this change). → `scripts/delivery/config.mjs`, `scripts/delivery/recommendation.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/{config,recommendation,run-session}.test.ts` _(friction - M)_
- [x] **DLV-12** ✅ **2026-07-30** — `writeFinishPackage(reason)` on **every** exit to a terminal, blocked, or paused state: `manifest.json`, `acceptance.json`, `remaining-work.json`, `recovery.md`, `risks.{md,json}`, `summary.md`. The exhaustiveness is the deliverable — the exits nobody instrumented are exactly the ones that left BUD-11's true result (a partial migration, a failing enforcement test, 67 unconverted occurrences) as a paragraph of agent prose under a "✅ COMPLETED" heading. Wired into `blockFrom` (which every phase funnels a failed turn through), both fix-loop-exhausted branches, cancel, shipped, the budget pause, and `parkSessionOnCrash` — the crash path most of all, since it is the likeliest to leave a half-written workspace with no explanation. Two properties carry the weight: **recovery instructions are display-only markdown**, never a script, because the runner performs no git write of any kind, and the file says so in bold; and **ownership is reported, not assumed** — a file this session touched that was *already dirty at launch* is `shared`, and the recovery text refuses to offer `git checkout` for it, offering `git diff` instead, because reverting it would destroy the owner's own uncommitted work. It never throws: a finish package that fails to write must not turn a graceful pause into a crash. Deliberately emits **no** `notification.requested` of its own — every exit that reaches it already notified for the same state change, and a second "your finish package is ready" is precisely the duplicate DLV-16's contract rules out; those notifications carry the package pointer instead. → `scripts/delivery/finish-package.mjs`, `run-session.mjs`, `tests/delivery/finish-package.test.ts` _(blocker - M)_
- [x] **DLV-13** ✅ **2026-07-30** — `GET /api/delivery/salvage` reads the predecessor's own `finish/remaining-work.json` (DLV-12) and returns a pre-filled launch payload; `POST /api/delivery/start` accepts `continuationOf`, narrows the successor's ACs to what is actually left, carries the durable ledger across (not the transcript — the successor inherits what was *learned*, same artifact-first reasoning as fork), records `packet.continuation`, and marks the predecessor superseded only **after** the successor is fully on disk. Superseding is a note about lineage, never a state transition: the predecessor keeps its terminal state, because that is the one record of how it actually ended. The successor goes through the ordinary Flight-Check with a **fresh** budget, lane and model — the previous selection is shown but never pre-filled, since the reason a session needed salvaging is usually that one of those three was wrong. Never triage-refused: the predecessor's finish package is direct evidence the item was not too trivial for the pipeline. **Found by the new tests:** the build lock counted the predecessor itself — which holds it *because* it stopped past the plan gate — making every stopped session permanently unsalvageable; `isBuildLockActive` now takes an exception for exactly that session. Refuses a live runner, a mismatched work item, or a predecessor with no finish package. → `scripts/delivery/server-routes.mjs`, `packet.mjs`, `tests/delivery/server-routes-salvage.test.ts` _(friction - M)_
- [x] **DLV-14** ✅ **2026-07-30** — the PM trace is an exit effect the runner performs, not a step the agent can skip. It could, and did: BUD-11 burned two sessions and left **zero** PM trace, so the command centre had no record the work was ever attempted. ACCEPTED keeps the existing checkbox tick; every other resting state (SHIPPED/CANCELLED/FAILED/BLOCKED/NEEDS_DECISION) appends one dated bullet to the campaign's `1 - Feature State.md` naming the session, its outcome, files changed, ACs satisfied, and the finish-package path. Three properties: **append-only** (it writes under its own heading and never edits a line it did not write, so nothing the owner authored can be clobbered); **drift-guarded** (the item's checklist line is re-hashed, and a drifted line still gets its trace *with the drift called out* — the point is that the attempt becomes visible, so refusing to record it would be the wrong failure); and **idempotent** via both a marker file and a session-id check in the document itself. → `scripts/delivery/server-routes.mjs`, `tests/delivery/server-routes-salvage.test.ts` _(friction - S)_

## Later

**M4 — Operability & Proof.**

- [x] **DLV-15** ✅ **2026-07-30** — a persistent `StatusHeader` answers both questions in two lines, above everything else on the page. "What's happening" resolves the phase, pause, runner-liveness and gate state into one sentence, falling back to the latest *meaningful* event (a curated list — surfacing the whole stream would be noise); "What it needs from you" lists every pending owner action in the second person, with the Q&A ones deep-linked. Both used to require scrolling and inference: the phase was in a stepper, live activity was buried in the Timeline tab, and whether the session wanted anything at all was only discoverable by finding the gate panel. The Q&A tab already existed but was missing from `TAB_LABEL`, so it rendered without a proper label — fixed. → `scripts/pm/src/features/delivery/SessionDetail.jsx` _(annoyance - M)_
- [x] **DLV-16** ✅ **2026-07-30** — the dashboard half. `pnpm pm` now raises a toast for the same `notification.requested` events DLV-22's bridge already pushed to the phone, plus gate transitions, which are the single thing an owner most needs to know about and are not notification events (a gate is a normal, expected stop). Both are deduplicated the way the bridge does it — notifications by event seq, gates on the **transition only** — so a session already parked at a gate does not re-toast on every poll, and a first page load does not produce a storm. Deliberately narrow: it consumes the one event the runner already emits per state change worth interrupting for, rather than inventing a second notion of "important" — which is also why DLV-12's finish package pointedly emits none of its own. `.toast.warn` added to the stylesheet; only `.toast.error` had a style. → `scripts/pm/src/features/delivery/deliveryStore.js`, `scripts/pm/src/styles/components.css` _(friction - S)_
- [x] **DLV-17** ◐ **PARTIAL 2026-07-29 (D12)** — the "gap check at session end" third only. New `findMissingTurnIds(turnCounter, closedTurnIds, promptTurnIds)` in `transcript.mjs` — distinct from the pre-existing `findOrphanedTurnIds` (a prompt file exists but the turn never closed, reconciled on `--resume`): this catches the whdv postmortem's actual failure shape, `turnCounter` reaching 23 with turns 0013–0018/0021 having **no trace at all**, neither a closed entry nor even a prompt file — a gap `findOrphanedTurnIds` cannot see because it can only compare against files that exist. New `runTranscriptGapCheck(sessionDir, state)` in `run-session.mjs` runs at both real terminal transitions (`consumeCancel`→CANCELLED, `consumeAcceptedDecision`→SHIPPED) and always emits a `transcript.gap.checked` event — including the zero-gap case, so the check having run is itself auditable, not only its findings. **Still open, not attempted**: stalled-session detection from the runner heartbeat, and proactive stub records written the moment a turn id is allocated (this only *detects* a gap after the fact by comparing `turnCounter` against what exists on disk — real insurance, but reactive, not the fully preventive "every allocated id gets something on disk immediately" the item also asks for). 2 new integration tests in `run-session.test.ts`: a normal SHIPPED session reports `gapCount:0`; a session with `turnCounter` deliberately bumped past what was actually written (simulating the whdv shape) reports the exact missing ids. `pnpm test tests/delivery/` green; see DLV-40 below for the rest of D12. → `scripts/delivery/transcript.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/run-session.test.ts` _(annoyance - S)_
- [x] **DLV-40** ◐ **PARTIAL 2026-07-29 (D12, rest of the owner's remediation backlog)** — role attribution, color, `parent_tool_use_id`, raw SDK linkage. No subagents exist (`Task` stays banned) — this makes the roles that already exist (Discovery/Planner/Builder/Reviewer/UAT/Handoff-verifier, one per phase) legible instead of invisible, and closes the one gap that would have let a future subagent go untraced.
  - **Role stamping**: `PHASE_ROLES`/`roleForPhase(phase)` (new, `transcript.mjs`) map each of the six turn-producing phases to a role label. Every raw record and turn entry now carries `role` alongside the pre-existing `provider`/`model`/`effort` — previously raw records carried `provider` only, with nothing distinguishing a DISCOVERY record from a REVIEWING one at the record level.
  - **`parent_tool_use_id`**: verified against the installed `@anthropic-ai/claude-agent-sdk` 0.3.207 `sdk.d.ts` before wiring in (not assumed) — `parent_tool_use_id: string | null` is a real top-level field on `SDKAssistantMessage`/`SDKUserMessage`. `mapSdkMessageToRawRecords` now records it as `parentToolUseId` on every record it emits, unconditionally — cheap insurance that a future subagent is traceable by construction, closing the exact gap the forensics found: the mapper never read the field at all, so a subagent's output would have been silently merged into the orchestrator's own shard with no way to tell the two apart after the fact.
  - **`ROLE_COLORS`**: new `scripts/pm/src/features/delivery/roleColors.js`, importing `PHASE_ROLES` from `transcript.mjs` (role *names* have one source; a role this map doesn't recognize is a build-visible bug, not a silent fallback) so color/icon can never drift from the runner's own role vocabulary. Wired into `ConversationView.jsx` (phase-group header + every `TurnCard`'s chip row) and `TimelineView.jsx` (`event.phase`, gracefully falling back to a plain `Chip` for gate-state phases like `SPEC_READY`/`BLOCKED` that aren't a turn-producing role at all). Every consumer renders icon + color + the role's own text label together, by construction — never color-only, the rule `TimelineView` already observed before this existed.
  - **Raw SDK linkage — reference, don't hoard**: new `resolveRawTranscriptPath`/`computeRawTranscriptPointer` in `drivers/claude.mjs`. `state.driver.ref.id` doubles as the Claude Agent SDK session UUID *and* the filename Claude Code's own CLI writes the session's complete raw transcript to, under `~/.claude/projects/<cwd-slug>/<uuid>.jsonl` — confirmed empirically against this machine's real layout before wiring in (not assumed). `withRawTranscriptLinkage` runs at the same two terminal-transition points as the DLV-17 gap check, Claude-only (Codex has no equivalent), and persists `{path, exists, sizeBytes, sha256, resolvedAt}` onto `state.driver.rawTranscript` — a pointer plus integrity check, never a copy. `exists:false` (file aged out, or never existed) is a valid, useful result, not an error.
  - **Not attempted in this pass**: the pinned opt-in export mechanism (redaction pass + de-dup against `transcript/`) — the plan itself frames this as optional, off by default, and it's a genuinely separate feature (write-side, not read-side) from the reference-pointer default this item actually delivers. Surfacing the pointer in the session-detail *UI* (only the state.json field + drivers-level tests exist so far) is also not done. And **`/pm/live` (the separate Next.js surface) is not wired to any of this** — real architectural separation, confirmed before attempting: `src/features/pm-live/` cannot statically import `scripts/delivery/*.mjs` the way `scripts/pm/`'s Preact build already does (different bundler/module resolution entirely; pm-live only ever *references* delivery config in comments, relayed through the bridge as JSON, never as an imported JS module) — extending role/color there needs the bridge to relay per-turn role data it doesn't carry today, a separate, larger effort.
  - 9 new tests: `drivers-claude.test.ts` (`parent_tool_use_id` propagation on both assistant/user messages; `resolveRawTranscriptPath`'s slug format matches this machine's real `~/.claude/projects/` layout; `computeRawTranscriptPointer`'s exists-true/exists-false/no-sessionId cases against a temp `homeDir` test seam); 4 pre-existing `mapSdkMessageToRawRecords` tests updated for the new `parentToolUseId` field. `pnpm test tests/delivery/` green (978/979 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors; `node scripts/pm/build.mjs` succeeds (169,865 B JS, up from 168,381 B). → `scripts/delivery/transcript.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/drivers/claude.mjs`, `scripts/pm/src/features/delivery/{ConversationView,TimelineView,roleColors}.{jsx,js}`, `tests/delivery/{drivers-claude,run-session}.test.ts` _(friction - M)_
- [x] **DLV-18** ✅ **2026-07-30** — `tests/delivery/failure-scenarios.test.ts` drives each governance behaviour against the failure it exists to prevent, every one of which happened at least once in BUD-11's forensics: **quota-hit** (the literal "monthly spend limit" string, asserting *zero* retries), **budget cap-hit** (paused at the boundary with the phase gate preserved underneath per DLV-29, artifacts intact), **config corruption** (last-known-good fallback, `config-invalid` recorded once rather than per tick, session still reaches its gate), **retry storm** (escalates to NEEDS_DECISION at the configured limit, exactly one notification), **provider unreachable at setup**, and **crash recovery** (allocated-but-unclosed turns sealed, idempotently). A final parameterised block asserts DLV-12's exhaustiveness claim directly — every recoverable exit leaves a complete `artifacts/finish/`, and the recovery text still says nothing runs those commands. Two driver-level failure modes added to `fake.mjs` because enumerating them turn-by-turn is impossible or absurd: `throwsEvery` (a retry storm scripted as N identical turns would couple the fixture to `maxAutoRetries`, so a config change would silently turn it into a script-exhaustion test) and `failStartSession`. The "pre-existing red `onEvent` baseline" this item told us to fix first no longer exists — see DLV-48/51; the whole repo is green. → `tests/delivery/failure-scenarios.test.ts`, `scripts/delivery/drivers/fake.mjs` _(friction - M)_
- [x] **DLV-19** ✅ **2026-07-30** — computed from the session directories at request time (`computeFleetMetrics`), no new store and no new persisted field: every input already existed on disk and had simply never been added up. Outcome distribution, owner decisions per session, first-pass validation rate (reached VALIDATING and passed with `fixLoop === 0`), and scope-estimate accuracy (files actually changed ÷ files DISCOVERY estimated, so 1.0x is accurate). **`costPerShippedItem` divides *total fleet spend* by shipped sessions**, not the average of the shipped ones — work that never shipped was still paid for, and averaging only successes would flatter the pipeline exactly where it has been weakest. The panel is meant to be an honest mirror: with today's fleet it renders the cancelled and blocked sessions first. → `scripts/delivery/server-routes.mjs`, `scripts/pm/src/features/delivery/DeliveryHome.jsx` _(annoyance - S)_

## Definition of Done

- **D1** A fake-driver test exists for every M1/M3 behavior (DLV-18 scenarios green on `pnpm test`).
- **D2** No owner non-negotiable violated: no git writes, no `bypassPermissions`, 3 gates in every lane, `agent-registry.mjs` single source of truth.
- **D3** A real session (S-item, FAST lane) runs launch → ACCEPTED with: budget envelope visible, AC matrix green, finish package written, PM trace auto-appended.
- **D4** The next BUD-11-class overrun is impossible by construction: cap-hit pauses gracefully, scope mismatch trips decomposition, spend-limit errors never retry.
