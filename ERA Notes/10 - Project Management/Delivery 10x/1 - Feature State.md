---
created: 2026-07-24
updated: 2026-07-25
type: feature-state
status: living
owner: Elio
tags: [pm/feature-state, tooling/delivery]
---

# Delivery 10x — Feature State

> **What this file is:** the verified current state of the Delivery feature (what exists in code as of 2026-07-24) followed by the ranked pain clusters that this campaign exists to close. Evidence for every pain bullet lives in [5 · Session Postmortem](<5 - Session Postmortem (s-20260722-225601-whdv).md>).

---

## 1. What exists today (verified against code, 2026-07-24)

**Working and proven in the 2026-07-22 session:**

- **State machine + 3 human gates** — `SELECTED → DISCOVERY → SPEC_READY → PLAN_READY → BUILDING → VALIDATING → REVIEWING → UAT_READY → ACCEPTED → SHIPPED` (+ `BLOCKED/NEEDS_DECISION/FAILED/CANCELLED`), pure transition table in `scripts/delivery/state-machine.mjs`. Gates fired correctly; owner answered 4 substantive clarifying questions; spec + plan formally approved.
- **Guardrails held 100%** — zero git mutations (read-only allowlist `scripts/delivery/gitread.mjs` + post-turn HEAD/ref guards), no `bypassPermissions` (`assertNeverBypass` in `scripts/delivery/drivers/claude.mjs`), forbidden paths (`src/components/ui/**`) respected, read-only phases stayed read-only.
- **Artifact-first persistence** — packet/state/events/decisions/transcripts/memory ledger all on disk under `.delivery/sessions/<id>/`; the entire postmortem was reconstructed from files alone. This is the feature's greatest strength — preserve it in every DLV change.
- **DW durable-memory layer** (shipped, see [Delivery Workspace](<../Delivery Workspace/_index.md>)) — full transcript capture, provider-neutral ledger, pause/resume/abort controls (`controls/`), mid-session model/effort switching (`/api/delivery/control`), provider handoff/rotation/fork.
- **Validation subprocess** — killable typecheck/lint/test runner with baseline capture (`validation-baseline.mjs`); fix-loop with `maxFixLoops` cap.
- **Deterministic classifier + agent registry + recommendation engine** — `classify.mjs`, `agent-registry.mjs`, `recommendation.mjs`.

**Campaign baseline gaps (resolved below where stamped):**

- ~~**`scripts/delivery/budgets.mjs`** — global caps only, absent from packet/launch UI and ineffective in the 2026-07-22 run.~~ **Resolved by DLV-1 (2026-07-24):** owner envelope is mandatory at launch, packet-persisted, boundary-enforced, visible in Usage, and resumable only through an audited raise.
- **`scripts/delivery/quota.mjs`** — quota-vs-transient classifier built *because of* the first BUD-11 incident; its pattern list (`session limit`, `usage limit`, `rate limit`, `quota`, `429`) **did not match "You've hit your monthly spend limit"**, so the fatal error was treated as retryable-transient.

---

## 2. Pain clusters (ranked)

**Cluster A — No owner-side governance** *(the campaign's reason to exist)*

- ✅ **Budget governance shipped 2026-07-24** — the owner sets cost/token caps (or types `NO CAP`) at launch; the immutable packet records authorization; warnings are one-shot; hard caps pause between turns with a finish artifact and audited raise-only resume. → DLV-1
- ✅ **Preflight Flight-Check shipped 2026-07-24** — one launch authorization panel now shows the item/available ACs, lane + model/effort fit, required budget, context manifest estimate, capabilities/risk flags, and governed baseline acknowledgments; the server persists the reviewed snapshot into `packet.json`. → DLV-2
- ✅ **Mobile kill-switch shipped 2026-07-25** — the `whdv` postmortem's core finding was "the defect is the absence of owner-side limiters," and until now every limiter required being at the laptop. `/pm/live` (Mobile Command Surface) puts Pause/Abort-turn/Cancel in the owner's pocket — reachable in three taps from anywhere, revoke-only by construction (it can only reduce a running session's authority, never grant more), and routed through the exact same `routeDelivery()` guards the desktop UI uses. Gate approval (spec/plan/uat/blocked) and budget increases stay laptop-only. → DLV-20, DLV-21
- ✅ **Mobile write surface narrowed to zero-regret actions 2026-07-25** — the same-day sibling of the kill-switch. `/pm/live` shipped at 01:20 with tappable checkboxes; within two minutes of the bridge first going live, a tap silently marked a real PM item done (`Budget/4 - Checklist.md`, 11:17:29) with no confirmation, no trace, and no way back — and the board then *hid the row*, because done items are filtered out. Root cause is a category error, not a UI bug: the phone got a **grant** capability (assert that work is finished) on a surface built for **revoke** capabilities and glanceable one-handed taps. Fixed by removing `tick` from the channel entirely — server-side (`REFUSED_TYPES` + the `pm_commands.type` CHECK), because an installed PWA can still issue it from a cached bundle — and repointing a row tap at the delivery flight check, which is guarded (mandatory envelope, dirty-tree/red-baseline refusal) and costs nothing if abandoned. Every remaining bridge write is now journaled with a restorable pre-image and offered as an Undo on the phone. → DLV-23
- 🟠 **Scope inflates silently at SPEC time** — BUD-11 entered as `_(annoyance - S)_` "verify cache timings"; the approved spec contained 8 ACs amounting to a repo-wide 72-occurrence migration program. Nothing compared spec-implied scope against packet effort. → DLV-7
- 🟠 **Model/effort fit never re-checked** — Haiku/low was accepted for what discovery itself measured as a 25-file cross-module refactor; no warning at launch, none after discovery. → DLV-9

**Cluster B — Reliability floor** *(demoted from headline to floor, still required)*

- ✅ **Nullable provider costs no longer crash PM Live 2026-07-25** — provider usage intentionally emits `costUsd: null` when pricing is unavailable, while the delivery card and bridge notification previously called `toFixed()` unconditionally. PM Live now displays an em dash and notifications say “Cost unavailable,” preserving the distinction from a true $0.00. → DLV-27
- 🔴 **Provider spend-limit error misclassified as transient** — `quota.mjs` pattern gap ("monthly spend limit" unmatched) → blind retries → dead `BLOCKED` with `returnTo: BUILDING` and a half-migrated tree. → DLV-4
- 🟠 **Retry storm with no escalation** — 7 consecutive blocked→retry cycles (decisions 0015–0021, 20:36–20:48Z) with no note, no escalation, no notification. → DLV-4
- ✅ **Fixed 2026-07-25 — a budget-exhausted pause landing on the same tick as a manual gate permanently strands the session** — discovered 2026-07-25 during the DLV D3 smoke test (two independent real sessions, both FAST/Haiku/low on DLV-28). Root cause: `enforceBudgetBoundary` (`run-session.mjs`) unconditionally overwrites `state.awaiting` with `{gate:"budget",...}` the instant usage exceeds cap — even when `awaiting` already held a fresh `{gate:"spec"|"plan"|"uat"}` set moments earlier by the phase transition. The `set-budget` resume control only nulls the budget `awaiting`; nothing re-derives the underlying phase gate. Since `SessionDetail.jsx` renders the approve/reject panel only when `state.awaiting` is truthy, and `POST /api/delivery/decision` hard-requires `state.awaiting.gate` to match, the session becomes permanently unresponsive to every UI action and API call — worse than a dead `BLOCKED` (DLV-4's original headline bug) because it shows "runner alive" with an empty gate panel instead of a visible state demanding owner attention. Reproduced twice: `s-20260725-151324-23aw` (PLAN_READY) and `s-20260725-154808-p1im` (UAT_READY), confirmed via a direct `POST /api/delivery/decision` returning `409 gate mismatch: session is awaiting "nothing"` on the first. Both sessions had to be cancelled; DLV-28 could not reach ACCEPTED. → DLV-29
- ✅ **Dirty/red launch baseline governed 2026-07-24** — the server fingerprints the working tree and runs validation before launch; dirty and red states require exact typed acknowledgments, packet ownership excludes pre-existing edits, and later validation passes only on a non-regressing delta with no new failure in a session-touched file. → DLV-5

**Cluster E — Cost anatomy** *(opened 2026-07-25 by the D3 smoke-test forensics — full analysis in [7 · Cost Anatomy](<7 - Cost Anatomy (DLV-28 smoke tests).md>))*

- ✅ **Per-spawn overhead floor shipped 2026-07-25** — `strictMcpConfig`/`skills`/build-mode tool allowlist were all unset, so every spawn loaded 16 skill descriptions, the full built-in toolset, and 16–36 never-invoked MCP tools (Gmail/Supabase/Chrome) on 10 of 13 turns. Measured with the new probe: BUILDING sheds 9,291 tok/spawn (−32.8%), readonly 1,013 (−6.4%). → DLV-33, DLV-35
- ✅ **Usage accounting truth shipped 2026-07-28** — real drivers now extract v2 usage (`cache_creation_input_tokens` was previously extracted nowhere for either provider — only the fake driver's test-only `usageV2` field existed), `state.usage` is v2-shaped end-to-end so `totalProcessedTokens`/budget enforcement finally includes cache-creation, `.delivery/config.json` corrected to the 1-hour cache-write rate (2x input, not 1.25x), and every PM UI spot that summed `input+output` or `input+cachedInput` (session header chip, UsageMeter, UsageView, fleet list) now includes cache-creation too, with a legacy-`cachedInput` fallback so old sessions don't crash or under-report to zero. → DLV-37
- ✅ **Owner guidance reaches every phase, shipped 2026-07-28** — `pendingGuidance` is now read in BUILDING, REVIEWING, UAT-prep, and PLAN (not only DISCOVERY); a UAT rejection note is written as guidance, not ledger-only; PLAN and REVIEWING can now stop and ask too (BUILDING via a new `BLOCKING QUESTION:` sentinel line, since it has no structured output contract to add a field to). Verified with 3 new tests that read the actual assembled prompt files, not just state, to prove guidance text reaches the model. Found and fixed a correctness bug in the process: answering a PLAN-raised question would have stranded the session exactly like DLV-29 (SPEC_READY is a gate, and `consumeQuestionDecision` didn't re-arm it) — caught before shipping, not after. → DLV-32
- ✅ **Rotation no longer blocks the session, shipped 2026-07-28** — every driver now exposes `reset()`, called from `getHandle` right before any `startSession` regardless of why `state.driver.ref` went null (rotate, quota-paused retry, future paths). The regression test that used to hide this (a fresh `planDriver` for the post-rotation turn) now reuses one instance across the rotation; verified red-then-green against the fix. → DLV-31
- ✅ **CLAUDE.md no longer force-loaded per spawn, shipped 2026-07-28** — `settingSources: []` (the SDK requires `'project'` in that list to auto-load CLAUDE.md at all), replaced with a compact `DOCTRINE_POINTER` referenced by path in DISCOVERY/PLAN/BUILDING only. **New finding while verifying it**: this also stops `.claude/settings.json`'s interactive-CLI hooks from firing inside delivery's own automated sessions — the `Stop` hook `check-pm-update.sh` returns `{decision:"block"}` whenever a session edited `src/` without touching `ERA Notes/10`, forcing an extra tool-free internal model call (plausibly part of Cost Anatomy §5's "13 turns → ~40 model calls," not confirmed as the specific cause), and the `PostToolUse` hook `update-atlas.sh` was writing `public/atlas/atlas.json` on every BUILDING edit under src/app|features|components — a file change the packet's own tracking never expected. → DLV-36
- ◐ **Phase-boundary rotation wired 2026-07-28 (context-package seeding still open)** — `decideContextStrategy` is finally called by the runner, at every phase boundary, via new `maybeRotateAtPhaseBoundary`; crossing rotateAtTokens (config default 150,000) now actually starts a fresh provider session instead of resuming one monotonically growing SDK conversation for the whole run. Every evaluation — including "no rotation needed" — is an auditable `context.strategy` event. The mid-phase hard-ceiling branch is also reachable for the first time (the model's `contextWindow` is now looked up and threaded through). Verified end-to-end with new fake-driver tests, including that the driver survives the automatic rotation (D2's fix). Still open: the fresh session isn't seeded with `buildContextPackage`'s rendered digest — it relies on the artifact-by-path mechanism (D4) instead, which covers the substantive spec/plan output but not a mechanical summary of prior exploration. → DLV-30
- ✅ **Hard triage gate at launch, shipped 2026-07-29 (D11)** — an S-effort item with zero risk flags now refuses to launch through the full pipeline without a typed override (`LAUNCH ANYWAY`, same house pattern as `DIRTY TREE`/`RED BASELINE`), showing its own D8-corrected cost forecast in the refusal. Refuses entry only — every gate stays exactly as strict once a session is running, in every lane. The UI (`DeliveryHome.jsx`) imports the same predicate the server enforces, so it can never drift. → DLV-39
- ✅ **Bash escape hatch to the live Supabase project closed, shipped 2026-07-29** — the git guard and the MCP seal (DLV-33) already blocked their routes; a plain Bash command was the third, uncovered one — `psql`, the `supabase` CLI (including runner-prefixed `npx supabase ...`), `curl`/`wget` against a Supabase REST/RPC endpoint, or a script referencing `SUPABASE_SERVICE_ROLE_KEY`/`supabaseAdmin()` all passed `canUseTool`'s Bash screen freely. New `containsDbWriteEscape()` denies all of it unconditionally (no read-only carve-out, unlike git), wired in right after the existing secret-reference check. → DLV-41
- ◐ **Lanes are a real policy bundle, D9 shipped 2026-07-29 (rest of DLV-6 still open)** — FAST/STANDARD/DEEP previously travelled launch form → packet → disk with the runner never reading it (`grep -i lane run-session.mjs` was 0 matches); the only effect was a cosmetic mismatch warning, and the launch UI's budget prefill was hardcoded to `laneDefaults.standard` regardless of the selected lane. New `resolveLanePolicy(lane, config)` resolves the lane into effort-per-phase + budget envelope + a new `maxInternalTurns` cap, recorded as `packet.lanePolicy`; the claude driver maps `maxInternalTurns` onto the SDK's own `Options.maxTurns` — closing the exact mechanism ("13 runner turns became ~40 model calls") Q1's cost forensics flagged as uncapped. An owner's per-phase effort override now merges with the lane default instead of falling back to a stale static default for the untouched phases. Per-lane context budgets and the validation-contract ladder remain open (D10), as does the "route trivial work around the pipeline" triage gate (D11). → DLV-6
- ◐ **Packet-embedding fixed 2026-07-28 (rest of DLV-8 still open)** — the full packet JSON is no longer re-serialized into every prompt. Measured directly against the real accepted session's own `packet.json` (`xdl9`): 15,513 bytes / ~3,878 tokens full-dump, matching the forensics' independently-documented "3,883 of each prompt's 4,047 tokens" almost exactly. `prompts.mjs` now sends a compact header (item, ACs, scope, constraints — 3,346 bytes / ~837 tokens) plus a path to `packet.json`, in all six phase prompts — a 78% reduction, ~3,042 tokens/prompt. Found and fixed a related, previously undocumented bug while doing this: the agent's `cwd` is the repo root, not the session directory, so bare `"artifacts/spec.md"`-style paths never resolved to the session's real artifacts folder — exactly the mechanism behind `whdv`'s own recorded "artifacts/ at repo root (untracked)" finding. Every session-owned artifact path is now anchored under `.delivery/sessions/<id>/` via new `sessionDirRel`/`sessionArtifactPath` helpers. Per-lane context budgets, recorded drops, and the loaded-context manifest remain open. → DLV-8
- ✅ **Flight-check forecast closes the loop, shipped 2026-07-29** — the "17,040 vs 2,386,471, ~140x" claim conflated two separate estimates: the context-load preview (17,040, honestly labeled, DLV-8/D10's scope) and the actual budget forecast (`packet.flightCheck.recommendation.estTokens`, 400,000 for `xdl9`'s economy tier — off ~6x, not 140x). The real defect: that forecast's own `history`-informed median path (`estUsageForTier`, already built) was unreachable because `recommendAgentConfig` was called with no `history` at either call site. New `loadRecommendationHistory()` feeds it same-tier ACCEPTED/SHIPPED sessions' recorded usage (excludes CANCELLED/BLOCKED — a session killed early skews toward "cost before dying," not "cost to finish"). Also added `forecastActual`, derived on read (not new persisted state) from data already on disk, on `GET /api/delivery/session`. → DLV-38
- 🟠 **Provider handoff completes on the wrong driver** — `performHandoff` verifies on a locally-created instance but writes back only its ref, so later turns resume the new provider's ref on the old provider's driver. → DLV-34
- ◐ **Risk-based validation ladder shipped, D10 2026-07-29 (DLV-10's AC coverage matrix still open)** — validation now varies by lane: FAST runs typecheck + a targeted `vitest related <changed files>` test (verified live against this repo before wiring it in), lint skipped; STANDARD/DEEP unchanged (full ladder, every rung). A skipped rung — whether the lane never scheduled it or an earlier rung's failure fast-tripped the rest — is now always a structured, reasoned entry in `validation.json` and a distinct `SKIPPED — <reason>` line in `validation-report.md`, never simply absent the way it was before. Delta-vs-baseline per rung turned out to already be fully built (DLV-5's `classifyValidationFailure`, pre-existing) — nothing to add there. → DLV-11
- ◐ **Lanes are a real policy bundle** (see above) — same D9/D10 pair. → DLV-6

**Cluster C — Untruthful finish**

- 🟡 **Progress claims contradicted by validation evidence — the runner's own silence is fixed (D10 2026-07-29), the agent's free-text narrative is not** — build log full of "✅ COMPLETED / Perfect!" while the same period's `validation-report.md` shows ~48 typecheck syntax errors in exactly those files. The runner-level half is closed: a skipped validation rung can no longer be simply absent from the record (see DLV-11 above). Still open: nothing forces the agent's own build-log prose to agree with that record, and there is still no first-class per-AC status. → DLV-10
- 🟠 **No honest partial outcome** — the session's real result ("Budget module migrated, 67 occurrences remain, enforcement test will fail the build if committed") exists only inside the agent's build log; state says `BLOCKED`, ACs have no tracked status, no remaining-work package, no revert instructions. → DLV-12
- 🟠 **PM trace skippable** — BUD-11 still `- [ ]` in [Budget/4](<../Budget/4 - Checklist.md>); AC6's doc never written; the writeback is an agent step, not a state-machine exit effect. → DLV-14
- ◐ **Transcript gaps are now detected, D12 2026-07-29 (proactive stub records + watchdog still open)** — turns jumping 0012→0019 (`t-0013…t-0018`, `t-0021` missing, the whdv shape) is now caught: `findMissingTurnIds` compares `state.turnCounter` against what actually exists on disk (distinct from the pre-existing `findOrphanedTurnIds`, which can only see a turn that at least got a prompt file written). A `transcript.gap.checked` event fires at every SHIPPED/CANCELLED, always — including "zero gaps" — so the check having run is itself part of the record. Detection only; nothing yet writes a stub the instant a turn id is allocated, and there's still no runner-heartbeat stalled-session watchdog. → DLV-17
- ◐ **Roles are now legible, colored, and traceable, D12 2026-07-29** — Discovery/Planner/Builder/Reviewer/UAT/Handoff-verifier (one per phase, no subagents) now stamp every transcript record and turn entry with `role`, render as icon+color+label in ConversationView/TimelineView (never color-only), and `parent_tool_use_id` (verified against the real installed SDK's `sdk.d.ts` first) is recorded on every raw record — cheap insurance against a future subagent going untraced. Raw SDK linkage is a pointer-plus-hash (`state.driver.rawTranscript`), never a copy, confirmed against this machine's real `~/.claude/projects/` layout before wiring in. `/pm/live` isn't wired to any of this (real architectural separation — it can't statically import `scripts/delivery/*.mjs` the way the Preact PM UI does); the opt-in pinned-export mechanism and session-detail UI surfacing of the raw pointer are also not built. → DLV-40

**Cluster D — Operability**

- ✅ **Push notifications shipped 2026-07-25 (web-push half)** — gate-waiting, budget events, terminal states, and runner death now push to the owner's phone and deep-link into `/pm/live`, consuming the `notification.requested` event that DLV-1 emitted but nothing read until now. Dashboard-toast-on-`pnpm pm`-itself is still open. → DLV-22 (DLV-16 remainder)
- 🟡 **No fleet truth** — 8 sessions on disk: 7 CANCELLED, 1 BLOCKED, **0 SHIPPED**; nothing surfaces outcome distribution, cost per shipped item, or intervention rate. → DLV-19
- 🟡 **Session page doesn't answer "what's happening / what do you need from me"** at a glance. → DLV-15

---

## 3. Fleet snapshot (2026-07-24; reconciled 2026-07-28)

| Session | Outcome |
|---|---|
| `s-20260712-150339-7kw8`, `s-20260712-204625-4qym`, `s-20260715-214421-hvfk` (BUD-11 #1, ~3M tokens), `s-20260722-203135-cv12`, `s-20260722-205308-8sgn`, `s-20260722-221533-wous`, `s3-accept-codex-1783968103139` (Codex acceptance fixture, not a real run) | CANCELLED |
| `s-20260722-225601-whdv` (BUD-11 #2, $2.91) | BLOCKED (later cancelled) |
| `s-20260725-151324-23aw`, `s-20260725-154808-p1im` (DLV-28 D3 smoke test, 2026-07-25, stranded by DLV-29) | CANCELLED |
| `s-20260725-181118-xdl9` (DLV-28 D3 smoke test retry, 2026-07-25, DLV-29 fix verification) | **ACCEPTED** (`awaiting: {gate:"shipped"}`, writeback done — parked at the owner-only SHIPPED gate, not yet marked) |
| **Real sessions on disk** | **10** (11 dirs minus the Codex fixture) |
| **Reached ACCEPTED** | **1** |
| **SHIPPED** | **0** — `ACCEPTED` is not terminal; `SHIPPED` requires an explicit owner `decision.shipped`. `xdl9` is still one owner action away. |

The same checklist item (BUD-11) has now consumed two failed sessions. That is the strongest single argument for M1+M2: governance and right-sizing, not more mechanics. Verified directly against every `state.json` on disk 2026-07-28 (prior snapshot conflated ACCEPTED with SHIPPED and undercounted the fleet by the two later DLV-28 reruns).

---

## 4. Done

- ✅ 2026-07-30 (`scripts/delivery/{scope,acceptance,finish-package,context-budget}.mjs` — all new — plus `run-session.mjs`, `recommendation.mjs`, `server-routes.mjs`, `prompts.mjs`, `config.mjs`, `packet.mjs`, `memory.mjs`, `validation-baseline.mjs`, `drivers/fake.mjs`, `scripts/pm/src/features/delivery/{SessionDetail.jsx,DeliveryHome.jsx,deliveryStore.js}`, `tests/delivery/{scope,acceptance,finish-package,run-session-scope,run-session-merged,server-routes-salvage,failure-scenarios}.test.ts`) **DLV-7, 8, 9, 10, 12, 13, 14, 15, 16, 18, 19, 34, 56, 62** — the whole remaining M2/M3/M4 backlog in one pass, closing every open item in the campaign except the lint burn-down. M2: the scope contract (measured estimate → runner-derived size class → decomposition proposal at the SPEC gate → post-PLAN scope lock), the post-discovery model-fit guard, per-lane/per-phase context budgets with recorded drops, and the handoff-driver fix. M3: the AC coverage matrix ("the agent may claim, the runner confirms" — a `met` claimed over red validation is recorded FAILED), the finish package on **every** terminal/blocked/paused exit, salvage into a right-sized successor, and the PM trace as a runner-performed exit effect rather than an agent step it can skip. M4: the session status header, dashboard toasts, the failure-injection suite, and the honest fleet-metrics panel. Plus **DLV-62**, on the owner's explicit (a)+(b) authorization: FAST merges DISCOVERY+PLAN into one turn for a single-file item — 4 turns to UAT instead of 5 — with all three approval gates intact, since (c) was not authorized and the standing "3 gates always" rule is unamended. Five defects were found *by the new tests* and fixed: the build lock made every stopped session permanently unsalvageable; three recycle-bin `${module.id}` references silently resolved to Node's global `NodeModule`; `priorSource` could label an entirely static forecast as "measured"; and a fixture claimed the test suite as evidence while never running it. Repo: **1403/1403 tests green (70 files), 0 typecheck errors**; lint 672 → 636 errors (see DLV-52 — the two large rule classes remain).
- ✅ 2026-07-30 (`scripts/delivery/prompts.mjs`, `scripts/delivery/usage.mjs`, `scripts/delivery/drivers/claude.mjs`, `tests/delivery/{prompts,usage}.test.ts`) **DLV-60, DLV-61** — the two cost findings left after the DLV-53..59 batch, both from the same session's turn records. **DLV-60:** DLV-45 narrowed what the runner *sends*; nothing told the agent how to *read*. Given an item ending in `MobileExpenseForm.tsx:1144`, DISCOVERY's first code read was `Read {file_path, limit: 2000}` with **no offset** — 69,204 bytes of a 3,099-line file — and its *next* call was `Read {offset: 1140}`, i.e. it knew where to look and read the whole file anyway. Because tool output is re-sent on every internal turn that follows, that ~18K-token read was charged 6x across the turn's 7 model calls: **~90K of its 147,952-token occupancy (74% of a 200K window) to change one character**. `renderReadWindowGuidance` now derives a bounded window (`offset: line-40, limit: 120`) from the item's own pointer and states the re-send economics; wired into DISCOVERY **and** BUILDING (which re-reads the file it is about to edit, at `medium` effort). Applied in **every lane**, unlike DLV-45's FAST-only narrowing — that traded context for speed, while reading 3,099 lines to change one is never the better choice. Cost: **+380 tokens per turn** against ~96K saved on the measured turn. **DLV-61:** cache *writes* were **231,567 tokens = $0.4631 = 87.4%** of the $0.5302 session (reads: 4.5%, output 8.1%), and nothing recorded which TTL they were bought at — the difference between a correct bill and a 60% over-estimate. Deduplicated per DLV-47, the raw transcript says `ephemeral_1h` 231,567 / `ephemeral_5m` **0**: 100% 1-hour, so `cacheWritePerMTok: 2` is right and the spend is real. Now recorded per turn (`extractCacheCreationTtl`), closing the diagnostic DLV-37 deferred. Kept **out** of `UsageTokensV2` on purpose — accumulators read named keys and never spread, so it can never reach a cap comparison (the DLV-43 NaN lesson, same file). Repo: **1276/1276 tests green, 0 typecheck errors, lint clean**.
- ✅ 2026-07-30 (`scripts/delivery/state-machine.mjs`, `scripts/delivery/packet.mjs`, `scripts/delivery/prompts.mjs`, `scripts/delivery/classify.mjs`, `scripts/delivery/recommendation.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/server-routes.mjs`, `tests/delivery/{state-machine,packet,prompts,classify,run-session,recommendation,server-routes}.test.ts`) **DLV-53, DLV-54, DLV-55, DLV-57, DLV-59** — the five defects the BUD-14 re-run surfaced at its own gates. Plan rejection returns to SPEC_READY with the gate re-armed instead of to DISCOVERY (it also cleared `awaiting` to null, so fixing only the destination would have swapped a wrong phase for a dead one). `packet.mode: "uat"` — a write-only field with zero readers whose value read as a phase name, and which the agent duly obeyed — is now `deliveryMode: "uat-gated"`, guarded by a test that no packet string field holds a bare phase name. The plan step budget is stated to the planner at generation time rather than warned about after the money is spent, and the warning now rides on the plan gate itself. Economy `plan` effort medium → low (a medium PLAN turn cost $0.1857 to decompose a one-line edit). And the triage gate finally refuses what it was written for: it reads the Agent Catalog's own blocking declarations — `frontend-impl`/`backend-impl` are declared neither blocking nor advisory because they are routing signals — plus the single-file signal DLV-42 made knowable, while keeping the original zero-flag rule that nothing else can replace. Repo: **1263/1263 tests green, 0 typecheck errors, lint clean**.
- ✅ 2026-07-30 (`scripts/delivery/drivers/claude.mjs`, `scripts/delivery/drivers/driver.mjs`, `scripts/delivery/quota.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/drivers-claude.test.ts`, `tests/delivery/run-session.test.ts`) **DLV-44** — the 07-29 crash loop, root-caused by probe rather than inference. `runTurn` flipped `ref.established` *after* its message loop, but the SDK throws from *inside* it on a non-success result, so the flag was never set exactly when it mattered; `tickOnce` reloads state per tick, so every retry re-sent `sessionId` for a session already on disk. Captured stderr the runner never surfaced: `Error: Session ID … is already in use.` → exit 1 in ~400 ms, zero agent activity, deterministic. Resume-after-max-turns is healthy (probed: clean `result/success`, $0.0009) — it was never reached. This bricked **any** turn failing after SDK init. Also gave `error_max_turns` a non-retryable `max-turns` kind that escalates to an owner decision instead of burning two doomed retries.
- ✅ 2026-07-30 (`scripts/delivery/run-session.mjs`, `scripts/delivery/usage.mjs`, `scripts/delivery/drivers/{claude,codex,fake,driver}.mjs`, `tests/delivery/{drivers-claude,run-session,usage}.test.ts`) **DLV-43** — failed turns now bank their spend. Drivers tag the raised error with observed usage; `runGuardedTurn` sums it across every retry attempt; `blockFrom` — the one choke point all phases funnel failures through — writes it into `state.usage` under the same per-phase key a success would use, so `enforceBudgetBoundary` finally sees spend from a phase that fails. Latent bug found en route: `addUsageV2` over a pre-DLV-37 legacy total produced **NaN**, and `NaN > cap` is false — a resumed old session's caps were silently dead for life.
- ✅ 2026-07-30 (`scripts/delivery/classify.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/delivery/recommendation.mjs`, `tests/delivery/{classify,recommendation,server-routes}.test.ts`) **DLV-42** — the triage gate's false positive, and a deeper mis-sizing behind it. `computeScopeHints` discarded BUD-14's own `→ …MobileExpenseForm.tsx:1144` pointer and substituted all six Budget campaign globs, so the "≥4 globs, +2" complexity rule fired for *every* Budget item ever launched; with the bare-`amount` money false positive (+1) a one-token chip edit scored 3 → premium → **DEEP**. Now scores **0 → economy → FAST**.
- ✅ 2026-07-30 (`scripts/delivery/run-session.mjs`, `scripts/delivery/prompts.mjs`, `scripts/delivery/config.mjs`, `tests/delivery/{prompts,run-session,recommendation,server-routes}.test.ts`) **DLV-45** — FAST made genuinely fast. It differed from STANDARD only in its turn cap, never in what it asked the agent to read: 4 campaign docs + every attached skill + CLAUDE.md ≈ 8 tool calls and ~33,151 tokens of doctrine before the target line, against `maxInternalTurns: 8` — **the phase was structurally unable to finish**. FAST now drops campaign docs, keeps only risk-flag skills, and drops the doctrine pointer in DISCOVERY/PLAN while always keeping it in BUILDING; cap 8 → 12. STANDARD/DEEP untouched.
- ✅ 2026-07-30 (`eslint.config.mjs`) **DLV-46** — `pnpm lint` was OOM-ing, not failing: flat config ignored no build output, so a bare `eslint` type-aware-linted `.next`'s ~5,130 emitted files → heap limit, exit 134, 452 s, every run. Because the launch preflight runs the full ladder, **every delivery session had a red baseline on lint** for a reason unrelated to the code, and delta-validation had a crash dump instead of comparable diagnostics. Now 14 s.
- ✅ 2026-07-30 (`scripts/delivery/smoke-launch.mjs`, `ERA Notes/10 - Project Management/Delivery 10x/7 - Cost Anatomy (DLV-28 smoke tests).md`) **DLV-47, DLV-49** — the raw SDK jsonl repeats each assistant record (12 usage-bearing records / 5 unique `message.id`s), so this campaign's own "actual" cost figures were inflated ~2.08x. Deduplicated, recorded usage matches raw **exactly** — post-DLV-37 fidelity is 100%, not the 1.9x under-report in Cost Anatomy §4, which is now corrected in place. `smoke-launch.mjs` gives the whole launch/inspect/reconcile loop a CLI, and deliberately cannot author a gate decision.
- ✅ 2026-07-30 (`scripts/delivery/{budgets,config,packet,recommendation,validation-baseline,probe-overhead}.mjs`, `scripts/delivery/drivers/codex.mjs`, `tests/delivery/run-session-handoff.test.ts`, `src/lib/schedule/expandOccurrences.test.ts`) **DLV-48, DLV-50, DLV-51** — baseline made honest. Repo typecheck **11 → 0 errors** (all JSDoc typedefs that under-described real signatures, fixed at source not cast away in tests), delivery lint clean (two `eslint-disable` directives that silenced nothing), and the last two red tests were both **mislabelled non-bugs**: the "pre-existing DLV-34 handoff failure" was a DLV-4 escalation collision asserted against a state name rather than its contract, and the Schedule flexible-placement guard demanded a source string from a view that correctly delegates to `expandOccurrencesInRange`. Whole repo now **1253/1253 green**.
- ✅ 2026-07-28 (`scripts/delivery/run-session.mjs`, `scripts/delivery/prompts.mjs`, `scripts/delivery/memory.mjs`, `tests/delivery/{run-session-memory,memory,prompts}.test.ts`) **DLV-32** — owner guidance reaches every phase, and PLAN/REVIEWING/BUILDING can stop and ask for the first time (previously only DISCOVERY could). Read side: `pendingGuidance` is now merged into `ownerMessages` in `handleBuilding`, `handleReviewing`, the UAT-prep sub-turn inside `handleReviewing` (which previously hardcoded `ownerMessages:[]` and didn't even drain fresh `messages/` for its own second turn), and `consumeSpecDecision` (PLAN). Write side: `consumeUatDecision`'s reject branch now writes the note as guidance (`Owner rejected UAT: ...`), not ledger-only. Raise side: `PLAN_OUTPUT_SCHEMA` gained `openQuestions` — deliberately **optional**, not required like SPEC's, because making it required would have broken every pre-existing plan-turn fixture across 7 test files (confirmed the hard way: making it required first caused 23 test failures, all the same shape — `parsePlanOutput` throwing on a missing field every old fixture never had); `parsePlanOutput` now defaults a missing field to `[]` instead. `memory.mjs` gained a matching `applyPlan` extension (its own logic, not a reuse of `applySpec` — that function also seeds `ledger.requirements` from `acceptanceCriteria`, which a plan object doesn't have, and would have silently wiped whatever DISCOVERY already seeded). REVIEWING's schema was already permissive (`{type:"object"}`), so it only needed a prompt instruction. BUILDING has no structured output contract at all (free text appended verbatim to `build-log.md`) — retrofitting a real JSON schema onto it was judged too invasive for this change, so it reuses this repo's own established sentinel-line pattern instead (REVIEWING's "VERDICT: PASS" line, same file) — a required-format `BLOCKING QUESTION: <text>` first line the runner greps for, exported as `BLOCKING_QUESTION_MARKER` from `prompts.mjs` so run-session.mjs never hand-copies the literal (DLV-26's own drift lesson). **A real bug was found and fixed while building this, before it shipped**: `SPEC_READY` is a GATE state, not a runner-dispatched one — answering a question raised during PLAN (`returnTo:"SPEC_READY"`) with the pre-existing `awaiting:null` would have stranded the session with no visible gate panel, the exact DLV-29 failure shape, reproduced by this session's own new code and caught by writing the regression test for it. Fixed by making `consumeQuestionDecision` re-arm the gate exactly like `consumeBlockedDecision` already does (`GATES[returnTo]` lookup) — a no-op for DISCOVERY's pre-existing question flow (not a gate state), so nothing else changed behavior. A second, smaller gap was found the same way: `consumeSpecDecision`'s own `ownerMessages` draining never read `pendingGuidance` either — needed once PLAN could raise its own question, since the answer must reach the *retried* PLAN turn. Verified with 3 new integration tests that read the literal `transcript/prompts/*.md` files the runner actually sent to the model (not just `state.pendingGuidance`, which would only prove the field got cleared, not that the model ever saw it): PLAN raises a question → gate re-arms → answer appears in the retried PLAN prompt; BUILDING's marker sentinel raises a question without touching `build-log.md` or advancing `build.stepIndex` → answer appears in the retried BUILDING prompt; a UAT rejection note appears in the fix-loop BUILDING prompt — DLV-32's own flagship complaint, "owner-visible as 'I answered and it ignored me'," reproduced and closed end-to-end. `pnpm test tests/delivery/` green (930/931 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set.
- ◐ 2026-07-28 (`scripts/delivery/run-session.mjs`, `scripts/delivery/context-policy.mjs` (no code change — finally imported), `tests/delivery/run-session-context.test.ts`) **DLV-30 (partial — trigger + survival, not context-package seeding)** — wired `decideContextStrategy` into the runner for the first time since it shipped. New `maybeRotateAtPhaseBoundary(sessionDir, state, packet, deliveryConfig, phase)` runs once at the top of every phase-producing handler (`handleDiscovery`, `consumeSpecDecision`/PLAN, `handleBuilding`, `handleReviewing`, and the UAT-prep block inside `handleReviewing`), computing `isPhaseBoundary` by comparing a canonical phase key (discovery/plan/building/reviewing/uat — the same keys `accumulateUsage` already uses) against a new `state.context.lastTurnPhase`, and `occupancyTokens` from a new `state.context.lastOccupancyTokens`, both set by a new `withContextOccupancy` helper called alongside every `accumulateUsage` site. A second BUILDING step, or a fix-loop re-entry into the same phase, correctly does not re-trigger (`lastPhase === phase`) — rotating mid-plan-execution or mid-fix would strand the agent without the context it needs to finish. Also fixed a second dead-config bug found while wiring this: `sealTurn`'s `computeOccupancy(usageV2, null)` hardcoded `windowTokens` to `null` unconditionally, permanently disabling `decideContextStrategy`'s mid-phase hard-ceiling branch (it requires a known window size) even though the model catalog (`config.mjs` `ModelEntry.contextWindow`) already carries this — `turnContext` now looks it up via `getModelInfo` and threads it through `runGuardedTurn`. Every evaluation, including "resume-native, no action needed," is emitted as a new `context.strategy` event for audit — the item's own acceptance bar. When rotation fires, the existing `performRotation` (DW-7, unmodified) runs exactly as it does for the owner `rotate` control, and D2's `driver.reset()` is what lets the same reused driver instance survive it automatically, not only manually. Verified with two new fake-driver tests in `run-session-context.test.ts`: a small-occupancy DISCOVERY→PLAN session makes zero rotations while still recording `resume-native` decisions at the boundary; a DISCOVERY turn scripted to clear the 150,000-token default threshold auto-rotates exactly at the PLAN boundary, writes the same compaction/snapshot artifacts a manual rotation would, and the fresh PLAN turn still completes to `PLAN_READY` — proving the automatic path and D2's driver-survival fix compose correctly. `pnpm test tests/delivery/` green (925/926 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, nothing new. **Not done**: seeding the fresh post-rotation session with `buildContextPackage`'s rendered digest/ledger/pins — `performRotation` already builds this internally (`pkg`) but only persists it to `context/snapshots/`, never passes `pkg.renderedMd` into the next phase's prompt. The fresh session instead relies on the artifact-by-path mechanism (D4/DLV-8) — real coverage for the substantive spec/plan output, not for a mechanical summary of the prior phase's raw exploration. Scoped out rather than folded in, to keep this change reviewable; a natural DLV-30b if the owner wants it closed.
- ◐ 2026-07-28 (`scripts/delivery/prompts.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/prompts.test.ts`) **DLV-8 (partial — the packet-embedding sub-problem only)** — stopped re-serializing the full packet JSON into every prompt. New `packetHeader(packet)` sends only what every phase actually needs inline (item, acceptance criteria, scope hints, constraints) plus a path to the already-on-disk `packet.json` for anything else (capabilities, `flightCheck`, budget, workspace baseline) — same artifact-first doctrine already used for skills and prior-phase artifacts, just extended to the packet itself. Measured directly against `xdl9`'s real `packet.json`: full dump 15,513 bytes (~3,878 tok) vs new header 3,346 bytes (~837 tok) — a 78% reduction, ~3,042 tokens saved on every one of the six phase prompts, not merely the three DOCTRINE_POINTER covers. The 3,878-token figure independently matches the forensics' documented "3,883 of each prompt's 4,047 tokens" for a different session almost exactly, confirming both measurements are looking at the same real cost. **Found and fixed a second, previously undocumented bug while doing this**: every phase prompt told the agent to read/write bare paths like `"artifacts/spec.md"`, but the agent's SDK `cwd` is always the repo root (`main()`'s `sessionDir = join(REPO_ROOT, ".delivery", "sessions", sessionId)`, confirmed against the SDK's own `cwd` doc: "current working directory for the session"), never the session directory — so those paths never resolved to where the runner's own `writeArtifactJson`/`writeArtifactText` actually put the files. Verified this is real by grepping the real `whdv` session's transcript: the agent's own `Edit`/`Write`/`Read` calls target bare `artifacts/build-log.md`, and the session's own postmortem independently records a stray `artifacts/` directory "at repo root (untracked)" — the same bug, from the outside, previously attributed only to sloppy agent behavior rather than a systemic path-resolution defect. Fake-driver tests never caught it because the fake driver performs no real file I/O. Fixed with new `sessionDirRel(packet)`/`sessionArtifactPath(packet, relPath)` helpers, applied to every artifact reference across all six prompt templates (spec.md, plan.md, build-log.md, review-self.md, uat/**, packet.json) and the `handleUat` caller that used to hardcode the same bare paths. `pnpm test tests/delivery/` green (923/924 — the one failure is the pre-existing unrelated DLV-34 handoff bug); 5 new tests lock in both fixes (packet fields correctly excluded/included, every artifact path session-anchored, explicit overrides still win). Per-lane context budgets, recorded drops, and the loaded-context manifest (DLV-8's other requirements) remain open — tracked separately as D10 in the owner's remediation backlog.
- ✅ 2026-07-28 (`scripts/delivery/drivers/claude.mjs`, `scripts/delivery/prompts.mjs`, `tests/delivery/{drivers-claude,prompts}.test.ts`) **DLV-36** — `settingSources: []` stops the SDK auto-loading CLAUDE.md (and all filesystem settings) on every one of the ~40 model calls a session makes; house-rule awareness preserved via a new `DOCTRINE_POINTER` in `prompts.mjs`, referenced by path exactly like the existing skill-path doctrine, in the three phases that actually shape or execute the change (DISCOVERY, PLAN, BUILDING) and deliberately not the three that only review already-produced artifacts (REVIEWING, UAT, HANDOFF). While verifying the plan's claim that this "also stops PostToolUse hooks writing files inside a git-guarded session," read `.claude/settings.json` directly and found it's a real, previously-undocumented mechanism: the project's `Stop` hook (`check-pm-update.sh`, Hard Rule #25 enforcement) fires on *any* Claude Agent SDK session rooted at this repo, not only interactive ones — including delivery's own BUILDING turns — and its `{decision:"block"}` response forces an extra, tool-free internal model call whenever a session has touched `src/` without touching `ERA Notes/10`, reacting to a rule that delivery's own (still-unbuilt, DLV-14) governed PM-trace step is supposed to own instead. Plausibly connects to Cost Anatomy §5's unexplained "13 runner turns became ~40 internal model calls" and specifically Turn 7's "one model call, zero tool uses, 96,535 input tokens" — consistent with a hook-forced re-prompt, though not confirmed as *the* cause without a live A/B session. Also found: the `PostToolUse` hook `update-atlas.sh` runs `pnpm atlas` (writing `public/atlas/atlas.json`) on every BUILDING edit under `src/app|features|components` — an uncontrolled file write outside the packet's own change tracking, now also stopped. `pnpm test tests/delivery/` green (919/920 — the one failure is the pre-existing unrelated DLV-34 handoff bug). Live measurement via `probe-overhead.mjs` (a real, cost-bearing SDK call) deferred to the owner, same stance as DLV-37/DLV-31.
- ✅ 2026-07-28 (`scripts/delivery/drivers/{claude,codex,fake,driver}.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/run-session-context.test.ts`) **DLV-31** — drivers survive a rotation. Root cause: `runLoop` creates one driver instance per process and reuses it for every tick, but `startSession` refuses to run twice on the same instance (a real invariant — kept). Rotation and the quota-paused retry both null `state.driver.ref` to signal "start fresh next time," but neither ever told the *driver instance itself* that its previous logical session had ended, so the next `getHandle` call's `startSession` threw "session already started" forever after — every rotated or quota-recovered session was permanently stuck. Fix: a new idempotent `reset()` method on every driver (claude/codex/fake), called from `getHandle` unconditionally right before any `startSession` — one choke point that covers rotate, quota-retry, and any future ref-nulling path, rather than patching each call site. The existing rotation test only proved rotation *worked* because it built a brand-new `planDriver` for the post-rotation turn instead of reusing the original — a fresh instance's `started` flag is always false, so it could never have caught this. Rewrote it to reuse one driver across DISCOVERY → rotate → PLAN, and verified the test actually catches the regression (temporarily disabled the `reset()` call: session went `BLOCKED` instead of reaching `PLAN_READY`; restored, green again). `pnpm test tests/delivery/` green (917/918 — the one failure is the pre-existing unrelated DLV-34 handoff bug).
- ✅ 2026-07-28 (`scripts/delivery/usage.mjs`, `scripts/delivery/drivers/claude.mjs`, `scripts/delivery/drivers/codex.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/delivery/budgets.mjs`, `.delivery/config.json`, `scripts/pm/src/features/delivery/{SessionDetail,UsageView,DeliveryHome}.jsx`, `tests/delivery/{run-session,drivers-codex}.test.ts`) **DLV-37** — usage accounting truth, first item of a full delivery-fleet review (owner questions: cost-vs-forecast, FAST lane reality, guardrail/escalation confidence, caching discipline, session recording completeness). Root cause was structural, not just a missing rollup field: neither real driver ever extracted v2 usage at all — `interpretResultMessage` (Claude) and the codex `runTurn` only ever called the v1 `normalizeUsage`, so `cache_creation_input_tokens` was extracted nowhere for a real session; `result.usageV2` was always `undefined` and `run-session.mjs` silently fell back to a zeroed-`cacheCreation` shape on every single turn. Fixed by adding `extractUsageV2`/wiring `normalizeUsageV2` into both real drivers, rewriting `accumulateUsage`/`state.usage` onto the v2 shape (`addUsageV2`, reusing `usage.mjs`'s existing correct math rather than new code), and making `totalProcessedTokens` — the figure every budget cap is checked against — include cache-creation with a legacy-`cachedInput` fallback so old on-disk sessions don't crash. Also fixed: `.delivery/config.json` `cacheWritePerMTok` was priced at the 5-minute rate (1.25x input) for all three models while Claude Code bills the 1-hour rate it always uses (2x) — 37.5% low on the bucket that carried 86.6% of one phase's real cost; and four separate PM UI spots (`SessionDetail.jsx` header chip + `UsageMeter`, `UsageView.jsx` processed-token calc + pre-DW-1 fallback, `DeliveryHome.jsx` fleet list) were each independently summing `input+output` or `input+cachedInput`, none including cache-creation — "three token numbers exist and none is true" is now one true number in four places. `pnpm test tests/delivery/` green (917/918; the one failure is the pre-existing unrelated DLV-34 handoff bug, confirmed present before this change). Live re-verification against a real session's raw SDK usage (the acceptance bar this fix sets for itself) is deferred to the owner — launching a real session is cost-bearing and the owner's call, not mine to spend without asking.
- ✅ 2026-07-25 (`scripts/delivery/drivers/claude.mjs`, `scripts/delivery/probe-overhead.mjs`, `tests/delivery/drivers-claude.test.ts`) **DLV-33, DLV-35** — per-spawn overhead floor, found by asking why a one-line doc edit cost $2.40. Three SDK options were unset and every default is "load everything": `strictMcpConfig` (16–36 never-invoked Gmail/Supabase/Chrome tools attached on 10 of 13 turns — `options.tools` never filtered them, it is the built-in allowlist only), `skills` (all 16 repo skill descriptions injected into every system prompt, though prompts already reference skills by path), and no build-mode tool allowlist (BUILDING advertised every built-in the CLI ships). A new probe script measures fixed per-spawn context with one `maxTurns:1` query for a fraction of a cent, so this class of change is now A/B-measurable without a real session. Measured: BUILDING 28,331 → 19,040 tok/spawn (−32.8%), readonly 15,795 → 14,782 (−6.4%) ≈ 8–9% of a session, paid on every one of ~40 model calls. The readonly delta also confirmed `skills: []` is honoured as "none" rather than coerced to "omitted". Probe limitation recorded honestly in doc 7 §6: it did not reproduce the MCP attachment (connector race), so that portion is a correctness guarantee with an unverified saving.
- ✅ 2026-07-29 (`scripts/delivery/server-routes.mjs`, `tests/delivery/server-routes.test.ts`) **DLV-38** — flight-check forecast closed-loop fix, D8 of the owner's remediation backlog. **Found while investigating the original review's own "17,040 vs 2,386,471, ~140x" claim: it conflated two separate, already-distinct estimates**, both real and both on disk in every session's `packet.json`. `contextManifest.estimatedTokens` (17,040 for `xdl9`) lives at `packet.flightCheck.contextManifest` — `context-assembly.mjs`'s chars/4 sum over six context-load files, honestly self-labeled `"rough chars/4 if every read-by-path input is loaded"` and explicitly out of scope here (that's DLV-8/D10's per-lane context-budget work). The actual owner-facing budget/cost forecast is a *different* field entirely, `packet.flightCheck.recommendation.{estTokens,estCostUsd}` — 400,000 tokens / $0.17 for `xdl9`'s economy tier (`STATIC_USAGE_BY_TIER`), which the D1-corrected recorded actual (2,386,471) beats by ~6x, not 140x. Still a real gap, and a structural one: `recommendAgentConfig`'s own `history` parameter already implements a correct median-of-3+-same-tier-completed-sessions calculation (`estUsageForTier`) — it was simply never fed one, at either of its two call sites (`getRecommendation`'s preview endpoint, and the launch route), making the "learn from experience" path permanently dead no matter how large the fleet grew. Fixed with new `loadRecommendationHistory(ctx)`, reusing the existing `listSessionIds`/`readSession` helpers (already used for the build-lock and active-session-for-item checks, so no new session-scanning logic) — keeps only sessions that reached **ACCEPTED or SHIPPED**, deliberately excluding CANCELLED/BLOCKED/in-flight: a session killed early by a quota, budget, or git-guard stop would teach the median "how much a session costs before it dies," not "how much a session costs to actually finish," which is the number a forecast needs. Wired into both `recommendAgentConfig` call sites; the static per-tier table remains the correct fallback below the existing 3-sample threshold (unchanged `estUsageForTier` logic). **Also addressed the plan's other explicit D8 ask** — "record forecast-vs-actual" — with `computeForecastActual(packet, state)`, a small function computed **on read**, not new persisted state (both halves — `packet.flightCheck.recommendation` and `state.usage.total` — already exist on disk for any session with usage recorded), exposed as a new `forecastActual: {tier, estTokens, estCostUsd, actualTokens, actualCostUsd, tokenRatio}` field on `GET /api/delivery/session`. Reuses `budgets.mjs`'s `totalProcessedTokens` for the actual-tokens sum — the same v2-aware, cache-creation-inclusive formula DLV-37 already made correct — rather than writing new math. DLV-12's finish package doesn't exist yet to persist this into, so exposing it via the existing session-detail read is the honest scope boundary for this change; surfacing it in `SessionDetail.jsx`'s UI is a deliberate, non-blocking follow-up, not folded in here. 3 new tests in `server-routes.test.ts`: one proves 3 fixture ACCEPTED-state sessions' recorded median (2,386,471) reaches a fresh recommendation call instead of the static per-tier fallback; two cover `forecastActual` (present with a correct `tokenRatio` once usage exists; `null` before any usage is recorded, so the field never lies about a session that hasn't run yet). `pnpm test tests/delivery/` green (941/942 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors.
- ◐ 2026-07-29 (`scripts/delivery/recommendation.mjs`, `scripts/delivery/config.mjs`, `scripts/delivery/packet.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/drivers/{claude,codex,fake}.mjs`, `scripts/pm/src/features/delivery/DeliveryHome.jsx`, `tests/delivery/{recommendation,config,packet,server-routes,drivers,drivers-claude,run-session}.test.ts`) **DLV-6 (partial — D9 of the owner's remediation backlog)** — lanes become a real, packet-resolved policy bundle instead of a label the runner never read. New `resolveLanePolicy(lane, config)` (`recommendation.mjs`) resolves FAST/STANDARD/DEEP into `{lane, tier, effortByPhase, budget, maxInternalTurns}` via the existing tier/lane mapping (`tierForLane`, the exact reverse of the pre-existing `laneForTier`) — pure and deterministic, safe to snapshot. Recorded verbatim as a new `packet.lanePolicy` field (`buildPacket` gained a `lanePolicy = null` param, spread conditionally exactly like `budget`/`flightCheck`). Effort resolution in the launch route changed from "an omitted `effort` falls through to the SDK's own default, lane has zero influence" to a per-phase merge — `{...lanePolicy.effortByPhase, ...(effort||{})}` — so a partial owner override (e.g. only `building`) still gets the lane's own defaults for the other three phases instead of silently reverting to the old static `packet.mjs` default (`building:"high"` regardless of lane), which two pre-existing tests had baked in as an assumption and needed updating once the fix was correct. New config field `budgets.laneDefaults.<lane>.maxInternalTurns` (defaults FAST 8 / STANDARD 20 / DEEP 40, schema-validated alongside `maxUsd`/`maxTokens`/`warnPct`) maps, via new `packetMaxInternalTurns(packet)` in `run-session.mjs` threaded through all 5 phase-producing `runGuardedTurn` call sites (DISCOVERY/PLAN/BUILDING/REVIEWING/UAT-prep — deliberately not the lightweight DW-8 handoff-verification turn), onto the claude driver's `buildSessionOptions`'s new `maxTurns` option — which is the SDK's own `Options.maxTurns`, the literal missing cap behind Cost Anatomy §5's "13 runner turns became ~40 internal model calls" and Turn 7's "96,535 input tokens, zero tool uses." **Found and fixed a second bug while wiring the UI**: `DeliveryHome.jsx`'s Flight-Check budget prefill (`budgetDefault`) was hardcoded to `laneDefaults.standard` regardless of `selectedLane` — a FAST launch pre-filled the $2/2M STANDARD envelope, not FAST's $0.50/500K; fixed by reordering the component's derived-state so `selectedLane` is computed before `budgetDefault` and keying the lookup off it. Codex's `runTurn` accepts `maxTurns` for interface parity with the claude driver but does not forward it to the SDK — no verified `@openai/codex-sdk` `TurnOptions` equivalent to `Options.maxTurns` exists, and Cost Anatomy's own fleet data shows every observed real session used the claude provider, so an unverified guess was judged a worse risk (silent no-op or a hard SDK error) than an honestly-documented gap, revisitable if a real codex session ever shows the same turn-amplification pattern. 15 new tests across 7 files: `resolveLanePolicy`/`tierForLane` unit tests (incl. a config-with-no-laneDefaults null-safety case), config validation, `buildPacket` persists/omits `lanePolicy`, the launch route resolves FAST correctly end-to-end and merges a partial owner override correctly, the fake driver tracks a per-turn `maxTurns` onto its ref (mirroring the existing model/effort override test), `buildSessionOptions` passes `maxTurns` through (and omits it for 0/null/undefined), and a full `run-session.mjs` integration test proving `packet.lanePolicy.maxInternalTurns` reaches `state.driver.ref.maxTurns` via the real turn-dispatch path (plus the inverse: a pre-D9 packet with no `lanePolicy` stays uncapped, unchanged behavior). `pnpm test tests/delivery/` green (954/955 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set — three new inference-quirk errors the change would otherwise have introduced (matching the pre-existing `budget`/`flightCheck` pattern in `packet.test.ts`) were fixed at the source by correcting `resolveLanePolicy`'s own JSDoc `@returns` type instead of left to accumulate. **Still open, tracked separately**: per-lane context budgets + the validation-contract ladder (D10, needs D5+D9 — both now satisfied) and the "route trivial work around the pipeline" hard triage gate (D11, needs D8 — now satisfied).
- ◐ 2026-07-29 (`scripts/delivery/config.mjs`, `scripts/delivery/recommendation.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/{config,recommendation,run-session}.test.ts`) **DLV-11 (partial — D10 of the owner's remediation backlog; DLV-10's AC coverage matrix and DLV-8's context-budget half both remain separately open)** — risk-based validation ladder. **Found while scoping this that delta-vs-baseline at each rung was already fully built** — `validation-baseline.mjs`'s `classifyValidationFailure` (shipped under DLV-5) already produces a per-command `commandDeltas` verdict (`baseline-equivalent`/`regressed`) and was already wired into `handleValidating`; nothing needed adding there. The real gap: `VALIDATION_COMMANDS` ran unconditionally for every session regardless of lane, and a skipped rung had no representation at all in `results` — it was simply absent, with `renderValidationReportMd`'s only handling being a bare `(skipped)` text label for a missing key and zero reason recorded. That silence is the literal mechanism behind DLV-11's own flagship complaint: `whdv`'s "lint (skipped)" / "test (skipped)" turned out to be the *agent's own unverified build-log prose*, never contradicted by the runner because the runner kept no record to contradict it with. New config section `validation.laneLadder` (schema-validated: `rungs` must be a non-empty array of known command keys, `targetedTest` a boolean) resolves through `resolveLanePolicy` (extending D9's function) into `packet.lanePolicy.validationLadder`: FAST is `{rungs:["typecheck","test"], targetedTest:true}`, STANDARD/DEEP are the untouched full ladder. `runValidationCommands` now populates *every* `VALIDATION_COMMANDS` key in `results`, always — a lane-ladder exclusion is recorded before any command even runs (`{ok:true, skipped:true, reason:"not in this lane's validation ladder"}`), and the pre-existing fail-fast short-circuit (one failing rung stops the rest) now records the same shape instead of leaving the key missing (`reason:"not run — an earlier validation command failed"`). `renderValidationReportMd` renders a `SKIPPED — <reason>` line, structurally distinct from `PASS`/`FAIL`/`PASS ON DELTA`, so a report can never read as if a rung simply wasn't part of the session. Targeted-test mode runs `pnpm exec vitest related <state.workspace.changedFiles> --passWithNoTests` for the "test" rung instead of the full suite — **verified live against this real repo before wiring it in**, not assumed: `npx vitest related scripts/delivery/recommendation.mjs` correctly narrowed 955 tests across 33 files down to 117 tests across the 3 files that actually import it (real dependency-graph targeting, not a naive glob), and a docs-only file with zero related tests exited 0 under `--passWithNoTests` rather than reading as a failure — the exact FAST-lane scenario (a Feature State/checklist edit) this is built for. "Every skipped rung explicit and *authorized*" is interpreted as the owner's own explicit lane choice at Flight-Check (an already-reviewed, already-typed-acknowledged panel per DLV-2) — selecting FAST *is* authorizing FAST's lighter ladder, so no second confirmation was added on top of it; noted here rather than left ambiguous. 9 new tests: config schema validation, `resolveLanePolicy`'s ladder resolution + fallback-to-full-ladder when unconfigured, `runValidationCommands` unit tests (lane-skip recorded before any command runs and never fails the session; the real `vitest related` invocation shape; the untargeted fallback when no changed files are known), and 2 full `advanceSession` integration tests — one drives a FAST-lane packet through DISCOVERY→PLAN→BUILDING→VALIDATING→UAT_READY and asserts every `runValidation` call received `rungs:["typecheck","test"]` and that `validation.json`/`validation-report.md` show lint as a governed skip, the other proves a pre-D9 packet with no `lanePolicy` still runs the full ladder unchanged. 4 pre-existing tests updated for the new honest behavior (a fail-fast-skipped rung is now a structured `{skipped:true}` result, not `undefined`). `pnpm test tests/delivery/` green (962/963 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set — 8 transient errors the change would otherwise have introduced (6 from an under-typed test callback signature, 2 from `excerpt` correctly becoming optional in the broadened skip-aware result shape) were fixed at the source rather than left to accumulate.
- ✅ 2026-07-29 (`scripts/delivery/classify.mjs`, `scripts/delivery/validation-baseline.mjs`, `scripts/delivery/server-routes.mjs`, `scripts/pm/src/features/delivery/DeliveryHome.jsx`, `tests/delivery/{classify,server-routes}.test.ts`) **DLV-39** — hard triage gate at launch, D11 of the owner's remediation backlog. Refuses to launch an S-effort item with zero risk flags through the full pipeline without a typed override — refuses *entry* only, gates inside a running session are never touched, in every lane, always. New `isTrivialLaunchCandidate(item, riskFlags)` in `classify.mjs`: real changed files don't exist yet at launch time, so "single-file"/"docs-only" isn't directly knowable pre-launch — the best available proxy reuses the exact `riskFlags` vocabulary the Flight-Check panel already computes (`buildLaunchPreview`'s optional-capabilities list: money-domain, frontend-impl, backend-impl, product-ba-refinement), rather than inventing a second, parallel concept. Deliberately, a vague item (product-ba-refinement) never triage-refuses even at S-effort — it needs DISCOVERY's clarifying pass, not a bypass around it. New `TRIAGE_OVERRIDE_ACK = "LAUNCH ANYWAY"` in `validation-baseline.mjs`, same house pattern as `DIRTY_TREE_ACK`/`RED_BASELINE_ACK`, recorded into both `packet.workspace.acknowledgments.triage` and `packet.flightCheck.baseline.acknowledgments.triage` for audit when used. The refusal message shows the item's own D8-corrected forecast (`recommendation.estCostUsd`/`estTokens`), not a fixed illustrative number. Wired symmetrically into the server (`startSession` throws 400 with the typed-override instruction) and the launch UI — `DeliveryHome.jsx` imports the *same* `isTrivialLaunchCandidate`/`TRIAGE_OVERRIDE_ACK` functions the server enforces (both are zero-dependency modules that already bundle cleanly into the browser build, same pattern as the existing `ALWAYS_ON_CAPABILITIES`/`DIRTY_TREE_ACK` imports), so client and server can never drift apart on what triggers the gate; a conditional forecast-warning block + typed-input field appears only when it would fire, mirroring the pre-existing dirty-tree/red-baseline UI exactly. **Found while writing the test fixture, not a bug in the new code**: every `Budget`-campaign item unconditionally carries `frontend-impl` (`CAMPAIGN_MODULE_GLOBS.Budget` is entirely `src/features/**` paths, all under `FRONTEND_GLOB_ROOTS`), regardless of what the item text itself says — so no Budget item, however trivial, can ever hit this gate. Judged correct on reflection: Budget items are money-adjacent product code far more often than not, and the actual DLV-28-shaped triviality this targets lives in PM/docs-only campaigns like Delivery 10x itself (its real campaign, reused in the test fixture for authenticity), not inside a money module. 8 new tests: 4 `classify.test.ts` unit tests on the predicate itself (fires on S+zero-flags; never on M/L-effort; never with any risk flag including product-ba-refinement; null/malformed-input safe) and 3 `server-routes.test.ts` integration tests (refuses without the override; launches and records the ack with the override; an M-effort item launches unimpeded even at zero risk flags). `pnpm test tests/delivery/` green (969/970 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set (one new error from an under-typed test input fixed at the source — `isTrivialLaunchCandidate`'s JSDoc widened to accept `null`, matching what the function already handled correctly at runtime, not a workaround).
- ◐ 2026-07-29 (`scripts/delivery/transcript.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/drivers/claude.mjs`, `scripts/pm/src/features/delivery/{ConversationView,TimelineView,roleColors}.{jsx,js}`, `tests/delivery/{drivers-claude,run-session}.test.ts`) **DLV-40 + DLV-17 (both partial — D12, the last item of the owner's 12-item remediation backlog)** — role attribution, color, transcript integrity, raw SDK linkage. No subagents exist (`Task` stays banned); this makes the roles that already exist legible and closes the gap that would have let a future subagent go untraced. **Role stamping**: new `PHASE_ROLES`/`roleForPhase(phase)` in `transcript.mjs` map each of the six turn-producing phases (DISCOVERY/PLAN/BUILDING/REVIEWING/UAT_PREP/HANDOFF) to a role label (Discovery/Planner/Builder/Reviewer/UAT/Handoff-verifier); `buildTurnEntry` gained a `role` field and `runGuardedTurn` stamps it onto both the turn entry and every raw record it writes (previously raw records carried `provider` only — nothing distinguished a DISCOVERY record from a REVIEWING one at the record level). **`parent_tool_use_id`**: verified against the installed `@anthropic-ai/claude-agent-sdk` 0.3.207's own `sdk.d.ts` before wiring in, not assumed (`parent_tool_use_id: string|null` is a real top-level field on `SDKAssistantMessage`/`SDKUserMessage`) — `mapSdkMessageToRawRecords` now records it as `parentToolUseId` on every record it emits unconditionally, closing the exact traceability gap the forensics found (the mapper never read the field at all, so a subagent's output would previously have been silently merged into the orchestrator's own shard). **`ROLE_COLORS`**: new `scripts/pm/src/features/delivery/roleColors.js` imports `PHASE_ROLES` directly from `transcript.mjs` rather than re-typing role names, so a role the UI doesn't recognize is a build-visible bug, not a silent, drifted fallback; wired into `ConversationView.jsx` (phase-group headers + every `TurnCard`) and `TimelineView.jsx` (`event.phase`, falling back to a plain `Chip` for non-role gate states like `SPEC_READY`/`BLOCKED`) — every consumer renders icon + color + the role's own text label together by construction, honoring the "never color-only" rule `TimelineView` already observed before any of this existed. **Transcript integrity (DLV-17's gap-check third)**: new `findMissingTurnIds(turnCounter, closedTurnIds, promptTurnIds)` — deliberately distinct from the pre-existing `findOrphanedTurnIds` (a prompt file exists but the turn never closed, reconciled on `--resume`): this catches the whdv postmortem's actual shape, `turnCounter` reaching 23 with turns 0013–0018/0021 having **no trace at all**, a gap the orphan-check cannot see because it can only compare against files that exist. New `runTranscriptGapCheck` runs at both real terminal transitions (`consumeCancel`→CANCELLED, `consumeAcceptedDecision`→SHIPPED) and always emits `transcript.gap.checked`, including the zero-gap case, so the check having run is itself auditable. **Raw SDK linkage — reference, don't hoard**: new `resolveRawTranscriptPath`/`computeRawTranscriptPointer` in `drivers/claude.mjs`. `state.driver.ref.id` doubles as both the SDK session UUID and the filename Claude Code's own CLI writes the session's complete raw transcript to, under `~/.claude/projects/<cwd-slug>/<uuid>.jsonl` — the exact slug format confirmed empirically against this machine's real on-disk layout before wiring in, not assumed. `withRawTranscriptLinkage` runs at the same two terminal points, Claude-only (Codex has no equivalent, checked via `packet.agent`), and persists `{path, exists, sizeBytes, sha256, resolvedAt}` onto `state.driver.rawTranscript` — a pointer plus integrity hash, never a copy; `exists:false` (aged out or never existed) is a valid result, not an error. **Deliberately not attempted**: the opt-in pinned-export mechanism (redaction pass + de-dup against `transcript/` — a genuinely separate, write-side feature the plan itself frames as optional/off-by-default); surfacing the raw pointer in the session-detail UI (only the state.json field + driver-level tests exist so far); proactive per-turn stub records and a runner-heartbeat stalled-session watchdog (DLV-17's other two thirds). **`/pm/live` is not wired to any of this** — confirmed real architectural separation before attempting, not skipped by oversight: `src/features/pm-live/` (the separate Next.js surface) cannot statically import `scripts/delivery/*.mjs` the way `scripts/pm/`'s Preact build already does (different bundler/module resolution entirely — grepped `src/features/pm-live/` and `src/components/pm-live/` and found only comments referencing delivery config as its semantic source, never an actual import; the bridge relays values as JSON, not as imported JS modules), so extending role/color there needs the bridge to relay per-turn role data it doesn't carry today — a separate, larger effort than this change's scope. 9 new tests: `drivers-claude.test.ts` (parent_tool_use_id propagation on both assistant/user messages via a new dedicated test; `resolveRawTranscriptPath`'s slug format asserted against this machine's real observed `~/.claude/projects/` layout; `computeRawTranscriptPointer`'s exists-true/exists-false/no-sessionId cases against a temp-dir `homeDir` test seam added specifically for testability); 4 pre-existing `mapSdkMessageToRawRecords`/`onRaw`-integration tests updated for the new `parentToolUseId` field; 2 new `run-session.test.ts` integration tests (zero-gap SHIPPED session; a deliberately-bumped `turnCounter` reporting the exact expected missing ids, plus a codex-agent session proving `rawTranscript` is never even attempted for a non-claude provider). `pnpm test tests/delivery/` green (978/979 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors; `node scripts/pm/build.mjs` succeeds (169,865 B JS, up from 168,381 B, confirming the new Preact imports resolve cleanly). This closes the last item of the owner's 2026-07-28 12-item remediation backlog (D1–D12) — see the plan file (`~/.claude/plans/i-had-multiple-pm-immutable-fairy.md`) for the full original scope; several items landed partial by deliberate, documented scope decision rather than full closure (DLV-6/DLV-8/DLV-11/DLV-17/DLV-40), each with its own remaining-work note above.
- ✅ 2026-07-29 (`scripts/delivery/drivers/claude.mjs`, `tests/delivery/drivers-claude.test.ts`) **DLV-41** — closed the one Bash-level route to the live Supabase project that survived the guardrail review otherwise rated "excellent." `canUseTool`'s Bash branch already denied git-mutating commands (allowlist-based, DLV-1-era) and secret/credential-path references (`.env`, `id_rsa`, etc.), but let through everything else — `psql`, the `supabase` CLI, `curl`/`wget`, any script text — because the MCP seal (`strictMcpConfig` + `mcp__*` denial, DLV-33) only ever covered the MCP tool-call surface, not a plain shell command reaching the same project by HTTP or its own CLI. Hard Rule #26 names exactly this set of routes as forbidden "equivalent back doors": the `supabase` CLI, `psql`, `curl` against the PostgREST/RPC endpoints, and any script using `SUPABASE_SERVICE_ROLE_KEY`/`supabaseAdmin()`. New `containsDbWriteEscape(command)` denies all four unconditionally — deliberately no read-only carve-out the way git gets one, since there is no legitimate delivery-session reason to touch the live project at all. Implementation: tokenizes each `&&`/`;`/`|`/newline-split shell segment and checks every token's executable name (not just the first) against `{supabase, psql}` — catching runner-prefixed invocations like `npx supabase migration up` or `pnpm dlx supabase db push`, not only a bare leading command; separately flags `curl`/`wget` segments whose text matches a Supabase domain (`*.supabase.co/in/net`) or a `/rest|auth|storage/v1/`/`/rpc/` path; and does a whole-command text match for the two service-role-key env-var spellings (`SUPABASE_SERVICE_ROLE_KEY`, `NEXT_SUPABASE_SERVICE_ROLE_KEY`, both live in `docs/ENV.md`) and `supabaseAdmin`. Wired into the Bash branch of `canUseTool` immediately after the existing `containsSecretReference` check, same position in the decision chain. 9 new tests: 6 for the standalone `containsDbWriteEscape` predicate (CLI, psql, curl/wget positive and negative, service-role-key/`supabaseAdmin` text, ordinary commands unaffected) plus 2 `buildCanUseTool` integration tests proving the wiring itself denies/allows through the real callback, not just the predicate in isolation. `pnpm test tests/delivery/` green (938/939 — the one failure is the pre-existing unrelated DLV-34 handoff bug); `tsc` shows the identical pre-existing error set, no new errors.
- ✅ 2026-07-25 **DLV-28** — D3 smoke test finally reached ACCEPTED. Session `s-20260725-181118-xdl9` (FAST/Haiku/low), same self-referential item as the two prior failed attempts. Verifies DLV-29's fix live: hit the identical budget-exhausted/manual-gate collision twice more in flight (PLAN_READY then UAT_READY), and both times the owner-authorized raise correctly restored the gate instead of stranding the session. First session ever to complete the full pipeline end-to-end (previously 0/8). The agent itself declined to fabricate the PM-trace write steps ahead of knowing its own outcome, citing Hard Rule 25, and correctly deferred them to the owner — a genuine positive signal for M3 (Truthful Finish) ahead of DLV-10/11/12 landing.
- ✅ 2026-07-25 (`scripts/delivery/run-session.mjs`, `tests/delivery/run-session.test.ts`) **DLV-29** — found and fixed during a real D3 smoke test (two live FAST/Haiku/low sessions, both stranded). `enforceBudgetBoundary` was unconditionally overwriting a fresh manual-gate `awaiting` (SPEC_READY/PLAN_READY/UAT_READY/NEEDS_DECISION/…) with the budget-exhausted pause's `awaiting` the instant a phase transition and a budget breach landed on the same tick; the `set-budget` resume path only knew how to null the budget `awaiting`, never restore what it had replaced — permanently stranding the session (no dashboard panel, `POST /api/delivery/decision` 409s "awaiting nothing" forever). Fix carries the prior `awaiting` through the pause (`priorAwaiting`) and restores it verbatim on an audited raise, including gate-specific payloads like NEEDS_DECISION's `questions`. Regression test reproduces the exact PLAN_READY collision end-to-end: budget trips mid-gate, raise resumes, the plan decision that was always pending is proven to actually unblock the session into BUILDING.
- ✅ 2026-07-25 (`src/features/pm-live/types.ts`, `src/features/pm-live/chartTheme.ts`, `src/components/pm-live/views/DeliveryView.tsx`, `scripts/pm/bridge.mjs`, `tests/pm-live-derive.test.ts`) **DLV-27** — aligned PM Live’s usage contract with providers that intentionally omit a USD price. Delivery cards render `—`, never crash or pretend the amount is zero; gate notifications instead state “Cost unavailable.”
- ✅ 2026-07-25 (`src/features/pm-live/`, `src/components/pm-live/`, `src/app/pm/live/pm-live.css`, `scripts/pm/bridge.mjs`, `tests/pm-live-derive.test.ts`, `tests/pm-live-query.test.ts`) **DLV-24, DLV-25, DLV-26** — `/pm/live` promoted from a 3-tab relay to a responsive PM application. Five views (Overview / Board / Delivery / Usage / Campaigns) from one component tree: bottom nav under `lg`, side rail + widget grid above it. Two new `pm_live` row kinds — `rollups` (per-campaign lane/severity/effort counts, pain bullets, lint health) and `history` (✅ done-stamp completions) — are derived laptop-side from the *existing* `severityItems`/`sumSeverity`/`lintChecklist` helpers and cost **no migration**: they ride the existing `(id, kind, payload jsonb)` shape. The Board gains the desktop board's own filter grammar (a TS port of `queryLang.js`, parity-tested) plus `lane:`/`e:`, group-by, sort-by, virtualized rows and a detail sheet; a Zustand store confines the 10s heartbeat to the status chip rather than re-rendering ~480 rows. The three commands the bridge had always accepted but nothing could issue — `capture`, `ask`, `abort-turn` — finally have an interface, which also makes DLV-23's Undo strip reachable for the first time. Row tap moved from *launch* to *open detail* (Hard Rule 2); a tap is too cheap to spend a budget envelope on. Gate approvals, `set-budget`, `rotate` and `fork` remain laptop-only and unchanged. 75 tests; the ~200 hardcoded colour literals became a scoped `[data-pm-live]` token block.
- ✅ 2026-07-25 (`scripts/pm/bridge.mjs`, `migrations/2026-07-25_pm-mobile-relay.sql`, `src/app/pm/live/`, `src/components/pm-live/`, `src/app/api/pm/notify/route.ts`, `tests/pm-bridge.test.ts`) **DLV-20, DLV-21, DLV-22** — Mobile Command Surface. `/pm/live` is a phone-installable checklist + delivery view fed by an outbound-only Supabase relay (`pnpm pm --bridge`); pm-server's own binding stays `127.0.0.1`. Every phone command is a thin allowlisted wrapper around the existing `routeDelivery()` handlers — no new delivery business logic. Revoke-tier (pause/abort-turn/cancel) is always reachable; grant-tier (launch, with a mandatory envelope; answer, only on an open question gate) is deliberately narrower; gate approval and budget increases are never exposed. Push notifications consume the previously-unread `notification.requested` event. Amends, and does not reopen, the "Remote decision controls" rejection — see [6 · Design Debates](<6 - Design Debates & Rejected Ideas.md>).
- ✅ 2026-07-24 (`scripts/delivery/recommendation.mjs`, `scripts/delivery/packet.mjs`, `scripts/delivery/server-routes.mjs`, Delivery PM launch UI, focused delivery tests) **DLV-2** — launch now requires one reviewed Flight-Check with a valid FAST/STANDARD/DEEP lane and budget/baseline authorization. The panel surfaces campaign Action Plan ACs (or the SPEC fallback), a broad-scope-aware model recommendation and mismatch warning, an estimated per-phase context manifest, and capabilities/risk signals; the server reconstructs and persists the authoritative snapshot in `packet.json`.
- ✅ 2026-07-24 (`scripts/delivery/validation-baseline.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/server-routes.mjs`, Delivery PM launch UI, focused delivery tests) **DLV-5** — launch preflight now persists an authoritative baseline receipt, lists every pre-existing changed file as `not-session-owned`, and requires `DIRTY TREE` / `RED BASELINE` typed acknowledgments when applicable. Build-turn git fingerprints populate session-touched files and classify ownership as `session-owned` or `shared`; validation advances on acknowledged baseline-equivalent failures while any increased failure count or new diagnostic in a session-touched file enters the normal fix loop.
- ✅ 2026-07-24 (`scripts/delivery/budgets.mjs`, `scripts/delivery/packet.mjs`, `scripts/delivery/run-session.mjs`, `scripts/delivery/server-routes.mjs`, Delivery PM UI, focused delivery tests) **DLV-1** — every new session now records an owner-authorized cost/token envelope in `packet.json` (or an explicit typed `NO CAP` authorization); the runner evaluates packet/current usage only between turns, emits each warning once, and pauses on a hard cap without changing the state-machine phase or discarding the completed turn. Cap hits write `artifacts/finish/budget.json` + `summary.md`, request a notification, and resume only after an audited raise-only `set-budget` control. Existing pre-DLV-1 packets retain the global-config backstop.
- ✅ 2026-07-24 (`scripts/delivery/quota.mjs`, `scripts/delivery/run-session.mjs`, `tests/delivery/quota.test.ts`) **DLV-4** — spend-limit, quota, and authentication failures now pause without retrying; resumptions require an audited reason and fresh provider preflight. Identical transient failures use the configurable two-retry default, then enter `NEEDS_DECISION` and emit a dashboard notification request.
- ✅ 2026-07-24 (`tests/delivery/config.test.ts`, `tests/delivery/runner-crash.test.ts`) **DLV-3** — Delivery config now validates owner edits, atomically snapshots valid config for last-known-good fallback, exposes degraded mode on the dashboard, and parks repeated identical runner startup crashes behind a one-minute restart backoff.
